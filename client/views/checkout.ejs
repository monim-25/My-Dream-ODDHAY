<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>পেমেন্ট চেকআউট | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..900&family=Noto+Sans+Bengali:wght@300..900&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <script>
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: { "primary": "#137fec", "background-light": "#f6f7f8", "background-dark": "#101922" },
                    fontFamily: { "display": ["Lexend", "Noto Sans Bengali", "sans-serif"] },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Lexend', 'Noto Sans Bengali', sans-serif;
        }
    </style>
</head>

<body
    class="bg-background-light dark:bg-background-dark text-[#0d141b] dark:text-slate-100 min-h-screen flex items-center justify-center p-4">

    <div class="max-w-4xl w-full grid grid-cols-1 md:grid-cols-2 gap-8">

        <!-- Left: Order Summary -->
        <div
            class="bg-white dark:bg-slate-900 rounded-[2rem] p-8 border border-slate-200 dark:border-slate-800 shadow-xl h-fit">
            <h2 class="text-xl font-black mb-6 flex items-center gap-2">
                <span class="material-symbols-outlined text-primary">shopping_cart</span> অর্ডার সামারি
            </h2>

            <div class="flex gap-4 mb-6">
                <img src="<%= course.thumbnail || 'https://images.unsplash.com/photo-1516321318423-f06f85e504b3?auto=format&fit=crop&q=80&w=200' %>"
                    class="w-24 h-24 object-cover rounded-2xl bg-slate-100 dark:bg-slate-800">
                <div>
                    <span class="text-[10px] font-bold bg-primary/10 text-primary px-2 py-1 rounded-full">
                        <%= course.category %>
                    </span>
                    <h3 class="font-bold text-lg leading-tight mt-1 mb-1">
                        <%= course.title %>
                    </h3>
                    <p class="text-xs text-slate-500">
                        <%= course.subject %> • <%= course.classLevel %>
                    </p>
                </div>
            </div>

            <div class="space-y-4 border-t border-slate-100 dark:border-slate-800 pt-6">
                <div class="flex justify-between items-center text-sm">
                    <span class="text-slate-500">মূল্য (১ বছরের জন্য)</span>
                    <span class="font-bold">৳<%= course.plans[0].price %></span>
                </div>
                <div class="flex justify-between items-center text-sm">
                    <span class="text-slate-500">ভ্যাট (০%)</span>
                    <span class="font-bold">৳0</span>
                </div>
                <div
                    class="flex justify-between items-center text-lg font-black text-primary border-t border-slate-100 dark:border-slate-800 pt-4">
                    <span>সর্বমোট</span>
                    <span>৳<%= course.plans[0].price %></span>
                </div>
            </div>

            <div
                class="mt-8 p-4 bg-amber-50 dark:bg-amber-900/20 text-amber-700 dark:text-amber-400 text-xs rounded-xl flex gap-3">
                <span class="material-symbols-outlined text-lg shrink-0">info</span>
                <p>পেমেন্ট সম্পন্ন করার পর কন্টেন্ট অ্যাক্সেস পেতে ১-৫ মিনিট সময় লাগতে পারে।</p>
            </div>
        </div>

        <!-- Right: Payment Method -->
        <div
            class="bg-white dark:bg-slate-900 rounded-[2rem] p-8 border border-slate-200 dark:border-slate-800 shadow-xl">
            <h2 class="text-xl font-black mb-6 flex items-center gap-2">
                <span class="material-symbols-outlined text-emerald-500">payments</span> পেমেন্ট মেথড
            </h2>

            <!-- Tabs -->
            <div class="flex gap-4 mb-8 overflow-x-auto pb-2">
                <button onclick="selectMethod('bkash')" id="btn-bkash"
                    class="flex-1 min-w-[100px] py-3 px-4 rounded-xl border-2 border-pink-500 bg-pink-500/5 text-pink-600 font-bold flex flex-col items-center gap-1 transition-all">
                    <span class="font-black text-xs">bKash</span>
                    <span class="text-[10px]">Merchant</span>
                </button>
                <button onclick="selectMethod('nagad')" id="btn-nagad"
                    class="flex-1 min-w-[100px] py-3 px-4 rounded-xl border border-slate-200 dark:border-slate-700 text-slate-500 flex flex-col items-center gap-1 transition-all opacity-50 hover:opacity-100">
                    <span class="font-black text-xs">Nagad</span>
                    <span class="text-[10px]">Personal</span>
                </button>
            </div>

            <!-- bKash Form -->
            <div id="form-bkash" class="space-y-6">
                <div class="text-center mb-6">
                    <p class="text-sm text-slate-500 mb-2">এই মার্চেন্ট নম্বরে পেমেন্ট করুন:</p>
                    <div class="text-2xl font-black text-slate-800 dark:text-white tracking-wider font-mono bg-slate-100 dark:bg-slate-800 py-3 rounded-xl select-all cursor-pointer"
                        onclick="navigator.clipboard.writeText('017XXXXXXXX')">
                        017XXXXXXXX
                    </div>
                </div>

                <form action="/buy-course/<%= course._id %>" method="POST" class="space-y-4">
                    <input type="hidden" name="planIndex" value="0">
                    <input type="hidden" name="paymentMethod" value="bkash">

                    <div>
                        <label class="block text-xs font-bold mb-2 ml-1 text-slate-500">যে নম্বর থেকে টাকা
                            পাঠিয়েছেন</label>
                        <input type="text" name="phoneNumber" placeholder="017..."
                            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 outline-none focus:border-pink-500 transition-all font-bold text-sm"
                            required>
                    </div>
                    <div>
                        <label class="block text-xs font-bold mb-2 ml-1 text-slate-500">ট্রানজেকশন আইডি (TrxID)</label>
                        <input type="text" name="transactionId" placeholder="AX5G..."
                            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 outline-none focus:border-pink-500 transition-all font-bold text-sm uppercase"
                            required>
                    </div>

                    <button type="submit"
                        class="w-full py-4 bg-pink-500 text-white rounded-xl font-bold hover:bg-pink-600 transition-all shadow-lg shadow-pink-500/30 mt-4">
                        পেমেন্ট ভেরিফাই করুন
                    </button>
                </form>
            </div>

            <!-- Nagad Form (Hidden initially) -->
            <div id="form-nagad" class="hidden space-y-6">
                <div class="text-center mb-6">
                    <p class="text-sm text-slate-500 mb-2">এই পার্সোনাল নম্বরে সেন্ড মানি করুন:</p>
                    <div
                        class="text-2xl font-black text-slate-800 dark:text-white tracking-wider font-mono bg-slate-100 dark:bg-slate-800 py-3 rounded-xl select-all cursor-pointer">
                        019XXXXXXXX
                    </div>
                </div>
                <form action="/buy-course/<%= course._id %>" method="POST" class="space-y-4">
                    <input type="hidden" name="planIndex" value="0">
                    <input type="hidden" name="paymentMethod" value="nagad">

                    <div>
                        <label class="block text-xs font-bold mb-2 ml-1 text-slate-500">যে নম্বর থেকে টাকা
                            পাঠিয়েছেন</label>
                        <input type="text" name="phoneNumber" placeholder="019..."
                            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 outline-none focus:border-orange-500 transition-all font-bold text-sm"
                            required>
                    </div>
                    <div>
                        <label class="block text-xs font-bold mb-2 ml-1 text-slate-500">ট্রানজেকশন আইডি (TrxID)</label>
                        <input type="text" name="transactionId" placeholder="7U..."
                            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 outline-none focus:border-orange-500 transition-all font-bold text-sm uppercase"
                            required>
                    </div>

                    <button type="submit"
                        class="w-full py-4 bg-orange-500 text-white rounded-xl font-bold hover:bg-orange-600 transition-all shadow-lg shadow-orange-500/30 mt-4">
                        পেমেন্ট ভেরিফাই করুন
                    </button>
                </form>
            </div>

            <a href="/course-details/<%= course._id %>"
                class="block text-center text-xs font-bold text-slate-400 mt-6 hover:text-primary">ফিরে যান</a>
        </div>
    </div>

    <script>
        function selectMethod(method) {
            const btnBkash = document.getElementById('btn-bkash');
            const btnNagad = document.getElementById('btn-nagad');
            const formBkash = document.getElementById('form-bkash');
            const formNagad = document.getElementById('form-nagad');

            if (method === 'bkash') {
                formBkash.classList.remove('hidden');
                formNagad.classList.add('hidden');

                btnBkash.classList.add('border-2', 'border-pink-500', 'bg-pink-500/5', 'text-pink-600');
                btnBkash.classList.remove('border', 'border-slate-200', 'text-slate-500', 'opacity-50');

                btnNagad.classList.remove('border-2', 'border-orange-500', 'bg-orange-500/5', 'text-orange-600');
                btnNagad.classList.add('border', 'border-slate-200', 'text-slate-500', 'opacity-50');
            } else {
                formNagad.classList.remove('hidden');
                formBkash.classList.add('hidden');

                btnNagad.classList.add('border-2', 'border-orange-500', 'bg-orange-500/5', 'text-orange-600', 'opacity-100');
                btnNagad.classList.remove('border', 'border-slate-200', 'text-slate-500', 'opacity-50');

                btnBkash.classList.remove('border-2', 'border-pink-500', 'bg-pink-500/5', 'text-pink-600');
                btnBkash.classList.add('border', 'border-slate-200', 'text-slate-500', 'opacity-50');
            }
        }
    </script>
</body>

</html>