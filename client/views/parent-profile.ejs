<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>প্রোফাইল | অধ্যায়</title>
    <%- include('partials/head') %>
        <style>
            .profile-gradient {
                background: linear-gradient(135deg, #137fec 0%, #005bb5 100%);
            }

            .glass-card {
                backdrop-filter: blur(16px);
                background: rgba(255, 255, 255, 0.8);
                border: 1px solid rgba(255, 255, 255, 0.2);
            }

            .dark .glass-card {
                background: rgba(15, 23, 42, 0.8);
                border: 1px solid rgba(255, 255, 255, 0.05);
            }
        </style>
</head>

<body class="bg-slate-50 dark:bg-[#0f172a] text-slate-900 dark:text-slate-100 min-h-screen">
    <div class="flex flex-col md:flex-row min-h-screen">
        <!-- Sidebar Navigation (Reusable) -->
        <aside
            class="w-full md:w-72 bg-white dark:bg-slate-900 border-r border-slate-200 dark:border-slate-800 p-8 flex flex-col sticky top-0 md:h-screen">
            <div class="mb-12">
                <a href="/" class="flex items-center gap-3 group">
                    <div class="text-primary transform group-hover:rotate-12 transition-transform duration-300">
                        <svg class="size-10" fill="none" viewbox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
                            <path d="M4 4H17.3334V17.3334H30.6666V30.6666H44V44H4V4Z" fill="currentColor"></path>
                        </svg>
                    </div>
                    <h1 class="text-2xl font-black tracking-tighter">ODDHAY</h1>
                </a>
            </div>

            <nav class="space-y-4 flex-1">
                <a href="/parent/dashboard"
                    class="flex items-center gap-3 px-6 py-4 rounded-2xl text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-800/50 font-bold transition-all">
                    <span class="material-symbols-outlined">family_restroom</span> মনিটর প্যানেল
                </a>
                <a href="/profile"
                    class="flex items-center gap-3 px-6 py-4 rounded-2xl bg-primary/10 text-primary font-black transition-all">
                    <span class="material-symbols-outlined">person</span> প্রোফাইল
                </a>
            </nav>

            <div class="pt-8 border-t border-slate-100 dark:border-slate-800">
                <a href="/logout"
                    class="flex items-center gap-3 px-6 py-4 rounded-2xl text-red-500 hover:bg-red-50 dark:hover:bg-red-500/10 font-black transition-all">
                    <span class="material-symbols-outlined">logout</span> লগআউট
                </a>
            </div>
        </aside>

        <!-- Main Area -->
        <main class="flex-1 p-6 md:p-12 max-w-5xl mx-auto w-full">
            <header class="flex justify-between items-center mb-12">
                <h2 class="text-3xl font-black italic">আমার প্রোফাইল</h2>
                <button onclick="document.documentElement.classList.toggle('dark')"
                    class="size-12 rounded-2xl bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 shadow-sm flex items-center justify-center">
                    <span class="material-symbols-outlined dark:hidden">dark_mode</span>
                    <span class="material-symbols-outlined hidden dark:block text-amber-400">light_mode</span>
                </button>
            </header>

            <div class="space-y-8 animate-pop">
                <!-- Profile Header Card -->
                <div
                    class="bg-white dark:bg-slate-900 rounded-[3rem] p-8 md:p-12 border border-slate-100 dark:border-slate-800 shadow-sm flex flex-col md:flex-row items-center gap-8 relative overflow-hidden">
                    <div class="absolute -right-20 -top-20 size-64 bg-primary/5 rounded-full blur-3xl"></div>

                    <div class="relative">
                        <div
                            class="size-32 md:size-40 rounded-[2.5rem] bg-gradient-to-br from-primary to-blue-600 flex items-center justify-center text-white text-5xl font-black shadow-2xl shadow-primary/30 ring-4 ring-white dark:ring-slate-800">
                            <%= user.name[0] %>
                        </div>
                        <label for="avatar-upload"
                            class="absolute -bottom-2 -right-2 size-10 bg-white dark:bg-slate-800 rounded-xl shadow-lg border border-slate-100 dark:border-slate-700 flex items-center justify-center cursor-pointer hover:text-primary transition-all">
                            <span class="material-symbols-outlined text-sm">photo_camera</span>
                        </label>
                        <input type="file" id="avatar-upload" class="hidden" accept="image/*">
                    </div>

                    <div class="text-center md:text-left flex-1">
                        <p class="text-[10px] font-black text-primary uppercase tracking-[0.4em] mb-2">অভিভাবক প্রোফাইল
                        </p>
                        <h3 class="text-4xl font-black mb-2">
                            <%= user.name %>
                        </h3>
                        <p class="text-slate-500 font-bold mb-6">
                            <%= user.email || user.phone %>
                        </p>

                        <div class="flex flex-wrap gap-4 justify-center md:justify-start">
                            <a href="/messages"
                                class="px-8 py-3.5 bg-primary text-white rounded-2xl font-black text-sm shadow-xl shadow-primary/20 hover:scale-105 active:scale-95 transition-all flex items-center gap-2">
                                <span class="material-symbols-outlined text-xl">forum</span> শিক্ষককে বার্তা দিন
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Update Information Card -->
                <div
                    class="bg-white dark:bg-slate-900 rounded-[3rem] p-8 md:p-12 border border-slate-100 dark:border-slate-800 shadow-sm">
                    <h4 class="text-xl font-black mb-8 flex items-center gap-3">
                        <span class="material-symbols-outlined text-primary">edit_square</span> তথ্য পরিবর্তন করুন
                    </h4>

                    <% if (typeof success !=='undefined' && success) { %>
                        <div
                            class="p-4 bg-emerald-50 dark:bg-emerald-500/10 border border-emerald-100 dark:border-emerald-500/20 text-emerald-600 dark:text-emerald-400 rounded-2xl font-bold text-sm mb-8 flex items-center gap-3">
                            <span class="material-symbols-outlined">check_circle</span> সফলভাবে আপডেট করা হয়েছে।
                        </div>
                        <% } %>

                            <form action="/profile/update" method="POST" class="grid grid-cols-1 md:grid-cols-2 gap-8">
                                <div class="space-y-3">
                                    <label
                                        class="block text-[10px] font-black text-slate-400 uppercase tracking-widest ml-1">আপনার
                                        নাম</label>
                                    <input type="text" name="name" value="<%= user.name %>"
                                        class="w-full h-14 rounded-2xl bg-slate-50 dark:bg-slate-950 border-none px-6 font-black focus:ring-4 focus:ring-primary/10 transition-all shadow-inner"
                                        required>
                                </div>
                                <div class="space-y-3">
                                    <label
                                        class="block text-[10px] font-black text-slate-400 uppercase tracking-widest ml-1">নতুন
                                        পাসওয়ার্ড</label>
                                    <input type="password" name="password" placeholder="পরিবর্তন না করলে খালি রাখুন"
                                        class="w-full h-14 rounded-2xl bg-slate-50 dark:bg-slate-950 border-none px-6 font-black focus:ring-4 focus:ring-primary/10 transition-all shadow-inner">
                                </div>

                                <div class="md:col-span-2 pt-4">
                                    <button type="submit"
                                        class="w-full md:w-auto px-12 h-16 bg-slate-900 dark:bg-white dark:text-slate-950 text-white rounded-2xl font-black text-lg shadow-2xl hover:scale-[1.02] active:scale-98 transition-all">
                                        আপডেট করুন
                                    </button>
                                </div>
                            </form>
                </div>
            </div>
        </main>
    </div>
</body>

</html>