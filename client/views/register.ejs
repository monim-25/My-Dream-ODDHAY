<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>রেজিস্ট্রেশন | ODDHAY</title>
    <%- include('partials/head') %>
</head>

<body
    class="bg-background-light dark:bg-background-dark min-h-screen flex items-center justify-center p-6 relative overflow-hidden">
    <div class="max-w-2xl w-full card-smart shadow-2xl reveal active !rounded-3xl">
        <div class="text-center mb-10">
            <a href="/" class="inline-flex items-center gap-2 mb-6 group">
                <div class="text-primary group-hover:rotate-12 transition-transform duration-300">
                    <svg class="size-10" fill="none" viewbox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
                        <path d="M4 4H17.3334V17.3334H30.6666V30.6666H44V44H4V4Z" fill="currentColor"></path>
                    </svg>
                </div>
                <span class="text-2xl font-black">ODDHAY</span>
            </a>
            <h1 class="h2-section !text-2xl mb-2">রেজিস্ট্রেশন</h1>
            <p class="caption text-slate-500 !text-[10px]">অ্যাকাউন্ট তৈরি করে যাত্রা শুরু করুন</p>
        </div>

        <form action="/register" method="POST" class="space-y-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div class="space-y-4">
                    <p class="caption !text-[9px] text-slate-400 border-b pb-1">একাডেমিক</p>
                    <div class="space-y-3">
                        <select name="role"
                            class="w-full h-12 px-4 rounded-xl bg-slate-50 dark:bg-slate-900 border-none focus:ring-1 focus:ring-primary transition-all font-bold text-sm outline-none cursor-pointer">
                            <option value="student" selected>শিক্ষার্থী (Student)</option>
                            <option value="parent">অভিভাবক (Guardian)</option>
                        </select>
                        <select name="classLevel"
                            class="w-full h-12 px-4 rounded-xl bg-slate-50 dark:bg-slate-900 border-none focus:ring-1 focus:ring-primary transition-all font-bold text-sm outline-none cursor-pointer">
                            <% ['Class 6', 'Class 7' , 'Class 8' , 'Class 9' , 'Class 10' , 'Class 11' , 'Class 12'
                                , 'HSC' ].forEach(cls=> { %>
                                <option value="<%= cls %>">
                                    <%= cls %>
                                </option>
                                <% }) %>
                        </select>
                    </div>
                </div>

                <div class="space-y-4">
                    <p class="caption !text-[9px] text-slate-400 border-b pb-1">ব্যক্তিগত</p>
                    <div class="space-y-3">
                        <input type="text" name="name" placeholder="পুরো নাম"
                            class="w-full h-12 px-4 rounded-xl bg-slate-50 dark:bg-slate-900 border-none focus:ring-1 focus:ring-primary transition-all font-bold text-sm"
                            required>
                        <input type="email" name="email" placeholder="ইমেইল"
                            class="w-full h-12 px-4 rounded-xl bg-slate-50 dark:bg-slate-900 border-none focus:ring-1 focus:ring-primary transition-all font-bold text-sm"
                            required>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <input type="text" name="phone" placeholder="ফোন নম্বর"
                    class="w-full h-12 px-4 rounded-xl bg-slate-50 dark:bg-slate-900 border-none focus:ring-1 focus:ring-primary transition-all font-bold text-sm"
                    required>
                <input type="password" name="password" placeholder="পাসওয়ার্ড"
                    class="w-full h-12 px-4 rounded-xl bg-slate-50 dark:bg-slate-900 border-none focus:ring-1 focus:ring-primary transition-all font-bold text-sm"
                    required>
            </div>

            <button type="submit" class="btn-primary w-full h-14 !rounded-xl !text-base">রেজিস্ট্রেশন সম্পন্ন
                করুন</button>
        </form>

        <p class="text-center mt-8 body-main !text-sm text-slate-500">
            একাউন্ট আছে? <a href="/login" class="text-primary hover:underline font-bold">লগইন করুন</a>
        </p>
    </div>
</body>

</html>