<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>নতুন অ্যাকাউন্ট | ODDHAY</title>
    <%- include('partials/head') %>
        <style>
            .auth-card {
                background: rgba(255, 255, 255, 0.8);
                backdrop-filter: blur(20px);
                border: 1px solid rgba(255, 255, 255, 0.3);
            }

            .dark .auth-card {
                background: rgba(16, 25, 34, 0.8);
                border: 1px solid rgba(255, 255, 255, 0.05);
            }

            .input-glow:focus {
                box-shadow: 0 0 20px rgba(19, 127, 236, 0.15);
            }
        </style>
</head>

<body
    class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100 min-h-screen flex items-center justify-center p-6 relative overflow-x-hidden">
    <!-- Decorative Elements -->
    <div
        class="absolute top-[-10%] right-[-10%] size-[500px] bg-primary/10 rounded-full blur-[120px] -z-10 animate-pulse">
    </div>
    <div class="absolute bottom-[-10%] left-[-10%] size-[500px] bg-blue-400/10 rounded-full blur-[120px] -z-10"></div>

    <div class="max-w-2xl w-full auth-card p-10 md:p-14 rounded-[3rem] shadow-2xl reveal active">
        <div class="text-center mb-12">
            <a href="/" class="inline-flex items-center gap-2 mb-8 group">
                <div class="text-primary group-hover:rotate-12 transition-transform duration-300">
                    <svg class="size-10" fill="none" viewbox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
                        <path d="M4 4H17.3334V17.3334H30.6666V30.6666H44V44H4V4Z" fill="currentColor"></path>
                    </svg>
                </div>
                <span class="text-2xl font-black">ODDHAY</span>
            </a>
            <h1 class="text-3xl font-black mb-3">জয়েন করুন ODDHAY-তে 🚀</h1>
            <p class="text-slate-500 font-bold text-sm">বাংলাদেশের আধুনিক ডিজিটাল লার্নিং প্ল্যাটফর্ম</p>
        </div>

        <form action="/register" method="POST" class="space-y-8">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <!-- Academic -->
                <div class="space-y-6">
                    <p class="text-[10px] font-black uppercase text-slate-400 tracking-widest mb-4">অ্যাকাউন্টের ধরণ</p>
                    <div class="space-y-4">
                        <div class="relative group">
                            <span
                                class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 text-xl group-focus-within:text-primary transition-colors">badge</span>
                            <select name="role"
                                class="w-full pl-12 pr-4 py-4 rounded-2xl bg-slate-50 dark:bg-slate-800/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-sm appearance-none cursor-pointer outline-none">
                                <option value="student" selected>শিক্ষার্থী (Student)</option>
                                <option value="parent">অভিভাবক (Guardian)</option>
                                <option value="teacher">শিক্ষক (Teacher)</option>
                            </select>
                        </div>
                        <div class="relative group">
                            <span
                                class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 text-xl group-focus-within:text-primary transition-colors">school</span>
                            <select name="classLevel"
                                class="w-full pl-12 pr-4 py-4 rounded-2xl bg-slate-50 dark:bg-slate-800/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-sm appearance-none cursor-pointer outline-none">
                                <% ['Class 6', 'Class 7' , 'Class 8' , 'Class 9' , 'Class 10' , 'Class 11' , 'Class 12'
                                    , 'HSC' ].forEach(cls=> { %>
                                    <option value="<%= cls %>">
                                        <%= cls %>
                                    </option>
                                    <% }) %>
                            </select>
                        </div>
                    </div>
                </div>

                <!-- Personal -->
                <div class="space-y-6">
                    <p class="text-[10px] font-black uppercase text-slate-400 tracking-widest mb-4">ব্যক্তিগত তথ্য</p>
                    <div class="space-y-4">
                        <div class="relative group">
                            <span
                                class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 text-xl group-focus-within:text-primary transition-colors">person</span>
                            <input type="text" name="name" placeholder="আপনার পুরো নাম"
                                class="w-full pl-12 pr-4 py-4 rounded-2xl bg-slate-50 dark:bg-slate-800/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-sm input-glow"
                                required>
                        </div>
                        <div class="relative group">
                            <span
                                class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 text-xl group-focus-within:text-primary transition-colors">mail</span>
                            <input type="email" name="email" placeholder="ইমেইল অ্যাড্রেস"
                                class="w-full pl-12 pr-4 py-4 rounded-2xl bg-slate-50 dark:bg-slate-800/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-sm input-glow"
                                required>
                        </div>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <div class="relative group">
                    <span
                        class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 text-xl group-focus-within:text-primary transition-colors">call</span>
                    <input type="text" name="phone" placeholder="ফোন নম্বর"
                        class="w-full pl-12 pr-4 py-4 rounded-2xl bg-slate-50 dark:bg-slate-800/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-sm input-glow"
                        required>
                </div>
                <div class="relative group">
                    <span
                        class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 text-xl group-focus-within:text-primary transition-colors">lock</span>
                    <input type="password" name="password" placeholder="পাসওয়ার্ড"
                        class="w-full pl-12 pr-4 py-4 rounded-2xl bg-slate-50 dark:bg-slate-800/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-sm input-glow"
                        required>
                </div>
            </div>

            <button type="submit"
                class="w-full py-5 bg-primary text-white rounded-2xl font-black text-lg shadow-xl shadow-primary/30 hover:scale-[1.02] active:scale-95 transition-all flex items-center justify-center gap-3">
                নিবন্ধন সম্পন্ন করুন <span class="material-symbols-outlined">how_to_reg</span>
            </button>
        </form>

        <p class="text-center mt-12 text-sm text-slate-500 font-bold">
            আগে থেকেই একাউন্ট আছে? <a href="/login" class="text-primary hover:underline">লগইন করুন</a>
        </p>
    </div>
</body>

</html>