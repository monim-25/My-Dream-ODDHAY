<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>নিবন্ধন | ODDHAY</title>
    <%- include('partials/head') %>
        <style>
            .auth-card {
                background: rgba(255, 255, 255, 0.8);
                backdrop-filter: blur(20px);
                border: 1px solid rgba(255, 255, 255, 0.4);
            }

            .dark .auth-card {
                background: rgba(15, 23, 42, 0.8);
                border: 1px solid rgba(255, 255, 255, 0.05);
            }
        </style>
</head>

<body
    class="bg-[#f8fafc] dark:bg-slate-950 text-slate-900 dark:text-slate-100 min-h-screen flex items-center justify-center p-6 relative overflow-x-hidden">
    <!-- Blurs -->
    <div
        class="absolute top-[-10%] right-[-10%] size-[500px] bg-primary/10 rounded-full blur-[120px] -z-10 animate-pulse">
    </div>
    <div class="absolute bottom-[-10%] left-[-10%] size-[500px] bg-indigo-500/10 rounded-full blur-[120px] -z-10"></div>

    <div class="max-w-xl w-full auth-card p-10 rounded-[2.5rem] shadow-2xl reveal active">
        <div class="text-center mb-10">
            <a href="/" class="inline-flex items-center gap-2 mb-8 group">
                <div class="text-primary group-hover:rotate-12 transition-transform duration-300">
                    <svg class="size-10" fill="none" viewbox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
                        <path d="M4 4H17.3334V17.3334H30.6666V30.6666H44V44H4V4Z" fill="currentColor"></path>
                    </svg>
                </div>
                <span class="text-2xl font-black">ODDHAY</span>
            </a>
            <h1 class="text-2xl font-black mb-2">নিবন্ধন করুন</h1>
            <p class="text-[10px] text-slate-500 font-black uppercase tracking-[0.2em]">জয়েন করুন আধুনিক লার্নিং
                প্ল্যাটফর্মে</p>
        </div>

        <form action="/register" method="POST" class="space-y-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <!-- Academic -->
                <div class="space-y-4">
                    <p class="text-[9px] font-black uppercase text-slate-400 tracking-widest px-1">মোড</p>
                    <div class="space-y-3">
                        <select name="role"
                            class="w-full px-5 py-3 rounded-xl bg-slate-50 dark:bg-slate-900/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-xs appearance-none outline-none">
                            <option value="student" selected>শিক্ষার্থী (Student)</option>
                            <option value="parent">অভিভাবক (Guardian)</option>
                            <option value="teacher">শিক্ষক (Teacher)</option>
                        </select>
                        <select name="classLevel"
                            class="w-full px-5 py-3 rounded-xl bg-slate-50 dark:bg-slate-900/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-xs appearance-none outline-none">
                            <% ['Class 6', 'Class 7' , 'Class 8' , 'Class 9' , 'Class 10' , 'Class 11' , 'Class 12'
                                , 'HSC' ].forEach(cls=> { %>
                                <option value="<%= cls %>">
                                    <%= cls %>
                                </option>
                                <% }) %>
                        </select>
                    </div>
                </div>

                <!-- Personal -->
                <div class="space-y-4">
                    <p class="text-[9px] font-black uppercase text-slate-400 tracking-widest px-1">তথ্য</p>
                    <div class="space-y-3">
                        <input type="text" name="name" placeholder="আপনার নাম"
                            class="w-full px-5 py-3 rounded-xl bg-slate-50 dark:bg-slate-900/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-xs"
                            required>
                        <input type="email" name="email" placeholder="ইমেইল অ্যাড্রেস"
                            class="w-full px-5 py-3 rounded-xl bg-slate-50 dark:bg-slate-900/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-xs"
                            required>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <input type="text" name="phone" placeholder="ফোন নম্বর"
                    class="w-full px-5 py-3 rounded-xl bg-slate-50 dark:bg-slate-900/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-xs"
                    required>
                <input type="password" name="password" placeholder="পাসওয়ার্ড"
                    class="w-full px-5 py-3 rounded-xl bg-slate-50 dark:bg-slate-900/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-xs"
                    required>
            </div>

            <button type="submit"
                class="w-full py-4 bg-primary text-white rounded-xl font-bold text-sm shadow-xl shadow-primary/20 hover:scale-[1.02] active:scale-95 transition-all flex items-center justify-center gap-2">
                নিবন্ধন সম্পন্ন করুন <span class="material-symbols-outlined text-[20px]">how_to_reg</span>
            </button>
        </form>

        <p class="text-center mt-12 text-[10px] text-slate-500 font-bold uppercase tracking-widest">
            আগে থেকেই একাউন্ট আছে? <a href="/login" class="text-primary hover:underline font-black">লগইন করুন</a>
        </p>
    </div>
</body>

</html>