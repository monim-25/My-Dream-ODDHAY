<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>বিশেষ নোটস | ODDHAY</title>
    <%- include('partials/head') %>
</head>

<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100 min-h-screen">
    <%- include('partials/nav') %>

        <main class="pt-32 pb-20 px-6 max-w-7xl mx-auto">
            <div class="mb-16 flex flex-col md:flex-row justify-between items-end gap-8">
                <div class="reveal">
                    <h1 class="text-4xl md:text-5xl font-black mb-4 tracking-tight">বিশেষ <span
                            class="text-primary">নোটস</span> কালেকশন</h1>
                    <p class="text-slate-500 max-w-xl">আপনার পড়াশোনাকে আরও সহজ করতে আমাদের অভিজ্ঞ শিক্ষকদের তৈরি বিশেষ
                        নোটসগুলো এখানে পাবেন।</p>
                </div>
                <!-- Search/Filter placeholder -->
                <div class="w-full md:w-auto glass-nav p-2 rounded-2xl flex items-center gap-2 reveal delay-200">
                    <input type="text" placeholder="নোট খুঁজুন..."
                        class="bg-transparent border-none focus:ring-0 px-4 py-2 w-full md:w-64 font-bold text-sm">
                    <button class="bg-primary text-white p-2 rounded-xl"><span
                            class="material-symbols-outlined">search</span></button>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-10">
                <% notes.forEach(note=> { %>
                    <div
                        class="glass-nav rounded-[2.5rem] overflow-hidden group shadow-xl hover:-translate-y-2 transition-all duration-500">
                        <div class="aspect-[4/3] bg-primary/10 relative">
                            <% if(note.thumbnail) { %><img src="<%= note.thumbnail %>"
                                    class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700">
                                <% } else { %>
                                    <div
                                        class="w-full h-full flex items-center justify-center text-primary/30 group-hover:scale-125 transition-transform duration-500">
                                        <span class="material-symbols-outlined text-7xl">description</span></div>
                                    <% } %>
                                        <div class="absolute top-6 left-6 flex flex-col gap-2">
                                            <span
                                                class="bg-primary text-white text-[10px] font-black px-4 py-1.5 rounded-full uppercase tracking-widest shadow-lg shadow-primary/20">
                                                <%= note.subject %>
                                            </span>
                                        </div>
                                        <!-- Bookmark Toggle (Interactive Feature) -->
                                        <button
                                            class="absolute top-6 right-6 size-10 rounded-full bg-white/20 backdrop-blur-md text-white border border-white/30 flex items-center justify-center hover:bg-white hover:text-primary transition-all opacity-0 group-hover:opacity-100"
                                            onclick="toggleBookmark('<%= note._id %>')">
                                            <span class="material-symbols-outlined text-xl"
                                                id="bookmark-icon-<%= note._id %>">bookmark</span>
                                        </button>
                        </div>
                        <div class="p-8">
                            <h3 class="font-black text-lg mb-2 leading-snug group-hover:text-primary transition-colors">
                                <%= note.title %>
                            </h3>
                            <p class="text-xs font-bold text-slate-400 mb-6 uppercase tracking-widest">
                                <%= note.classLevel %>
                            </p>
                            <a href="<%= note.fileUrl %>" target="_blank"
                                class="w-full py-4 bg-slate-100 dark:bg-slate-800 rounded-2xl font-black text-sm flex items-center justify-center gap-3 hover:bg-primary hover:text-white transition-all duration-300">
                                নোটটি দেখুন <span class="material-symbols-outlined text-lg">open_in_new</span>
                            </a>
                        </div>
                    </div>
                    <% }) %>
                        <% if(notes.length===0) { %>
                            <div class="col-span-full py-32 text-center glass-nav rounded-[3rem]">
                                <span class="material-symbols-outlined text-8xl text-slate-200 mb-6">notes</span>
                                <h2 class="text-3xl font-black mb-2 tracking-tight">আপাতত কোনো নোটস নেই</h2>
                                <p class="text-slate-500">শিক্ষকদের তৈরি বিশেষ নোটসগুলো শীঘ্রই এখানে যোগ করা হবে।</p>
                            </div>
                            <% } %>
            </div>
        </main>

        <script>
            function toggleBookmark(id) {
                const icon = document.getElementById('bookmark-icon-' + id);
                if (icon.innerText === 'bookmark') {
                    icon.innerText = 'bookmark_added';
                    icon.classList.add('fill-1');
                    // In production, send to backend API
                } else {
                    icon.innerText = 'bookmark';
                    icon.classList.remove('fill-1');
                }
            }
        </script>

        <%- include('partials/footer') %>
</body>

</html>