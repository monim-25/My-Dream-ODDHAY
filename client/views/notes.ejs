<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>বিশেষ নোটস | অধ্যায়</title>
    <%- include('partials/head') %>
</head>

<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100 min-h-screen">
    <%- include('partials/nav') %>

        <main class="relative overflow-hidden pt-40 pb-20 px-6 max-w-7xl mx-auto">
            <!-- Background Blobs -->
            <div class="bg-blob -top-20 -left-20 opacity-30"></div>
            <div class="bg-blob top-1/2 -right-20 opacity-20" style="animation-delay: -5s;"></div>

            <div class="mb-16 flex flex-col md:flex-row justify-between items-end gap-12 reveal">
                <div class="max-w-2xl">
                    <h1 class="h1-hero">নোটস</h1>
                </div>

                <div
                    class="w-full md:w-auto card-smart !p-2 !rounded-2xl flex items-center gap-2 group hover:border-primary transition-all">
                    <input type="text" placeholder="নোট খুঁজুন..."
                        class="bg-transparent border-none focus:ring-0 px-4 py-2 w-full md:w-72 font-black text-sm">
                    <button
                        class="bg-primary text-white size-12 rounded-xl flex items-center justify-center shadow-lg shadow-primary/30 group-hover:scale-105 transition-all">
                        <span class="material-symbols-outlined">search</span>
                    </button>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 reveal delay-200">
                <% notes.forEach(note=> { %>
                    <div
                        class="card-smart group hover:translate-y-[-10px] !p-0 overflow-hidden border-2 border-primary/5">
                        <div class="aspect-[4/3] bg-slate-100 dark:bg-slate-800 relative overflow-hidden">
                            <% if(note.thumbnail) { %>
                                <img src="<%= note.thumbnail %>"
                                    class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-1000">
                                <% } else { %>
                                    <div
                                        class="w-full h-full flex items-center justify-center text-primary/20 scale-100 group-hover:scale-125 transition-transform duration-700">
                                        <span class="material-symbols-outlined text-8xl">description</span>
                                    </div>
                                    <% } %>
                                        <div class="absolute top-4 left-4">
                                            <span
                                                class="px-3 py-1 rounded-lg bg-primary text-white text-[10px] font-black uppercase tracking-widest shadow-xl shadow-primary/20">
                                                <%= note.subject %>
                                            </span>
                                        </div>
                        </div>
                        <div class="p-8">
                            <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-3">
                                <%= note.classLevel %>
                            </p>
                            <h3
                                class="text-lg font-black mb-10 leading-snug group-hover:text-primary transition-colors line-clamp-2 min-h-[3rem]">
                                <%= note.title %>
                            </h3>
                            <a href="/note/<%= note._id %>"
                                class="w-full h-14 bg-slate-50 dark:bg-slate-800 shadow-inner rounded-2xl font-black text-xs flex items-center justify-center gap-3 group-hover:bg-primary group-hover:text-white transition-all duration-500 uppercase tracking-[0.1em]">
                                পিডিএফ দেখুন <span class="material-symbols-outlined text-lg">visibility</span>
                            </a>
                        </div>
                    </div>
                    <% }) %>

                        <% if(notes.length===0) { %>
                            <div class="col-span-full py-32 text-center card-smart border-dashed border-2">
                                <div
                                    class="size-24 rounded-full bg-slate-100 dark:bg-slate-800 flex items-center justify-center mx-auto mb-8 text-slate-300">
                                    <span class="material-symbols-outlined text-6xl">notes</span>
                                </div>
                                <h2 class="text-3xl font-black mb-4 tracking-tight">আপাতত কোনো নোটস নেই</h2>
                                <p class="text-slate-500 font-bold italic">শীঘ্রই নতুন চ্যাপ্টারের নোটস যোগ করা হবে।</p>
                            </div>
                            <% } %>
            </div>
        </main>

        <%- include('partials/footer') %>

            <script>
                document.addEventListener('DOMContentLoaded', () => {
                    const observer = new IntersectionObserver((entries) => {
                        entries.forEach(entry => entry.isIntersecting && entry.target.classList.add('active'));
                    }, { threshold: 0.1 });
                    document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
                });
            </script>
</body>

</html>