<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>নতুন পাসওয়ার্ড সেট করুন | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..900&family=Noto+Sans+Bengali:wght@300..900&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <style>
        body {
            font-family: 'Lexend', 'Noto Sans Bengali', sans-serif;
        }

        .btn-animate {
            transition: all 0.2s ease;
        }

        .btn-animate:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.4);
        }

        .strength-bar {
            height: 4px;
            border-radius: 2px;
            transition: all 0.3s ease;
        }
    </style>
</head>

<body
    class="bg-gradient-to-br from-blue-50 to-indigo-100 dark:bg-[#101922] text-[#0d141b] dark:text-slate-100 min-h-screen flex items-center justify-center p-4">
    <div class="max-w-md w-full">
        <!-- Logo -->
        <div class="text-center mb-8">
            <a href="/" class="inline-flex items-center gap-2 text-2xl font-black text-blue-600">
                🎓 <span>ODDHAY</span>
            </a>
        </div>

        <div
            class="bg-white dark:bg-slate-900 rounded-3xl p-10 shadow-2xl border border-white/60 dark:border-slate-800">
            <div class="text-center mb-8">
                <div
                    class="size-16 bg-green-500/10 text-green-500 rounded-full flex items-center justify-center mx-auto mb-4">
                    <span class="material-symbols-outlined text-3xl">lock</span>
                </div>
                <h1 class="text-2xl font-black mb-2">নতুন পাসওয়ার্ড সেট করুন</h1>
                <p class="text-slate-500 text-sm">একটি শক্তিশালী পাসওয়ার্ড বেছে নিন।</p>
            </div>

            <% if (locals.error && error) { %>
                <div
                    class="mb-6 p-4 bg-red-50 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 border border-red-100">
                    <span class="material-symbols-outlined text-xl flex-shrink-0">error</span>
                    <span>
                        <%= error %>
                    </span>
                </div>
                <% } %>

                    <form action="/reset-password/<%= token %>" method="POST" class="space-y-5" id="resetForm">
                        <!-- নতুন পাসওয়ার্ড -->
                        <div>
                            <label class="block text-xs font-bold mb-2 ml-1 text-slate-500 uppercase tracking-wide">নতুন
                                পাসওয়ার্ড</label>
                            <div class="relative">
                                <span
                                    class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 text-xl">lock</span>
                                <input type="password" name="password" id="password"
                                    class="w-full pl-12 pr-12 py-3.5 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent font-medium transition-all"
                                    placeholder="কমপক্ষে ৬ অক্ষর" required minlength="6"
                                    oninput="checkStrength(this.value)">
                                <button type="button" onclick="togglePw('password', 'eyeIcon1')"
                                    class="absolute right-4 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-600">
                                    <span class="material-symbols-outlined text-xl" id="eyeIcon1">visibility</span>
                                </button>
                            </div>
                            <!-- Strength indicator -->
                            <div class="mt-2 flex gap-1" id="strengthBars">
                                <div class="strength-bar flex-1 bg-slate-200" id="bar1"></div>
                                <div class="strength-bar flex-1 bg-slate-200" id="bar2"></div>
                                <div class="strength-bar flex-1 bg-slate-200" id="bar3"></div>
                                <div class="strength-bar flex-1 bg-slate-200" id="bar4"></div>
                            </div>
                            <p class="text-xs text-slate-400 mt-1 ml-1" id="strengthText">পাসওয়ার্ড শক্তি</p>
                        </div>

                        <!-- পাসওয়ার্ড নিশ্চিত করুন -->
                        <div>
                            <label
                                class="block text-xs font-bold mb-2 ml-1 text-slate-500 uppercase tracking-wide">পাসওয়ার্ড
                                নিশ্চিত করুন</label>
                            <div class="relative">
                                <span
                                    class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 text-xl">lock_check</span>
                                <input type="password" name="confirmPassword" id="confirmPassword"
                                    class="w-full pl-12 pr-12 py-3.5 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent font-medium transition-all"
                                    placeholder="আবার পাসওয়ার্ড দিন" required oninput="checkMatch()">
                                <button type="button" onclick="togglePw('confirmPassword', 'eyeIcon2')"
                                    class="absolute right-4 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-600">
                                    <span class="material-symbols-outlined text-xl" id="eyeIcon2">visibility</span>
                                </button>
                            </div>
                            <p class="text-xs mt-1 ml-1 hidden" id="matchMsg"></p>
                        </div>

                        <button type="submit" id="submitBtn"
                            class="btn-animate w-full py-3.5 bg-blue-600 text-white rounded-xl font-bold shadow-lg shadow-blue-500/30 flex items-center justify-center gap-2 mt-2">
                            <span class="material-symbols-outlined text-xl">check_circle</span>
                            <span id="btnText">পাসওয়ার্ড আপডেট করুন</span>
                        </button>
                    </form>

                    <div class="mt-6 text-center">
                        <a href="/forgot-password"
                            class="text-slate-400 font-semibold hover:text-slate-600 text-sm transition-colors">
                            ← নতুন রিসেট লিঙ্ক চাই
                        </a>
                    </div>
        </div>

        <p class="text-center text-slate-400 text-xs mt-6">© 2025 ODDHAY শিক্ষা প্ল্যাটফর্ম</p>
    </div>

    <script>
        function togglePw(inputId, iconId) {
            const input = document.getElementById(inputId);
            const icon = document.getElementById(iconId);
            if (input.type === 'password') {
                input.type = 'text';
                icon.textContent = 'visibility_off';
            } else {
                input.type = 'password';
                icon.textContent = 'visibility';
            }
        }

        function checkStrength(pw) {
            const bars = ['bar1', 'bar2', 'bar3', 'bar4'];
            const colors = ['bg-red-400', 'bg-orange-400', 'bg-yellow-400', 'bg-green-500'];
            const labels = ['খুব দুর্বল', 'দুর্বল', 'মাঝারি', 'শক্তিশালী'];

            let score = 0;
            if (pw.length >= 6) score++;
            if (pw.length >= 10) score++;
            if (/[A-Z]/.test(pw) || /[0-9]/.test(pw)) score++;
            if (/[^A-Za-z0-9]/.test(pw)) score++;

            bars.forEach((id, i) => {
                const bar = document.getElementById(id);
                bar.className = 'strength-bar flex-1';
                if (i < score) {
                    bar.classList.add(colors[score - 1]);
                } else {
                    bar.classList.add('bg-slate-200');
                }
            });

            const txt = document.getElementById('strengthText');
            txt.textContent = score > 0 ? labels[score - 1] : 'পাসওয়ার্ড শক্তি';
            txt.className = `text-xs mt-1 ml-1 ${score >= 3 ? 'text-green-500' : score >= 2 ? 'text-yellow-500' : 'text-red-400'}`;
        }

        function checkMatch() {
            const pw = document.getElementById('password').value;
            const cpw = document.getElementById('confirmPassword').value;
            const msg = document.getElementById('matchMsg');
            if (!cpw) { msg.classList.add('hidden'); return; }
            msg.classList.remove('hidden');
            if (pw === cpw) {
                msg.textContent = '✓ পাসওয়ার্ড মিলেছে';
                msg.className = 'text-xs mt-1 ml-1 text-green-500';
            } else {
                msg.textContent = '✗ পাসওয়ার্ড মিলছে না';
                msg.className = 'text-xs mt-1 ml-1 text-red-500';
            }
        }

        document.getElementById('resetForm').addEventListener('submit', function (e) {
            const pw = document.getElementById('password').value;
            const cpw = document.getElementById('confirmPassword').value;
            if (pw !== cpw) {
                e.preventDefault();
                alert('পাসওয়ার্ড দুটি মিলছে না!');
                return;
            }
            const btn = document.getElementById('submitBtn');
            btn.disabled = true;
            btn.classList.add('opacity-75');
            document.getElementById('btnText').textContent = 'আপডেট হচ্ছে...';
        });
    </script>
</body>

</html>