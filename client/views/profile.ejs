<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <%- include('partials/head') %>
        <title>প্রোফাইল | অধ্যায়</title>
        <style>
            .pb-safe {
                padding-bottom: env(safe-area-inset-bottom, 20px);
            }

            .profile-avatar-container {
                position: relative;
                display: inline-block;
            }

            .add-photo-btn {
                position: absolute;
                bottom: 0px;
                right: 0px;
                background: #2563eb;
                color: white;
                border-radius: 50%;
                width: 32px;
                height: 32px;
                display: flex;
                align-items: center;
                justify-content: center;
                border: 3px solid white;
                cursor: pointer;
                box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            }
        </style>
</head>

<body class="bg-slate-50 dark:bg-slate-950 text-slate-900 dark:text-slate-100 min-h-screen pb-20">

    <!-- Header -->
    <header
        class="bg-white dark:bg-slate-900 border-b border-slate-200 dark:border-slate-800 px-4 py-4 sticky top-0 z-50">
        <div class="max-w-5xl mx-auto flex items-center justify-between">
            <div class="flex items-center gap-4">
                <a href="/dashboard"
                    class="p-2.5 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-2xl transition-all">
                    <span class="material-symbols-outlined">arrow_back</span>
                </a>
                <h1 class="text-xl font-black">প্রোফাইল</h1>
            </div>
            <a href="/profile/edit"
                class="px-5 py-2.5 bg-slate-900 text-white dark:bg-accent rounded-xl text-sm font-black shadow-lg">এডিট
                প্রোফাইল</a>
        </div>
    </header>

    <main class="max-w-5xl mx-auto p-4 lg:p-8 grid grid-cols-1 lg:grid-cols-3 gap-8">

        <!-- Sidebar Info -->
        <div class="lg:col-span-1 space-y-6">
            <div
                class="bg-white dark:bg-slate-900 rounded-[2.5rem] p-8 border border-slate-200 dark:border-slate-800 shadow-sm text-center">
                <div class="profile-avatar-container mb-6">
                    <div
                        class="size-32 lg:size-40 rounded-[2.5rem] bg-slate-100 dark:bg-slate-800 border-4 border-white dark:border-slate-800 shadow-2xl overflow-hidden mx-auto flex items-center justify-center">
                        <% if(user.profilePicture) { %>
                            <img src="<%= user.profilePicture %>" class="w-full h-full object-cover"
                                id="profile-preview">
                            <% } else { %>
                                <span class="material-symbols-outlined text-slate-300 text-6xl">person</span>
                                <% } %>
                    </div>
                </div>
            </div>

            <h2 class="text-2xl font-black mb-1">
                <%= user.name %>
            </h2>
            <p class="text-slate-500 font-medium mb-6 text-sm">
                <%= user.email || user.phone %>
            </p>

            <div class="flex items-center justify-center gap-2 mb-8">
                <span
                    class="px-3 py-1 bg-blue-50 dark:bg-blue-900/20 text-blue-600 rounded-full text-[10px] font-black uppercase">
                    <%= user.role==='student' ? 'শিক্ষার্থী' : 'শিক্ষক' %>
                </span>
                <span
                    class="px-3 py-1 bg-slate-100 dark:bg-slate-800 rounded-full text-[10px] font-black uppercase">Class
                    <%= user.classLevel %>
                </span>
            </div>

            <div
                class="bg-slate-50 dark:bg-slate-800/50 p-4 rounded-2xl border border-slate-100 dark:border-slate-800 text-left">
                <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-1">স্টুডেন্ট আইডি</p>
                <p class="font-mono font-black text-accent tracking-tighter text-lg">
                    <%= user._id.toString().slice(-10).toUpperCase() %>
                </p>
            </div>
        </div>

        <!-- Stats -->
        <div class="grid grid-cols-2 gap-4">
            <div
                class="bg-white dark:bg-slate-900 p-6 rounded-3xl border border-slate-200 dark:border-slate-800 text-center">
                <p class="text-2xl font-black text-blue-600">
                    <%= user.quizResults ? user.quizResults.length : 0 %>
                </p>
                <p class="text-[10px] font-bold text-slate-400 uppercase tracking-widest mt-1">পরীক্ষা</p>
            </div>
            <div
                class="bg-white dark:bg-slate-900 p-6 rounded-3xl border border-slate-200 dark:border-slate-800 text-center">
                <p class="text-2xl font-black text-emerald-600">
                    <%= user.enrolledCourses ? user.enrolledCourses.length : 0 %>
                </p>
                <p class="text-[10px] font-bold text-slate-400 uppercase tracking-widest mt-1">কোর্স</p>
            </div>
        </div>
        </div>

        <!-- Menu Options -->
        <div class="lg:col-span-2 space-y-4">
            <h3 class="text-xs font-black text-slate-400 uppercase tracking-[0.2em] mb-4 px-4">অ্যাকাউন্ট সেটিংস</h3>

            <div
                class="bg-white dark:bg-slate-900 rounded-[2.5rem] border border-slate-200 dark:border-slate-800 shadow-sm overflow-hidden">
                <!-- Guardian Info -->
                <a href="/profile/guardian"
                    class="flex items-center gap-5 p-6 hover:bg-slate-50 dark:hover:bg-slate-800 transition-all border-b border-slate-100 dark:border-slate-800 group">
                    <div
                        class="size-12 rounded-2xl bg-emerald-50 dark:bg-emerald-900/20 text-emerald-500 flex items-center justify-center group-hover:scale-110 transition-transform">
                        <span class="material-symbols-outlined">family_restroom</span>
                    </div>
                    <div class="flex-1">
                        <p class="font-bold">অভিভাবক তথ্য</p>
                        <p class="text-xs text-slate-400 mt-0.5">অভিভাবকের যোগাযোগ এবং প্রোফাইল দেখুন</p>
                    </div>
                    <span class="material-symbols-outlined text-slate-300">chevron_right</span>
                </a>

                <!-- Report Card -->
                <a href="/profile/report-card"
                    class="flex items-center gap-5 p-6 hover:bg-slate-50 dark:hover:bg-slate-800 transition-all border-b border-slate-100 dark:border-slate-800 group">
                    <div
                        class="size-12 rounded-2xl bg-indigo-50 dark:bg-indigo-900/20 text-indigo-500 flex items-center justify-center group-hover:scale-110 transition-transform">
                        <span class="material-symbols-outlined">analytics</span>
                    </div>
                    <div class="flex-1">
                        <p class="font-bold">একাডেমিক রিপোর্ট কার্ড</p>
                        <p class="text-xs text-slate-400 mt-0.5">আপনার ফলাফল এবং অগ্রগতি রিপোর্ট দেখুন</p>
                    </div>
                    <span class="material-symbols-outlined text-slate-300">chevron_right</span>
                </a>

                <!-- ID Card -->
                <a href="/profile/id-card"
                    class="flex items-center gap-5 p-6 hover:bg-slate-50 dark:hover:bg-slate-800 transition-all border-b border-slate-100 dark:border-slate-800 group">
                    <div
                        class="size-12 rounded-2xl bg-rose-50 dark:bg-rose-900/20 text-rose-500 flex items-center justify-center group-hover:scale-110 transition-transform">
                        <span class="material-symbols-outlined">badge</span>
                    </div>
                    <div class="flex-1">
                        <p class="font-bold">ডিজিটাল আইডি কার্ড</p>
                        <p class="text-xs text-slate-400 mt-0.5">আপনার স্টুডেন্ট আইডি কার্ড ডাউনলোড করুন</p>
                    </div>
                    <span class="material-symbols-outlined text-slate-300">chevron_right</span>
                </a>

                <!-- Certificates -->
                <a href="/certificates"
                    class="flex items-center gap-5 p-6 hover:bg-slate-50 dark:hover:bg-slate-800 transition-all group">
                    <div
                        class="size-12 rounded-2xl bg-amber-50 dark:bg-amber-900/20 text-amber-500 flex items-center justify-center group-hover:scale-110 transition-transform">
                        <span class="material-symbols-outlined">workspace_premium</span>
                    </div>
                    <div class="flex-1">
                        <p class="font-bold">সাফল্য ও সার্টিফিকেট</p>
                        <p class="text-xs text-slate-400 mt-0.5">অর্জিত সকল সার্টিফিকেট এখানে পাবেন</p>
                    </div>
                    <span class="material-symbols-outlined text-slate-300">chevron_right</span>
                </a>
            </div>

            <!-- Danger Zone -->
            <div class="mt-8 px-4">
                <a href="/logout"
                    class="flex items-center justify-center gap-3 p-5 rounded-3xl bg-red-50 dark:bg-red-900/10 text-red-500 font-black hover:bg-red-100 transition-all">
                    <span class="material-symbols-outlined">logout</span> লগআউট করতে চাই
                </a>
            </div>
        </div>

    </main>


</body>

</html>