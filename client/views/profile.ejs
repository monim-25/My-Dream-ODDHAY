<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>আমার প্রোফাইল | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..900&family=Noto+Sans+Bengali:wght@300..900&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: { "primary": "#137fec", "background-light": "#f6f7f8", "background-dark": "#101922" },
                    fontFamily: { "display": ["Lexend", "Noto Sans Bengali", "sans-serif"] },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Lexend', 'Noto Sans Bengali', sans-serif;
        }
    </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-[#0d141b] dark:text-slate-100 min-h-screen">
    <header
        class="border-b border-slate-200 dark:border-slate-800 bg-white/80 dark:bg-background-dark/80 backdrop-blur-md px-6 py-4 sticky top-0 z-50">
        <div class="max-w-5xl mx-auto flex items-center justify-between">
            <div class="flex items-center gap-4">
                <a href="/dashboard"
                    class="p-2 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-full transition-colors">
                    <span class="material-symbols-outlined">arrow_back</span>
                </a>
                <h1 class="text-xl font-bold">আমার প্রোফাইল</h1>
            </div>
            <button class="p-2 rounded-lg bg-slate-100 dark:bg-slate-800"
                onclick="document.documentElement.classList.toggle('dark')">
                <span class="material-symbols-outlined block dark:hidden">dark_mode</span>
                <span class="material-symbols-outlined hidden dark:block">light_mode</span>
            </button>
        </div>
    </header>

    <main class="max-w-5xl mx-auto p-6 py-12">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Left: User Info Card -->
            <div class="lg:col-span-1 space-y-6">
                <div
                    class="bg-white dark:bg-slate-900 rounded-[2.5rem] p-8 border border-slate-200 dark:border-slate-800 shadow-xl text-center">
                    <div
                        class="size-32 rounded-full bg-primary/10 border-4 border-white dark:border-slate-800 mx-auto mb-6 flex items-center justify-center text-primary">
                        <span class="material-symbols-outlined text-6xl">person</span>
                    </div>
                    <h2 class="text-2xl font-black mb-1">
                        <%= user.name %>
                    </h2>
                    <p class="text-sm text-slate-500 mb-6">
                        <%= user.email %>
                    </p>
                    <div
                        class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-primary/10 text-primary text-xs font-bold uppercase">
                        <%= user.role==='student' ? 'শিক্ষার্থী' : 'শিক্ষক' %>
                    </div>

                    <a href="/profile/report-card"
                        class="mt-6 flex items-center justify-center gap-2 w-full py-2.5 bg-emerald-500 text-white rounded-xl font-black text-xs hover:bg-emerald-600 transition-all shadow-lg shadow-emerald-500/20">
                        <span class="material-symbols-outlined text-lg">description</span> রিপোর্ট কার্ড দেখুন
                    </a>

                    <div class="grid grid-cols-2 gap-4 mt-8 pt-8 border-t border-slate-100 dark:border-slate-800">
                        <div>
                            <p class="text-2xl font-black text-primary">
                                <%= user.quizResults.length %>
                            </p>
                            <p class="text-[10px] text-slate-400 font-bold uppercase">পরীক্ষা দিয়েছেন</p>
                        </div>
                        <div>
                            <p class="text-2xl font-black text-primary">
                                <%= user.completedLessons.length %>
                            </p>
                            <p class="text-[10px] text-slate-400 font-bold uppercase">লেসন শেষ</p>
                        </div>
                    </div>
                </div>

                <!-- Quick Services -->
                <div
                    class="bg-white dark:bg-slate-900 rounded-[2.5rem] p-6 border border-slate-200 dark:border-slate-800 shadow-sm mt-6">
                    <h3 class="font-bold text-sm mb-4 text-slate-500">দ্রুত সেবা</h3>
                    <div class="space-y-2">
                        <a href="/settings"
                            class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-slate-50 dark:hover:bg-slate-800 transition-all group">
                            <div
                                class="size-10 rounded-xl bg-blue-500/10 text-blue-500 flex items-center justify-center">
                                <span class="material-symbols-outlined text-xl">settings</span>
                            </div>
                            <span class="font-bold text-sm group-hover:text-primary transition-colors">সেটিংস</span>
                            <span class="material-symbols-outlined text-slate-300 ml-auto">chevron_right</span>
                        </a>
                        <a href="/messages"
                            class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-slate-50 dark:hover:bg-slate-800 transition-all group">
                            <div
                                class="size-10 rounded-xl bg-emerald-500/10 text-emerald-500 flex items-center justify-center">
                                <span class="material-symbols-outlined text-xl">mail</span>
                            </div>
                            <span class="font-bold text-sm group-hover:text-primary transition-colors">মেসেজ</span>
                            <span class="material-symbols-outlined text-slate-300 ml-auto">chevron_right</span>
                        </a>
                        <a href="/downloads"
                            class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-slate-50 dark:hover:bg-slate-800 transition-all group">
                            <div
                                class="size-10 rounded-xl bg-amber-500/10 text-amber-500 flex items-center justify-center">
                                <span class="material-symbols-outlined text-xl">download</span>
                            </div>
                            <span class="font-bold text-sm group-hover:text-primary transition-colors">ডাউনলোড</span>
                            <span class="material-symbols-outlined text-slate-300 ml-auto">chevron_right</span>
                        </a>
                        <a href="/help"
                            class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-slate-50 dark:hover:bg-slate-800 transition-all group">
                            <div
                                class="size-10 rounded-xl bg-indigo-500/10 text-indigo-500 flex items-center justify-center">
                                <span class="material-symbols-outlined text-xl">help</span>
                            </div>
                            <span class="font-bold text-sm group-hover:text-primary transition-colors">সাহায্য</span>
                            <span class="material-symbols-outlined text-slate-300 ml-auto">chevron_right</span>
                        </a>
                        <a href="/logout"
                            class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-red-50 dark:hover:bg-red-500/10 transition-all group">
                            <div class="size-10 rounded-xl bg-red-500/10 text-red-500 flex items-center justify-center">
                                <span class="material-symbols-outlined text-xl">logout</span>
                            </div>
                            <span class="font-bold text-sm text-red-500">লগআউট</span>
                            <span class="material-symbols-outlined text-red-300 ml-auto">chevron_right</span>
                        </a>
                    </div>
                </div>

                <!-- Edit Profile Form -->
                <div
                    class="bg-white dark:bg-slate-900 rounded-[2.5rem] p-8 border border-slate-200 dark:border-slate-800 shadow-sm mt-6">
                    <h3 class="font-bold text-lg mb-4 flex items-center gap-2">
                        <span class="material-symbols-outlined text-slate-400">edit</span> তথ্য পরিবর্তন
                    </h3>

                    <% if (locals.success) { %>
                        <div
                            class="mb-4 p-3 bg-emerald-50 text-emerald-600 text-xs font-bold rounded-xl flex items-center gap-2">
                            <span class="material-symbols-outlined text-sm">check_circle</span> সফলভাবে আপডেট হয়েছে!
                        </div>
                        <% } %>

                            <form action="/profile/update" method="POST" class="space-y-4">
                                <div>
                                    <label class="block text-xs font-bold mb-1 ml-1 text-slate-500">নাম</label>
                                    <input type="text" name="name" value="<%= user.name %>"
                                        class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm font-bold focus:ring-2 focus:ring-primary transition-all">
                                </div>
                                <div>
                                    <label class="block text-xs font-bold mb-1 ml-1 text-slate-500">ক্লাস</label>
                                    <select name="classLevel"
                                        class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm font-bold focus:ring-2 focus:ring-primary transition-all">
                                        <% ['Class 6', 'Class 7' , 'Class 8' , 'Class 9' , 'Class 10' , 'Class 11'
                                            , 'Class 12' ].forEach(cls=> { %>
                                            <option value="<%= cls %>" <%=user.classLevel===cls ? 'selected' : '' %>><%=
                                                    cls %>
                                            </option>
                                            <% }) %>
                                    </select>
                                </div>
                                <div>
                                    <label class="block text-xs font-bold mb-1 ml-1 text-slate-500">পাসওয়ার্ড পরিবর্তন
                                        (অপশনাল)</label>
                                    <input type="password" name="password" placeholder="নতুন পাসওয়ার্ড"
                                        class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm font-bold focus:ring-2 focus:ring-primary transition-all">
                                </div>
                                <button type="submit"
                                    class="w-full py-3 bg-slate-900 dark:bg-white text-white dark:text-slate-900 rounded-xl font-bold text-sm shadow-lg hover:opacity-90 transition-all">আপডেট
                                    করুন</button>
                            </form>
                </div>
            </div>

            <!-- Right: Progress & Activity -->
            <div class="lg:col-span-2 space-y-8">
                <!-- Quiz History -->
                <section>
                    <h3 class="text-xl font-black mb-6 flex items-center gap-2">
                        <span class="material-symbols-outlined text-amber-500">emoji_events</span> পরীক্ষার ফলাফলসমূহ
                    </h3>
                    <div class="space-y-4">
                        <% if(user.quizResults.length> 0) { %>
                            <% user.quizResults.reverse().forEach(result=> { %>
                                <div
                                    class="bg-white dark:bg-slate-900 p-6 rounded-3xl border border-slate-200 dark:border-slate-800 flex justify-between items-center group hover:border-primary transition-all">
                                    <div class="flex items-center gap-4">
                                        <div
                                            class="size-12 rounded-2xl bg-slate-50 dark:bg-slate-800 flex items-center justify-center text-slate-400 group-hover:text-primary">
                                            <span class="material-symbols-outlined">quiz</span>
                                        </div>
                                        <div>
                                            <h4 class="font-bold">
                                                <%= result.quiz ? result.quiz.title : 'Deleted Quiz' %>
                                            </h4>
                                            <p class="text-xs text-slate-500">
                                                <%= new Date(result.date).toLocaleDateString('bn-BD') %>
                                            </p>
                                        </div>
                                    </div>
                                    <div class="text-right">
                                        <div class="text-xl font-black text-primary">
                                            <%= result.score %> / <%= result.total %>
                                        </div>
                                        <div class="text-[10px] text-slate-400 font-bold">স্কোর</div>
                                    </div>
                                </div>
                                <% }); %>
                                    <% } else { %>
                                        <div
                                            class="py-12 bg-slate-50 dark:bg-slate-900/50 rounded-3xl border-2 border-dashed border-slate-200 dark:border-slate-800 text-center text-slate-400 italic">
                                            এখনো কোনো পরীক্ষা দেওয়া হয়নি।
                                        </div>
                                        <% } %>
                    </div>
                </section>

                <!-- Course Progress Charts (Static Simulation for now) -->
                <section>
                    <h3 class="text-xl font-black mb-6 flex items-center gap-2">
                        <span class="material-symbols-outlined text-primary">bar_chart</span> পড়াশোনার অগ্রগতি
                    </h3>
                    <div
                        class="bg-white dark:bg-slate-900 p-8 rounded-[2.5rem] border border-slate-200 dark:border-slate-800">
                        <div class="flex items-end gap-2 h-48 mb-6">
                            <% ['শনি', 'রবি' , 'সোম' , 'মঙ্গল' , 'বুধ' , 'বৃহঃ' , 'শুক্র' ].forEach((day, i)=> { %>
                                <div class="flex-1 flex flex-col items-center gap-2">
                                    <div class="w-full bg-primary/10 rounded-t-xl relative overflow-hidden group chart-bar"
                                        data-height="<%= Math.floor(Math.random() * 80) + 20 %>%">
                                        <div
                                            class="absolute inset-0 bg-primary opacity-0 group-hover:opacity-100 transition-opacity">
                                        </div>
                                    </div>
                                    <p class="text-[10px] font-bold text-slate-400">
                                        <%= day %>
                                    </p>
                                </div>
                                <% }); %>
                        </div>
                        <p class="text-sm text-center text-slate-500 italic">এটি আপনার গত ৭ দিনের পড়াশোনার সময়ের একটি
                            তুলনামূলক গ্রাফ।</p>
                    </div>
                </section>
            </div>
        </div>
    </main>

    <script>
        // Apply chart bars
        document.querySelectorAll('.chart-bar').forEach(bar => {
            bar.style.height = bar.getAttribute('data-height');
        });
    </script>
</body>

</html>