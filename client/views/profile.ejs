<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>Profile | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..900&family=Noto+Sans+Bengali:wght@300..900&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: { "primary": "#137fec", "background-light": "#f6f7f8", "background-dark": "#101922" },
                    fontFamily: { "display": ["Lexend", "Noto Sans Bengali", "sans-serif"] },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Lexend', 'Noto Sans Bengali', sans-serif;
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom, 20px);
        }
    </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-[#0d141b] dark:text-slate-100 min-h-screen pb-20">
    <!-- Header -->
    <header
        class="bg-white dark:bg-slate-900 border-b border-slate-200 dark:border-slate-800 px-4 py-4 sticky top-0 z-50">
        <div class="max-w-2xl mx-auto flex items-center justify-between">
            <div class="flex items-center gap-3">
                <a href="/dashboard"
                    class="p-2 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-xl transition-colors">
                    <span class="material-symbols-outlined">arrow_back</span>
                </a>
                <h1 class="text-lg font-black">Profile</h1>
            </div>
        </div>
    </header>

    <main class="max-w-2xl mx-auto p-4 py-6 space-y-4">

        <!-- Welcome Card (First time only - handled by backend) -->
        <% if(locals.isFirstVisit) { %>
            <div class="bg-gradient-to-r from-primary to-indigo-600 p-6 rounded-2xl text-white shadow-lg text-center">
                <span class="material-symbols-outlined text-5xl mb-2">waving_hand</span>
                <h2 class="text-xl font-black mb-1">স্বাগতম, <%= user.name %>!</h2>
                <p class="text-white/80 text-sm">আপনার প্রোফাইলে আপনি সব তথ্য দেখতে ও আপডেট করতে পারবেন।</p>
            </div>
            <% } %>

                <!-- Profile Card -->
                <div
                    class="bg-white dark:bg-slate-900 rounded-2xl p-6 border border-slate-200 dark:border-slate-800 shadow-sm">
                    <div class="flex items-center gap-4 mb-6">
                        <div
                            class="size-20 rounded-2xl bg-primary/10 border-2 border-primary/20 flex items-center justify-center overflow-hidden">
                            <% if(user.profilePicture) { %>
                                <img src="<%= user.profilePicture %>" class="w-full h-full object-cover">
                                <% } else { %>
                                    <span class="material-symbols-outlined text-primary text-4xl">person</span>
                                    <% } %>
                        </div>
                        <div>
                            <h2 class="text-xl font-black">
                                <%= user.name %>
                            </h2>
                            <p class="text-sm text-slate-500">
                                <%= user.email %>
                            </p>
                            <div class="flex items-center gap-2 mt-1">
                                <span
                                    class="px-2 py-0.5 bg-primary/10 text-primary rounded-full text-[10px] font-bold uppercase">
                                    <%= user.role==='student' ? 'শিক্ষার্থী' : 'শিক্ষক' %>
                                </span>
                                <span
                                    class="px-2 py-0.5 bg-slate-100 dark:bg-slate-800 rounded-full text-[10px] font-bold">
                                    <%= user.classLevel %>
                                </span>
                            </div>
                        </div>
                    </div>

                    <!-- Student ID -->
                    <div class="bg-slate-50 dark:bg-slate-800 p-4 rounded-xl mb-4">
                        <p class="text-[10px] text-slate-400 font-bold uppercase mb-1">স্টুডেন্ট আইডি</p>
                        <p class="font-mono font-black text-primary tracking-wider">
                            <%= user._id.toString().slice(-8).toUpperCase() %>
                        </p>
                    </div>

                    <!-- Stats -->
                    <div class="grid grid-cols-3 gap-3">
                        <div class="text-center p-3 bg-blue-50 dark:bg-blue-500/10 rounded-xl">
                            <p class="text-lg font-black text-blue-600">
                                <%= user.quizResults ? user.quizResults.length : 0 %>
                            </p>
                            <p class="text-[9px] text-slate-500 font-bold">পরীক্ষা</p>
                        </div>
                        <div class="text-center p-3 bg-emerald-50 dark:bg-emerald-500/10 rounded-xl">
                            <p class="text-lg font-black text-emerald-600">
                                <%= user.completedLessons ? user.completedLessons.length : 0 %>
                            </p>
                            <p class="text-[9px] text-slate-500 font-bold">লেসন শেষ</p>
                        </div>
                        <div class="text-center p-3 bg-amber-50 dark:bg-amber-500/10 rounded-xl">
                            <p class="text-lg font-black text-amber-600">
                                <%= user.enrolledCourses ? user.enrolledCourses.length : 0 %>
                            </p>
                            <p class="text-[9px] text-slate-500 font-bold">কোর্স</p>
                        </div>
                    </div>
                </div>

                <!-- Menu Options -->
                <div
                    class="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm overflow-hidden">

                    <!-- Edit Profile -->
                    <a href="/profile/edit"
                        class="flex items-center gap-4 p-4 hover:bg-slate-50 dark:hover:bg-slate-800 transition-colors border-b border-slate-100 dark:border-slate-800">
                        <div class="size-10 rounded-xl bg-blue-500/10 text-blue-500 flex items-center justify-center">
                            <span class="material-symbols-outlined">edit</span>
                        </div>
                        <div class="flex-1">
                            <p class="font-bold text-sm">প্রোফাইল এডিট করুন</p>
                            <p class="text-[10px] text-slate-400">নাম, পাসওয়ার্ড ইত্যাদি পরিবর্তন করুন</p>
                        </div>
                        <span class="material-symbols-outlined text-slate-300">chevron_right</span>
                    </a>

                    <!-- Guardian Contact -->
                    <a href="/profile/guardian"
                        class="flex items-center gap-4 p-4 hover:bg-slate-50 dark:hover:bg-slate-800 transition-colors border-b border-slate-100 dark:border-slate-800">
                        <div
                            class="size-10 rounded-xl bg-emerald-500/10 text-emerald-500 flex items-center justify-center">
                            <span class="material-symbols-outlined">family_restroom</span>
                        </div>
                        <div class="flex-1">
                            <p class="font-bold text-sm">অভিভাবক তথ্য</p>
                            <p class="text-[10px] text-slate-400">অভিভাবকের যোগাযোগ দেখুন</p>
                        </div>
                        <span class="material-symbols-outlined text-slate-300">chevron_right</span>
                    </a>

                    <!-- Enrolled Classes -->
                    <a href="/my-courses"
                        class="flex items-center gap-4 p-4 hover:bg-slate-50 dark:hover:bg-slate-800 transition-colors border-b border-slate-100 dark:border-slate-800">
                        <div class="size-10 rounded-xl bg-amber-500/10 text-amber-500 flex items-center justify-center">
                            <span class="material-symbols-outlined">school</span>
                        </div>
                        <div class="flex-1">
                            <p class="font-bold text-sm">এনরোল করা কোর্স</p>
                            <p class="text-[10px] text-slate-400">আপনার ক্লাস ও কোর্স দেখুন</p>
                        </div>
                        <span class="material-symbols-outlined text-slate-300">chevron_right</span>
                    </a>

                    <!-- Learning Progress -->
                    <a href="/profile/report-card"
                        class="flex items-center gap-4 p-4 hover:bg-slate-50 dark:hover:bg-slate-800 transition-colors border-b border-slate-100 dark:border-slate-800">
                        <div
                            class="size-10 rounded-xl bg-indigo-500/10 text-indigo-500 flex items-center justify-center">
                            <span class="material-symbols-outlined">bar_chart</span>
                        </div>
                        <div class="flex-1">
                            <p class="font-bold text-sm">শেখার অগ্রগতি</p>
                            <p class="text-[10px] text-slate-400">রিপোর্ট কার্ড ও পারফরম্যান্স দেখুন</p>
                        </div>
                        <span class="material-symbols-outlined text-slate-300">chevron_right</span>
                    </a>

                    <!-- Student ID Card -->
                    <a href="/profile/id-card"
                        class="flex items-center gap-4 p-4 hover:bg-slate-50 dark:hover:bg-slate-800 transition-colors border-b border-slate-100 dark:border-slate-800">
                        <div class="size-10 rounded-xl bg-rose-500/10 text-rose-500 flex items-center justify-center">
                            <span class="material-symbols-outlined">badge</span>
                        </div>
                        <div class="flex-1">
                            <p class="font-bold text-sm">স্টুডেন্ট আইডি কার্ড</p>
                            <p class="text-[10px] text-slate-400">আপনার ইউনিক আইডি দেখুন</p>
                        </div>
                        <span class="material-symbols-outlined text-slate-300">chevron_right</span>
                    </a>

                    <!-- Logout -->
                    <a href="/logout"
                        class="flex items-center gap-4 p-4 hover:bg-red-50 dark:hover:bg-red-500/10 transition-colors">
                        <div class="size-10 rounded-xl bg-red-500/10 text-red-500 flex items-center justify-center">
                            <span class="material-symbols-outlined">logout</span>
                        </div>
                        <div class="flex-1">
                            <p class="font-bold text-sm text-red-500">লগআউট</p>
                            <p class="text-[10px] text-slate-400">অ্যাকাউন্ট থেকে বের হন</p>
                        </div>
                        <span class="material-symbols-outlined text-red-300">chevron_right</span>
                    </a>
                </div>

    </main>

    <!-- Mobile Bottom Nav -->
    <nav
        class="fixed bottom-0 left-0 right-0 z-[100] bg-white dark:bg-slate-900 border-t border-slate-100 dark:border-slate-800 lg:hidden pb-safe">
        <div class="flex items-center justify-around h-16">
            <a href="/dashboard" class="flex flex-col items-center gap-0.5 text-slate-400">
                <span class="material-symbols-outlined text-xl">home</span>
                <span class="text-[9px] font-black">হোম</span>
            </a>
            <a href="/messages" class="flex flex-col items-center gap-0.5 text-slate-400">
                <span class="material-symbols-outlined text-xl">mail</span>
                <span class="text-[9px] font-black">মেসেজ</span>
            </a>
            <a href="/downloads" class="flex flex-col items-center gap-0.5 text-slate-400">
                <span class="material-symbols-outlined text-xl">download</span>
                <span class="text-[9px] font-black">ডাউনলোড</span>
            </a>
            <a href="/profile" class="flex flex-col items-center gap-0.5 text-primary">
                <span class="material-symbols-outlined text-xl">person</span>
                <span class="text-[9px] font-black">প্রোফাইল</span>
            </a>
        </div>
    </nav>
</body>

</html>