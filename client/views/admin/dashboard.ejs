<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>Master Admin Panel | ODDHAY</title>
    <%- include('../partials/head') %>
        <style>
            .admin-sidebar {
                width: 280px;
                height: 100vh;
                position: fixed;
                left: 0;
                top: 0;
                background: #fff;
                border-right: 1px solid rgba(0, 0, 0, 0.03);
                z-index: 100;
                transition: all 0.3s ease;
            }

            .dark .admin-sidebar {
                background: #101922;
                border-right: 1px solid rgba(255, 255, 255, 0.03);
            }

            .admin-main {
                margin-left: 280px;
                padding: 40px;
                min-height: 100vh;
            }

            @media (max-width: 1024px) {
                .admin-sidebar {
                    transform: translateX(-100%);
                }

                .admin-main {
                    margin-left: 0;
                    padding: 24px;
                }
            }

            .stat-card {
                background: white;
                border-radius: 2rem;
                padding: 2rem;
                border: 1px solid rgba(0, 0, 0, 0.02);
                transition: all 0.3s ease;
            }

            .dark .stat-card {
                background: #1e293b;
                border-color: rgba(255, 255, 255, 0.02);
            }
        </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100">
    <aside class="admin-sidebar p-8">
        <%- include('sidebar_content', { active: 'dashboard' }) %>
    </aside>

    <main class="admin-main">
        <header class="flex justify-between items-center mb-16 h-12">
            <div>
                <h1 class="h2-section !text-[28px]"><span class="text-primary">Master</span> Dashboard</h1>
                <p class="body-main !text-xs opacity-50 font-bold uppercase tracking-wider mt-1">Platform Overview</p>
            </div>
            <div class="flex items-center gap-4">
                <button
                    class="size-12 rounded-2xl bg-white dark:bg-slate-800 flex items-center justify-center shadow-sm border border-slate-100 dark:border-slate-700"
                    id="theme-toggle">
                    <span class="material-symbols-outlined text-[20px] dark:hidden">dark_mode</span>
                    <span
                        class="material-symbols-outlined text-[20px] hidden dark:block text-amber-400">light_mode</span>
                </button>
            </div>
        </header>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
            <div class="stat-card shadow-sm hover:shadow-xl group transition-all">
                <div
                    class="size-14 rounded-2xl bg-primary/5 text-primary flex items-center justify-center mb-6 transition-transform group-hover:scale-110">
                    <span class="material-symbols-outlined text-3xl">groups</span>
                </div>
                <h3 class="text-3xl font-black mb-1">
                    <%= allUsersCount %>
                </h3>
                <p class="caption !text-[10px] opacity-40">Total Students</p>
            </div>

            <div class="stat-card shadow-sm hover:shadow-xl group transition-all">
                <div
                    class="size-14 rounded-2xl bg-amber-500/5 text-amber-500 flex items-center justify-center mb-6 transition-transform group-hover:scale-110">
                    <span class="material-symbols-outlined text-3xl">import_contacts</span>
                </div>
                <h3 class="text-3xl font-black mb-1">
                    <%= courseCount %>
                </h3>
                <p class="caption !text-[10px] opacity-40">Active Courses</p>
            </div>

            <div class="stat-card shadow-sm hover:shadow-xl group transition-all">
                <div
                    class="size-14 rounded-2xl bg-green-500/5 text-green-500 flex items-center justify-center mb-6 transition-transform group-hover:scale-110">
                    <span class="material-symbols-outlined text-3xl">question_answer</span>
                </div>
                <h3 class="text-3xl font-black mb-1">
                    <%= openQas %>
                </h3>
                <p class="caption !text-[10px] opacity-40">Pending Q&A</p>
            </div>

            <div class="stat-card shadow-sm hover:shadow-xl group transition-all">
                <div
                    class="size-14 rounded-2xl bg-indigo-500/5 text-indigo-500 flex items-center justify-center mb-6 transition-transform group-hover:scale-110">
                    <span class="material-symbols-outlined text-3xl">payments</span>
                </div>
                <h3 class="text-3xl font-black mb-1">৳ ১৪,৫০০</h3>
                <p class="caption !text-[10px] opacity-40">Monthly Revenue</p>
            </div>
        </div>

        <div class="mt-12 grid grid-cols-1 lg:grid-cols-3 gap-8">
            <div class="lg:col-span-2 card-smart !p-10 shadow-sm">
                <h3 class="h3-card !text-xl mb-8">Recent Activity</h3>
                <div class="space-y-6">
                    <p class="body-main !text-sm text-slate-400 italic">No recent activity logs found...</p>
                </div>
            </div>
            <div class="card-smart !p-10 shadow-sm bg-primary/5 border-primary/10">
                <h3 class="h3-card !text-xl mb-8">Quick Actions</h3>
                <div class="space-y-4">
                    <a href="/admin/teacher" class="btn-primary w-full !rounded-xl !text-xs !py-3">Add New Course</a>
                    <a href="/admin/users"
                        class="btn-secondary w-full !rounded-xl !text-xs !py-3 !bg-white dark:!bg-slate-800">Manage
                        Users</a>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/nav_script') %>
</body>

</html>