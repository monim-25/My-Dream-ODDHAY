<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>অ্যাডমিন ড্যানশবোর্ড | ODDHAY</title>
    <%- include('../partials/head') %>
        <style>
            .admin-sidebar {
                width: 280px;
                height: 100vh;
                position: fixed;
                left: 0;
                top: 0;
                background: #fff;
                border-right: 1px solid rgba(0, 0, 0, 0.03);
                z-index: 100;
            }

            .dark .admin-sidebar {
                background: #1e293b;
                border-right: 1px solid rgba(255, 255, 255, 0.03);
            }

            .admin-main {
                margin-left: 280px;
                padding: 48px;
                min-height: 100vh;
            }

            @media (max-width: 1024px) {
                .admin-sidebar {
                    display: none;
                }

                .admin-main {
                    margin-left: 0;
                    padding: 24px;
                }
            }
        </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100">

    <aside class="admin-sidebar hidden lg:flex flex-col">
        <%- include('sidebar_content', { active: 'dashboard' }) %>
    </aside>

    <main class="admin-main max-w-7xl mx-auto">
        <header class="flex flex-col md:flex-row justify-between items-start md:items-center gap-6 mb-12 reveal">
            <div>
                <p class="text-[10px] font-black text-primary uppercase tracking-[0.2em] mb-2">সুইচবোর্ড</p>
                <h1 class="text-3xl font-black">সিস্টেম <span class="text-primary">ওভারভিউ</span></h1>
            </div>
            <div class="flex items-center gap-4">
                <button
                    class="size-12 rounded-2xl bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 shadow-sm flex items-center justify-center text-slate-500 hover:text-primary transition-all"
                    onclick="document.documentElement.classList.toggle('dark')">
                    <span class="material-symbols-outlined block dark:hidden">dark_mode</span>
                    <span class="material-symbols-outlined hidden dark:block text-amber-400">light_mode</span>
                </button>
            </div>
        </header>

        <!-- Stats Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-16 reveal delay-100">
            <div class="card-smart !p-8 group hover:translate-y-[-8px]">
                <div class="size-14 rounded-2xl bg-blue-500/10 text-blue-500 flex items-center justify-center mb-6">
                    <span class="material-symbols-outlined text-3xl">groups</span>
                </div>
                <h3 class="text-4xl font-black mb-1">
                    <%= allUsersCount %>
                </h3>
                <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest leading-none">মোট ইউজার</p>
            </div>
            <div class="card-smart !p-8 group hover:translate-y-[-8px]">
                <div
                    class="size-14 rounded-2xl bg-emerald-500/10 text-emerald-500 flex items-center justify-center mb-6">
                    <span class="material-symbols-outlined text-3xl">school</span>
                </div>
                <h3 class="text-4xl font-black mb-1">
                    <%= studentCount %>
                </h3>
                <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest leading-none">শিক্ষার্থী</p>
            </div>
            <div class="card-smart !p-8 group hover:translate-y-[-8px]">
                <div class="size-14 rounded-2xl bg-amber-500/10 text-amber-500 flex items-center justify-center mb-6">
                    <span class="material-symbols-outlined text-3xl">book_5</span>
                </div>
                <h3 class="text-4xl font-black mb-1">
                    <%= courseCount %>
                </h3>
                <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest leading-none">মোট কোর্স</p>
            </div>
            <div class="card-smart !p-8 group hover:translate-y-[-8px]">
                <div class="size-14 rounded-2xl bg-rose-500/10 text-rose-500 flex items-center justify-center mb-6">
                    <span class="material-symbols-outlined text-3xl">mark_chat_unread</span>
                </div>
                <h3 class="text-4xl font-black mb-1">
                    <%= openQas %>
                </h3>
                <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest leading-none">পেন্ডিং সাপোর্ট
                </p>
            </div>
        </div>

        <!-- Quick Links / Recent Activity -->
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 reveal delay-200">
            <div class="lg:col-span-2 card-smart">
                <h3 class="text-xl font-black mb-8 flex items-center gap-2">
                    <span class="material-symbols-outlined text-primary">analytics</span> সাম্প্রতিক পরিস্থিতি
                </h3>
                <div class="space-y-6">
                    <% [1,2,3].forEach(i=> { %>
                        <div
                            class="flex items-center justify-between p-5 bg-slate-50 dark:bg-slate-800/50 rounded-2xl border border-slate-100 dark:border-slate-800 group hover:border-primary transition-colors">
                            <div class="flex items-center gap-4">
                                <div
                                    class="size-10 rounded-xl bg-white dark:bg-slate-900 shadow-sm flex items-center justify-center text-primary">
                                    <span class="material-symbols-outlined">person_add</span>
                                </div>
                                <div>
                                    <p class="text-sm font-black">নতুন শিক্ষার্থী যোগ হয়েছে</p>
                                    <p class="text-[10px] text-slate-400 font-bold uppercase tracking-tight">২ মিনিট আগে
                                    </p>
                                </div>
                            </div>
                            <span
                                class="material-symbols-outlined text-slate-200 group-hover:text-primary transition-colors">east</span>
                        </div>
                        <% }) %>
                </div>
            </div>

            <div class="card-smart bg-primary !text-white !p-10 relative overflow-hidden group">
                <div
                    class="absolute -right-10 -bottom-10 size-48 bg-white/10 rounded-full blur-3xl group-hover:scale-150 transition-all duration-700">
                </div>
                <h3 class="text-2xl font-black mb-6">সুপার অ্যাডমিন <br> অ্যাকশন</h3>
                <div class="space-y-4 relative z-10">
                    <a href="/admin/users"
                        class="flex items-center justify-between p-4 bg-white/10 hover:bg-white/20 rounded-2xl border border-white/20 transition-all backdrop-blur-sm">
                        <span class="text-sm font-bold">ইউজার ম্যানেজ করুন</span>
                        <span class="material-symbols-outlined">groups</span>
                    </a>
                    <a href="/admin/teacher"
                        class="flex items-center justify-between p-4 bg-white/10 hover:bg-white/20 rounded-2xl border border-white/20 transition-all backdrop-blur-sm">
                        <span class="text-sm font-bold">নতুন কোর্স এড করুন</span>
                        <span class="material-symbols-outlined">add_circle</span>
                    </a>
                </div>
            </div>
        </div>
    </main>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => entry.isIntersecting && entry.target.classList.add('active'));
            }, { threshold: 0.1 });
            document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
        });
    </script>
</body>

</html>