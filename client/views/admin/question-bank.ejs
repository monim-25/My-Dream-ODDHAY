<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>কোশ্চেন ব্যাংক | ODDHAY Admin</title>
    <%- include('../partials/head') %>
        <style>
            :root {
                --admin-sidebar-width: 280px;
            }

            .admin-sidebar {
                width: var(--admin-sidebar-width);
                height: 100vh;
                position: fixed;
                left: 0;
                top: 0;
                z-index: 100;
            }

            .admin-main {
                margin-left: var(--admin-sidebar-width);
                min-height: 100vh;
            }

            @media (max-width: 1024px) {
                .admin-sidebar {
                    transform: translateX(-100%);
                }

                .admin-main {
                    margin-left: 0;
                }

                .admin-sidebar.active {
                    transform: translateX(0);
                }
            }
        </style>
</head>

<body class="bg-[#f8fafc] dark:bg-[#0b1120] text-slate-900 dark:text-slate-100 font-sans">

    <aside id="adminSidebar"
        class="admin-sidebar bg-white dark:bg-[#0f172a] border-r border-slate-100 dark:border-slate-800 overflow-y-auto transition-transform duration-300">
        <%- include('sidebar_content', { active: 'questions' }) %>
    </aside>

    <main class="admin-main">
        <nav
            class="h-20 bg-white/80 dark:bg-[#0f172a]/80 backdrop-blur-md border-b border-slate-100 dark:border-slate-800 flex items-center justify-between px-10 sticky top-0 z-50">
            <h1 class="text-xl font-black">🏛️ প্রশ্ন ব্যাংক কন্ট্রোল</h1>
            <div class="flex items-center gap-6">
                <button onclick="document.documentElement.classList.toggle('dark')"
                    class="size-11 rounded-2xl bg-slate-50 dark:bg-slate-800 border border-slate-100 dark:border-slate-700 flex items-center justify-center text-slate-500 shadow-sm transition-all"><span
                        class="material-symbols-outlined">dark_mode</span></button>
            </div>
        </nav>

        <section class="p-10 max-w-[1600px] mx-auto">
            <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-6 mb-12">
                <div>
                    <h2 class="text-4xl font-black tracking-tighter mb-2">বিগত বছরের <span
                            class="text-primary">প্রশ্নসমূহ</span></h2>
                    <p class="text-slate-500 dark:text-slate-400 font-bold text-sm">বোর্ড প্রশ্ন এবং প্রতিযোগিতামূলক
                        পরীক্ষার প্রশ্নের সংগ্রহ পরিচালনা করুন।</p>
                </div>
                <% if (user.role==='admin' ) { %>
                    <button onclick="document.getElementById('addQuestionModal').classList.remove('hidden')"
                        class="px-8 py-4 bg-primary text-white rounded-[2rem] font-black text-xs uppercase tracking-widest hover:bg-primary-dark transition-all shadow-xl shadow-primary/20 flex items-center gap-2">
                        <span class="material-symbols-outlined text-[20px]">add</span> নতুন প্রশ্ন যোগ করুন
                    </button>
                    <% } %>
            </div>

            <div
                class="bg-white dark:bg-[#0f172a] rounded-[3rem] border border-slate-100 dark:border-slate-800 overflow-hidden shadow-sm">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr class="bg-slate-50 dark:bg-slate-800/50">
                            <th class="p-8 text-[11px] font-black uppercase tracking-[0.2em] text-slate-400">পরীক্ষার
                                সাল ও বোর্ড</th>
                            <th class="p-8 text-[11px] font-black uppercase tracking-[0.2em] text-slate-400">বিষয় ও
                                ক্লাস</th>
                            <th class="p-8 text-[11px] font-black uppercase tracking-[0.2em] text-slate-400">আপলোড
                                করেছেন</th>
                            <th class="p-8 text-[11px] font-black uppercase tracking-[0.2em] text-slate-400">অ্যাকশন
                            </th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-100 dark:divide-slate-800">
                        <% questions.forEach(question=> { %>
                            <tr class="hover:bg-slate-50/30 dark:hover:bg-slate-800/10 transition-all">
                                <td class="p-8">
                                    <div class="flex items-center gap-4">
                                        <div
                                            class="size-10 rounded-xl bg-violet-500/10 text-violet-500 flex items-center justify-center font-black text-xs">
                                            <%= question.year %>
                                        </div>
                                        <p class="font-black text-sm uppercase tracking-tighter">
                                            <%= question.board %>
                                        </p>
                                    </div>
                                </td>
                                <td class="p-8">
                                    <span class="text-xs font-black text-slate-700 dark:text-slate-300 uppercase block">
                                        <%= question.subject %>
                                    </span>
                                    <span class="text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                                        <%= question.classLevel %>
                                    </span>
                                </td>
                                <td class="p-8 text-xs font-bold text-slate-500">
                                    <%= question.addedBy ? question.addedBy.name : 'সিস্টেম' %>
                                </td>
                                <td class="p-8">
                                    <div class="flex gap-2">
                                        <a href="<%= question.fileUrl %>" target="_blank"
                                            class="size-9 flex items-center justify-center bg-slate-50 dark:bg-slate-800 rounded-xl text-slate-400 hover:text-primary transition-all shadow-sm"><span
                                                class="material-symbols-outlined text-[20px]">download</span></a>
                                        <% if (user.role==='admin' ) { %>
                                            <button
                                                class="size-9 flex items-center justify-center bg-slate-50 dark:bg-slate-800 rounded-xl text-slate-400 hover:text-rose-500 transition-all shadow-sm"><span
                                                    class="material-symbols-outlined text-[20px]">delete</span></button>
                                            <% } %>
                                    </div>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
            </div>
        </section>
    </main>

    <div id="addQuestionModal"
        class="fixed inset-0 bg-slate-900/60 backdrop-blur-sm z-[200] hidden flex items-center justify-center p-6">
        <div class="bg-white dark:bg-[#0f172a] rounded-[3rem] w-full max-w-xl overflow-hidden shadow-2xl">
            <div class="p-10">
                <div class="flex justify-between items-center mb-8">
                    <h3 class="text-2xl font-black">প্রশ্ন ব্যাংক <span class="text-primary">আপডেট</span></h3>
                    <button onclick="document.getElementById('addQuestionModal').classList.add('hidden')"
                        class="size-10 rounded-xl bg-slate-50 dark:bg-slate-800 flex items-center justify-center text-slate-400"><span
                            class="material-symbols-outlined">close</span></button>
                </div>
                <form action="/admin/add-question-bank" method="POST" enctype="multipart/form-data" class="space-y-6">
                    <div class="grid grid-cols-2 gap-6">
                        <div class="space-y-2">
                            <label
                                class="text-[10px] font-black text-slate-400 uppercase tracking-widest ml-2">সাল</label>
                            <input type="text" name="year" placeholder="উদা: ২০২৪" required
                                class="w-full bg-slate-50 dark:bg-slate-800 border-none rounded-2xl px-6 py-3.5 text-sm font-bold focus:ring-2 focus:ring-primary/20">
                        </div>
                        <div class="space-y-2">
                            <label
                                class="text-[10px] font-black text-slate-400 uppercase tracking-widest ml-2">বোর্ড</label>
                            <input type="text" name="board" placeholder="উদা: ঢাকা বোর্ড" required
                                class="w-full bg-slate-50 dark:bg-slate-800 border-none rounded-2xl px-6 py-3.5 text-sm font-bold focus:ring-2 focus:ring-primary/20">
                        </div>
                    </div>
                    <div class="grid grid-cols-2 gap-6">
                        <div class="space-y-2">
                            <label
                                class="text-[10px] font-black text-slate-400 uppercase tracking-widest ml-2">বিষয়</label>
                            <input type="text" name="subject" required
                                class="w-full bg-slate-50 dark:bg-slate-800 border-none rounded-2xl px-6 py-3.5 text-sm font-bold focus:ring-2 focus:ring-primary/20">
                        </div>
                        <div class="space-y-2">
                            <label
                                class="text-[10px] font-black text-slate-400 uppercase tracking-widest ml-2">ক্লাস</label>
                            <input type="text" name="classLevel" required
                                class="w-full bg-slate-50 dark:bg-slate-800 border-none rounded-2xl px-6 py-3.5 text-sm font-bold focus:ring-2 focus:ring-primary/20">
                        </div>
                    </div>
                    <div class="space-y-2">
                        <label class="text-[10px] font-black text-slate-400 uppercase tracking-widest ml-2">প্রশ্ন ফাইল
                            (PDF)</label>
                        <input type="file" name="question" required class="w-full text-xs font-black">
                    </div>
                    <button type="submit"
                        class="w-full py-4 bg-primary text-white rounded-2xl font-black text-xs uppercase tracking-widest shadow-xl shadow-primary/20 transition-all mt-4">প্রশ্ন
                        সেভ করুন</button>
                </form>
            </div>
        </div>
    </div>
</body>

</html>