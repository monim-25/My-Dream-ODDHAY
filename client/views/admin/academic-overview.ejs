<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>একাডেমিক কন্ট্রোল | ODDHAY Admin</title>
    <%- include('../partials/head') %>
        <style>
            :root {
                --admin-sidebar-width: 280px;
            }

            .admin-sidebar {
                width: var(--admin-sidebar-width);
                height: 100vh;
                position: fixed;
                left: 0;
                top: 0;
                z-index: 100;
            }

            .admin-main {
                margin-left: var(--admin-sidebar-width);
                min-height: 100vh;
            }

            @media (max-width: 1024px) {
                .admin-sidebar {
                    transform: translateX(-100%);
                }

                .admin-main {
                    margin-left: 0;
                }

                .admin-sidebar.active {
                    transform: translateX(0);
                }
            }

            .quick-action-card:hover {
                transform: translateY(-5px);
            }
        </style>
</head>

<body class="bg-[#f8fafc] dark:bg-[#0b1120] text-slate-900 dark:text-slate-100 font-sans">
    <aside id="adminSidebar"
        class="admin-sidebar bg-white dark:bg-[#0f172a] border-r border-slate-100 dark:border-slate-800 transition-transform duration-300">
        <%- include('sidebar_content', { active: 'dashboard' }) %>
    </aside>

    <main class="admin-main">
        <nav
            class="h-20 bg-white/80 dark:bg-[#0f172a]/80 backdrop-blur-md border-b border-slate-100 dark:border-slate-800 flex items-center justify-between px-10 sticky top-0 z-50">
            <div class="flex items-center gap-6 w-1/3">
                <div class="relative w-full max-w-xs group">
                    <span
                        class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-slate-400 text-[20px]">search</span>
                    <input type="text" placeholder="কন্টেন্ট খুঁজুন..."
                        class="w-full bg-slate-50 dark:bg-slate-800/50 border-none rounded-2xl pl-11 pr-4 py-2.5 text-sm font-bold focus:ring-2 focus:ring-primary/20">
                </div>
            </div>
            <div class="flex items-center gap-6">
                <button onclick="document.documentElement.classList.toggle('dark')"
                    class="size-11 rounded-2xl bg-slate-50 dark:bg-slate-800 flex items-center justify-center text-slate-500"><span
                        class="material-symbols-outlined">dark_mode</span></button>
                <div class="flex items-center gap-4">
                    <div class="text-right hidden sm:block">
                        <p class="text-xs font-black capitalize line-clamp-1">
                            <%= user.name %>
                        </p>
                        <p class="text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                            <%= user.role %>
                        </p>
                    </div>
                    <div
                        class="size-11 rounded-2xl bg-primary text-white flex items-center justify-center font-black shadow-lg shadow-primary/20 uppercase">
                        <%= user.name[0] %>
                    </div>
                </div>
            </div>
        </nav>

        <section class="p-10 max-w-[1600px] mx-auto">
            <!-- Welcome Section -->
            <div class="mb-12">
                <h1 class="text-4xl font-black tracking-tighter mb-2">একাডেমিক <span
                        class="text-primary">ড্যাশবোর্ড</span></h1>
                <p class="text-slate-500 dark:text-slate-400 font-bold text-sm">স্বাগতম! আপনার একাডেমিক কন্টেন্ট এখান
                    থেকে পরিচালনা করুন।</p>
            </div>

            <!-- Quick Action Buttons -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-12">
                <a href="/admin/courses"
                    class="quick-action-card p-8 rounded-[2.5rem] bg-primary text-white shadow-xl shadow-primary/20 transition-all">
                    <div class="size-12 rounded-2xl bg-white/20 flex items-center justify-center mb-6"><span
                            class="material-symbols-outlined">add_circle</span></div>
                    <h3 class="text-xl font-black mb-1">কোর্স তৈরি</h3>
                    <p class="text-white/60 text-xs font-bold">নতুন সিলেবাস শুরু করুন</p>
                </a>
                <a href="/admin/classes"
                    class="quick-action-card p-8 rounded-[2.5rem] bg-white dark:bg-[#0f172a] border border-slate-100 dark:border-slate-800 shadow-sm transition-all">
                    <div class="size-12 rounded-2xl bg-blue-500/10 text-blue-500 flex items-center justify-center mb-6">
                        <span class="material-symbols-outlined">video_call</span></div>
                    <h3 class="text-xl font-black mb-1 text-slate-900 dark:text-white">ক্লাস আপলোড</h3>
                    <p class="text-slate-400 text-xs font-bold text-slate-400">নতুন ভিডিও লেসন যোগ করুন</p>
                </a>
                <a href="/admin/quizzes"
                    class="quick-action-card p-8 rounded-[2.5rem] bg-white dark:bg-[#0f172a] border border-slate-100 dark:border-slate-800 shadow-sm transition-all">
                    <div
                        class="size-12 rounded-2xl bg-amber-500/10 text-amber-500 flex items-center justify-center mb-6">
                        <span class="material-symbols-outlined">quiz</span></div>
                    <h3 class="text-xl font-black mb-1 text-slate-900 dark:text-white">এক্সাম সেটআপ</h3>
                    <p class="text-slate-400 text-xs font-bold text-slate-400">পরীক্ষা বা কুইজ তৈরি করুন</p>
                </a>
                <a href="/admin/notes"
                    class="quick-action-card p-8 rounded-[2.5rem] bg-white dark:bg-[#0f172a] border border-slate-100 dark:border-slate-800 shadow-sm transition-all">
                    <div class="size-12 rounded-2xl bg-rose-500/10 text-rose-500 flex items-center justify-center mb-6">
                        <span class="material-symbols-outlined">note_add</span></div>
                    <h3 class="text-xl font-black mb-1 text-slate-900 dark:text-white">নোটস আপলোড</h3>
                    <p class="text-slate-400 text-xs font-bold text-slate-400">পিডিএফ বা লেকচার শিট</p>
                </a>
            </div>

            <!-- Content Summary Cards -->
            <div class="grid grid-cols-1 lg:grid-cols-4 gap-6 mb-12">
                <div
                    class="p-8 rounded-[2.5rem] bg-white dark:bg-[#0f172a] border border-slate-100 dark:border-slate-800">
                    <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-2">মোট ক্লাস</p>
                    <h3 class="text-4xl font-black">
                        <%= stats.totalClasses %>
                    </h3>
                </div>
                <div
                    class="p-8 rounded-[2.5rem] bg-white dark:bg-[#0f172a] border border-slate-100 dark:border-slate-800">
                    <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-2">মোট এক্সাম</p>
                    <h3 class="text-4xl font-black">
                        <%= stats.totalExams %>
                    </h3>
                </div>
                <div
                    class="p-8 rounded-[2.5rem] bg-white dark:bg-[#0f172a] border border-slate-100 dark:border-slate-800">
                    <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-2">মোট নোটস</p>
                    <h3 class="text-4xl font-black">
                        <%= stats.totalNotes %>
                    </h3>
                </div>
                <div
                    class="p-8 rounded-[2.5rem] bg-white dark:bg-[#0f172a] border border-slate-100 dark:border-slate-800">
                    <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-2">প্রশ্ন সংখ্যা</p>
                    <h3 class="text-4xl font-black">
                        <%= stats.totalQuestions %>
                    </h3>
                </div>
            </div>

            <div class="grid grid-cols-1 xl:grid-cols-2 gap-8">
                <!-- Recent Activity -->
                <div
                    class="p-10 rounded-[3rem] bg-white dark:bg-[#0f172a] border border-slate-100 dark:border-slate-800 shadow-sm">
                    <h4 class="text-xl font-black mb-8">সাম্প্রতিক অ্যাক্টিভিটি</h4>
                    <div class="space-y-6">
                        <% recentActivity.forEach(activity=> { %>
                            <div class="flex items-center gap-4">
                                <div
                                    class="size-10 rounded-xl bg-slate-50 dark:bg-slate-800 flex items-center justify-center">
                                    <span class="material-symbols-outlined text-[20px] text-slate-400">
                                        <%= activity.icon %>
                                    </span></div>
                                <div>
                                    <p class="text-sm font-black tracking-tighter capitalize">
                                        <%= activity.text %>
                                    </p>
                                    <p class="text-[10px] font-bold text-slate-400 uppercase">
                                        <%= activity.time %>
                                    </p>
                                </div>
                            </div>
                            <% }) %>
                    </div>
                </div>

                <!-- Pending Q&A -->
                <div
                    class="p-10 rounded-[3rem] bg-white dark:bg-[#0f172a] border border-slate-100 dark:border-slate-800 shadow-sm">
                    <div class="flex justify-between items-center mb-8">
                        <h4 class="text-xl font-black text-slate-900 dark:text-white">উত্তরহীন প্রশ্ন (Q&A)</h4>
                        <a href="/admin/qas"
                            class="text-[10px] font-black text-primary uppercase tracking-widest">সবগুলো দেখুন</a>
                    </div>
                    <div class="space-y-4">
                        <% pendingQas.forEach(qa=> { %>
                            <div
                                class="p-5 rounded-2xl bg-slate-50 dark:bg-slate-800/50 border border-transparent hover:border-primary/20 transition-all">
                                <p class="text-xs font-bold mb-2 line-clamp-1 dark:text-slate-300">
                                    <%= qa.question %>
                                </p>
                                <div class="flex items-center justify-between">
                                    <span class="text-[10px] font-black text-slate-400 uppercase">
                                        <%= qa.user ? qa.user.name : 'ছাত্র' %>
                                    </span>
                                    <a href="/admin/qas" class="text-[10px] font-black text-primary uppercase">উত্তর
                                        দিন</a>
                                </div>
                            </div>
                            <% }) %>
                    </div>
                </div>
            </div>
        </section>
    </main>
</body>

</html>