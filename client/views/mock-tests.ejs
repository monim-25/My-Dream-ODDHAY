<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>মক টেস্ট সেন্টার | ODDHAY</title>
    <%- include('partials/head') %>
</head>

<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100 min-h-screen">
    <%- include('partials/nav') %>

        <main class="pt-28 pb-20 px-4 md:px-6 max-w-6xl mx-auto">
            <!-- Header -->
            <div class="mb-10 reveal active">
                <div class="flex items-center gap-3 mb-3">
                    <div class="size-12 bg-primary/10 text-primary rounded-2xl flex items-center justify-center">
                        <span class="material-symbols-outlined text-2xl">timer</span>
                    </div>
                    <div>
                        <h1 class="text-3xl md:text-4xl font-black">মক টেস্ট <span class="text-primary">সেন্টার</span>
                        </h1>
                        <p class="text-slate-500 text-sm">বোর্ড পরীক্ষার পরিবেশে নিজেকে যাচাই করুন</p>
                    </div>
                </div>
            </div>

            <!-- Quick Actions -->
            <div class="grid md:grid-cols-3 gap-4 mb-10 reveal active">
                <a href="/create-self-quiz"
                    class="glass-card p-6 hover:scale-[1.02] transition-all group cursor-pointer">
                    <div
                        class="size-12 bg-purple-500/10 text-purple-500 rounded-2xl flex items-center justify-center mb-4 group-hover:scale-110 transition-transform">
                        <span class="material-symbols-outlined text-2xl">auto_awesome</span>
                    </div>
                    <h3 class="font-black text-lg mb-1">নিজে কুইজ তৈরি করুন</h3>
                    <p class="text-slate-500 text-sm">বিষয় ও ক্লাস বেছে নিজের মতো কুইজ তৈরি করুন</p>
                </a>
                <a href="/exams" class="glass-card p-6 hover:scale-[1.02] transition-all group cursor-pointer">
                    <div
                        class="size-12 bg-blue-500/10 text-blue-500 rounded-2xl flex items-center justify-center mb-4 group-hover:scale-110 transition-transform">
                        <span class="material-symbols-outlined text-2xl">quiz</span>
                    </div>
                    <h3 class="font-black text-lg mb-1">পরীক্ষা দিন</h3>
                    <p class="text-slate-500 text-sm">সব পরীক্ষার তালিকা দেখুন এবং অংশ নিন</p>
                </a>
                <a href="/analytics" class="glass-card p-6 hover:scale-[1.02] transition-all group cursor-pointer">
                    <div
                        class="size-12 bg-emerald-500/10 text-emerald-500 rounded-2xl flex items-center justify-center mb-4 group-hover:scale-110 transition-transform">
                        <span class="material-symbols-outlined text-2xl">insights</span>
                    </div>
                    <h3 class="font-black text-lg mb-1">ফলাফল বিশ্লেষণ</h3>
                    <p class="text-slate-500 text-sm">আপনার পারফরম্যান্স ট্র্যাক করুন</p>
                </a>
            </div>

            <!-- Available Quizzes -->
            <div class="glass-card p-6 reveal active">
                <h2 class="text-xl font-black mb-6 flex items-center gap-2">
                    <span class="material-symbols-outlined text-primary">list_alt</span>
                    উপলব্ধ মক টেস্ট
                </h2>

                <% if (quizzes && quizzes.length> 0) { %>
                    <div class="grid md:grid-cols-2 gap-4">
                        <% quizzes.forEach(function(quiz) { %>
                            <div
                                class="p-5 bg-slate-50 dark:bg-slate-800/50 rounded-2xl hover:shadow-md transition-all group">
                                <div class="flex items-start justify-between mb-3">
                                    <div class="flex-1">
                                        <h3
                                            class="font-black text-base mb-1 group-hover:text-primary transition-colors">
                                            <%= quiz.title %>
                                        </h3>
                                        <% if (quiz.course) { %>
                                            <span
                                                class="text-xs bg-primary/10 text-primary px-2 py-0.5 rounded-full font-semibold">
                                                <%= quiz.course.title || 'সাধারণ' %>
                                            </span>
                                            <% } %>
                                    </div>
                                    <div
                                        class="size-10 bg-primary/10 text-primary rounded-xl flex items-center justify-center flex-shrink-0 ml-3">
                                        <span class="material-symbols-outlined text-xl">quiz</span>
                                    </div>
                                </div>
                                <div class="flex items-center gap-4 text-xs text-slate-500 mb-4">
                                    <span class="flex items-center gap-1">
                                        <span class="material-symbols-outlined text-sm">help</span>
                                        <%= quiz.questions ? quiz.questions.length : 0 %> প্রশ্ন
                                    </span>
                                    <% if (quiz.duration) { %>
                                        <span class="flex items-center gap-1">
                                            <span class="material-symbols-outlined text-sm">timer</span>
                                            <%= quiz.duration %> মিনিট
                                        </span>
                                        <% } %>
                                </div>
                                <a href="/quiz/<%= quiz._id %>"
                                    class="block w-full text-center py-2.5 bg-primary text-white rounded-xl font-bold text-sm hover:bg-primary/90 transition-colors">
                                    পরীক্ষা দিন →
                                </a>
                            </div>
                            <% }); %>
                    </div>
                    <% } else { %>
                        <div class="text-center py-20">
                            <span class="material-symbols-outlined text-7xl text-primary/20 mb-4 block">quiz</span>
                            <h3 class="text-2xl font-black mb-3 opacity-50">এখনো কোনো মক টেস্ট নেই</h3>
                            <p class="text-slate-400 mb-8">অ্যাডমিন শীঘ্রই নতুন মক টেস্ট যোগ করবেন।</p>
                            <a href="/create-self-quiz" class="btn-primary !py-3 !px-8">
                                <span class="material-symbols-outlined">auto_awesome</span>
                                নিজে কুইজ তৈরি করুন
                            </a>
                        </div>
                        <% } %>
            </div>

            <!-- Tips Section -->
            <div class="mt-6 grid md:grid-cols-3 gap-4 reveal active">
                <div class="glass-card p-5 border-l-4 border-blue-500">
                    <span class="material-symbols-outlined text-blue-500 mb-2 block">lightbulb</span>
                    <h4 class="font-black mb-1">টিপস ১</h4>
                    <p class="text-sm text-slate-500">পরীক্ষার আগে সব প্রশ্ন একবার পড়ে নিন।</p>
                </div>
                <div class="glass-card p-5 border-l-4 border-emerald-500">
                    <span class="material-symbols-outlined text-emerald-500 mb-2 block">timer</span>
                    <h4 class="font-black mb-1">টিপস ২</h4>
                    <p class="text-sm text-slate-500">সময় ব্যবস্থাপনা করুন - প্রতি প্রশ্নে সমান সময় দিন।</p>
                </div>
                <div class="glass-card p-5 border-l-4 border-purple-500">
                    <span class="material-symbols-outlined text-purple-500 mb-2 block">repeat</span>
                    <h4 class="font-black mb-1">টিপস ৩</h4>
                    <p class="text-sm text-slate-500">নিয়মিত অনুশীলন করলে স্কোর বাড়বে।</p>
                </div>
            </div>
        </main>

        <%- include('partials/footer') %>
</body>

</html>