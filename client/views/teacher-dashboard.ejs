<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <%- include('partials/head') %>
        <title>অ্যাডমিন কোর্স প্যানেল | অধ্যায়</title>
        <style>
            .glass-card {
                background: rgba(255, 255, 255, 0.8);
                backdrop-filter: blur(12px);
                border: 1px solid rgba(255, 255, 255, 0.3);
            }

            .dark .glass-card {
                background: rgba(15, 23, 42, 0.8);
                border: 1px solid rgba(255, 255, 255, 0.05);
            }

            .sidebar-link.active {
                background: #1e293b;
                color: white;
            }

            .dark .sidebar-link.active {
                background: #334155;
            }
        </style>
</head>

<body class="bg-slate-50 dark:bg-[#0f172a] text-slate-900 dark:text-slate-100 min-h-screen">
    <div class="flex flex-col lg:flex-row min-h-screen">
        <!-- Sidebar -->
        <aside id="sidebar"
            class="fixed inset-y-0 left-0 w-72 bg-white dark:bg-slate-900 border-r border-slate-200 dark:border-slate-800 p-8 flex flex-col z-[4000] -translate-x-full lg:relative lg:translate-x-0 transition-transform duration-300">
            <div class="flex items-center gap-3 mb-12">
                <div class="text-primary">
                    <svg class="size-8" fill="none" viewBox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
                        <path d="M4 4H17.3334V17.3334H30.6666V30.6666H44V44H4V4Z" fill="currentColor"></path>
                    </svg>
                </div>
                <span class="text-2xl font-black tracking-tighter uppercase">Oddhay</span>
            </div>

            <nav class="space-y-3 flex-1">
                <a href="/admin"
                    class="flex items-center gap-3 px-6 py-4 rounded-2xl text-slate-500 hover:bg-slate-50 dark:hover:bg-slate-800/50 font-bold transition-all">
                    <span class="material-symbols-outlined">dashboard</span> মেইন ড্যাশবোর্ড
                </a>
                <a href="/admin/courses"
                    class="flex items-center gap-3 px-6 py-4 rounded-2xl bg-slate-900 text-white font-black shadow-lg shadow-black/10">
                    <span class="material-symbols-outlined">menu_book</span> আমার কোর্সসমূহ
                </a>
                <a href="/admin/qas"
                    class="flex items-center gap-3 px-6 py-4 rounded-2xl text-slate-500 hover:bg-slate-50 dark:hover:bg-slate-800/50 font-bold transition-all">
                    <span class="material-symbols-outlined">question_answer</span> প্রশ্নোত্তর
                </a>
                <a href="/admin/users"
                    class="flex items-center gap-3 px-6 py-4 rounded-2xl text-slate-500 hover:bg-slate-50 dark:hover:bg-slate-800/50 font-bold transition-all">
                    <span class="material-symbols-outlined">group</span> শিক্ষার্থী তালিকা
                </a>
            </nav>

            <div class="pt-6 border-t border-slate-100 dark:border-slate-800">
                <a href="/profile"
                    class="flex items-center gap-3 px-6 py-4 rounded-2xl text-slate-500 hover:bg-slate-50 dark:hover:bg-slate-800 font-bold transition-all">
                    <span class="material-symbols-outlined">account_circle</span> প্রোফাইল
                </a>
                <a href="/logout"
                    class="flex items-center gap-3 px-6 py-4 rounded-2xl text-red-500 hover:bg-red-50 dark:hover:bg-red-500/10 font-black">
                    <span class="material-symbols-outlined">logout</span> লগআউট
                </a>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="flex-1 p-6 md:p-10 max-w-7xl mx-auto w-full">
            <!-- Header -->
            <header class="flex justify-between items-center mb-10">
                <div class="flex items-center gap-4">
                    <button onclick="toggleSidebar()"
                        class="lg:hidden size-11 flex items-center justify-center rounded-xl bg-white dark:bg-slate-800 border border-slate-100 dark:border-slate-800 text-slate-500 shadow-sm">
                        <span class="material-symbols-outlined">menu</span>
                    </button>
                    <div>
                        <h2 class="text-2xl md:text-3xl font-black leading-none mb-1">অ্যাডমিন প্যানেল ⚙️</h2>
                        <p class="text-slate-500 font-bold text-sm">স্বাগতম, <%= user.name %>। আপনার আজকের কার্যক্রম
                                দেখুন।</p>
                    </div>
                </div>
                <div class="flex items-center gap-4">
                    <button onclick="document.documentElement.classList.toggle('dark')"
                        class="size-11 rounded-xl bg-slate-100 dark:bg-slate-800 text-slate-500 flex items-center justify-center transition-all active:scale-95">
                        <span class="material-symbols-outlined dark:hidden">dark_mode</span>
                        <span class="material-symbols-outlined hidden dark:block text-amber-400">light_mode</span>
                    </button>
                    <div
                        class="size-11 rounded-xl bg-primary text-white flex items-center justify-center font-black shadow-lg shadow-primary/20">
                        <%= user.name[0] %>
                    </div>
                </div>
            </header>

            <!-- Stats Grid -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12">
                <div
                    class="bg-white dark:bg-slate-900 p-8 rounded-[2.5rem] border border-slate-100 dark:border-slate-800 shadow-sm">
                    <div class="size-12 rounded-2xl bg-blue-500/10 text-blue-500 flex items-center justify-center mb-6">
                        <span class="material-symbols-outlined text-3xl">group</span>
                    </div>
                    <p class="text-[11px] font-black text-slate-400 uppercase tracking-widest mb-1">মোট শিক্ষার্থী</p>
                    <h3 class="text-3xl font-black">
                        <%= stats.totalStudents %>
                    </h3>
                    <p class="text-[10px] text-emerald-500 font-bold mt-2">+১২% এই মাসে</p>
                </div>
                <div
                    class="bg-white dark:bg-slate-900 p-8 rounded-[2.5rem] border border-slate-100 dark:border-slate-800 shadow-sm">
                    <div
                        class="size-12 rounded-2xl bg-amber-500/10 text-amber-500 flex items-center justify-center mb-6">
                        <span class="material-symbols-outlined text-3xl">menu_book</span>
                    </div>
                    <p class="text-[11px] font-black text-slate-400 uppercase tracking-widest mb-1">সক্রিয় কোর্স</p>
                    <h3 class="text-3xl font-black">
                        <%= stats.totalCourses %>
                    </h3>
                    <p class="text-[10px] text-slate-400 font-bold mt-2">২টি কোর্স আপকামিং</p>
                </div>
                <div
                    class="bg-white dark:bg-slate-900 p-8 rounded-[2.5rem] border border-slate-100 dark:border-slate-800 shadow-sm">
                    <div class="size-12 rounded-2xl bg-rose-500/10 text-rose-500 flex items-center justify-center mb-6">
                        <span class="material-symbols-outlined text-3xl">pending_actions</span>
                    </div>
                    <p class="text-[11px] font-black text-slate-400 uppercase tracking-widest mb-1">অপেক্ষমান প্রশ্ন</p>
                    <h3 class="text-3xl font-black">
                        <%= stats.pendingQuestions %>
                    </h3>
                    <p class="text-[10px] text-rose-500 font-bold mt-2">তাড়াতাড়ি উত্তর দিন</p>
                </div>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-10">
                <!-- My Courses List -->
                <div class="lg:col-span-2 space-y-8">
                    <div class="flex items-center justify-between mb-4">
                        <h4 class="text-xl font-black">আমার কোর্সসমূহ</h4>
                        <a href="/admin/courses"
                            class="px-5 py-2 bg-primary/10 text-primary rounded-xl font-black text-xs hover:bg-primary/20 transition-all">+
                            নতুন কোর্স</a>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <% if(courses && courses.length> 0) { %>
                            <% courses.forEach(course=> { %>
                                <div
                                    class="bg-white dark:bg-slate-900 border border-slate-100 dark:border-slate-800 p-6 rounded-[2.5rem] shadow-sm hover:border-primary transition-all group">
                                    <div class="flex gap-4 mb-6">
                                        <div
                                            class="size-20 rounded-2xl bg-slate-100 dark:bg-slate-800 overflow-hidden shrink-0">
                                            <img src="<%= course.thumbnail || '/images/course-default.jpg' %>"
                                                class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500">
                                        </div>
                                        <div class="flex-1 min-w-0">
                                            <h5 class="font-black text-sm truncate mb-1">
                                                <%= course.title %>
                                            </h5>
                                            <div class="flex items-center gap-1.5 text-slate-400 mb-2">
                                                <span class="material-symbols-outlined text-[14px]">school</span>
                                                <span class="text-[10px] font-bold">
                                                    <%= course.classLevel %>
                                                </span>
                                            </div>
                                            <span
                                                class="px-3 py-1 bg-slate-100 dark:bg-slate-800 rounded-lg text-[9px] font-black uppercase text-slate-500">সক্রিয়</span>
                                        </div>
                                    </div>
                                    <div class="flex gap-3">
                                        <a href="/admin/course-edit/<%= course._id %>"
                                            class="flex-1 text-center py-2.5 bg-slate-900 dark:bg-slate-800 text-white dark:text-slate-300 rounded-xl text-[10px] font-black uppercase hover:bg-slate-800 dark:hover:bg-slate-700 transition-all">এডিট</a>
                                        <a href="/course-details/<%= course._id %>"
                                            class="size-10 flex items-center justify-center bg-slate-100 dark:bg-slate-800 rounded-xl text-slate-500 hover:text-primary transition-all">
                                            <span class="material-symbols-outlined text-lg">visibility</span>
                                        </a>
                                    </div>
                                </div>
                                <% }) %>
                                    <% } else { %>
                                        <div
                                            class="col-span-full py-12 text-center border-2 border-dashed border-slate-100 dark:border-slate-800 rounded-[3rem]">
                                            <p class="text-slate-400 font-bold italic">আপনি এখনো কোনো কোর্স তৈরি করেননি।
                                            </p>
                                        </div>
                                        <% } %>
                    </div>
                </div>

                <!-- Recent Questions -->
                <div class="space-y-8">
                    <h4 class="text-xl font-black flex items-center gap-2">
                        নতুন প্রশ্ন <span class="bg-rose-500 text-white text-[10px] px-2 py-0.5 rounded-full">
                            <%= stats.pendingQuestions %>
                        </span>
                    </h4>
                    <div class="space-y-4">
                        <% if(recentQuestions && recentQuestions.length> 0) { %>
                            <% recentQuestions.forEach(q=> { %>
                                <div
                                    class="bg-white dark:bg-slate-900 p-6 rounded-[2rem] border border-slate-100 dark:border-slate-800 shadow-sm">
                                    <div class="flex items-center gap-3 mb-3">
                                        <div
                                            class="size-8 rounded-full bg-slate-100 dark:bg-slate-800 flex items-center justify-center text-[10px] font-black uppercase text-slate-400">
                                            <%= q.user && q.user.name ? q.user.name[0] : 'U' %>
                                        </div>
                                        <div>
                                            <h6 class="text-[11px] font-black">
                                                <%= q.user ? q.user.name : 'অজানা শিক্ষার্থী' %>
                                            </h6>
                                            <p class="text-[9px] text-slate-400 font-bold">১০ মিনিট আগে</p>
                                        </div>
                                    </div>
                                    <p class="text-xs text-slate-600 dark:text-slate-400 font-bold line-clamp-2 mb-4">
                                        <%= q.question %>
                                    </p>
                                    <a href="/admin/qas"
                                        class="text-[10px] font-black text-primary uppercase hover:underline">উত্তর দিন
                                        →</a>
                                </div>
                                <% }) %>
                                    <% } else { %>
                                        <p class="text-slate-400 font-bold italic text-center py-10">কোনো নতুন প্রশ্ন
                                            নেই।</p>
                                        <% } %>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script>
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            sidebar.classList.toggle('-translate-x-full');
        }
    </script>
</body>

</html>