<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>টিচার ড্যাশবোর্ড (অ্যাডমিন) | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..700&family=Noto+Sans+Bengali:wght@300..700&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#137fec",
                        "background-light": "#f6f7f8",
                        "background-dark": "#101922",
                    },
                    fontFamily: {
                        "display": ["Lexend", "Noto Sans Bengali", "sans-serif"],
                    },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Lexend', 'Noto Sans Bengali', sans-serif;
        }
    </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-[#0d141b] dark:text-slate-200">
    <div class="flex h-screen overflow-hidden">
        <!-- Sidebar Navigation -->
        <aside
            class="w-64 flex-shrink-0 border-r border-slate-200 dark:border-slate-800 bg-white dark:bg-background-dark flex flex-col justify-between p-4">
            <div class="flex flex-col gap-6">
                <div class="flex items-center gap-3 px-2">
                    <div class="bg-primary rounded-lg p-1.5 text-white"><span
                            class="material-symbols-outlined">school</span></div>
                    <div>
                        <h1 class="text-base font-bold leading-tight">ODDHAY অ্যাডমিন</h1>
                        <p class="text-slate-500 text-xs">সিনিয়র শিক্ষক</p>
                    </div>
                </div>
                <nav class="flex flex-col gap-1">
                    <a class="flex items-center gap-3 px-3 py-2 rounded-lg bg-primary/10 text-primary font-medium"
                        href="/admin/teacher"><span class="material-symbols-outlined">dashboard</span> ড্যাশবোর্ড</a>
                    <a class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors"
                        href="/admin/quizzes"><span class="material-symbols-outlined">quiz</span> মক এক্সাম
                        ম্যানেজমেন্ট</a>
                    <a class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors"
                        href="#manage-courses"><span class="material-symbols-outlined">groups</span> কোর্স
                        ম্যানেজমেন্ট</a>
                </nav>
            </div>
            <div class="px-2">
                <button
                    class="w-full flex items-center justify-center gap-2 p-2 rounded-lg bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-300 mb-4"
                    onclick="document.documentElement.classList.toggle('dark')">
                    <span class="material-symbols-outlined text-sm">dark_mode</span> মোড পরিবর্তন
                </button>
                <a href="/" class="flex items-center gap-3 px-3 py-2 text-red-500 font-bold"><span
                        class="material-symbols-outlined">logout</span> লগআউট</a>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="flex-1 overflow-y-auto p-8">
            <header class="flex justify-between items-center mb-10">
                <h2 class="text-2xl font-black">টিচার ড্যাশবোর্ড ওভারভিউ</h2>
                <div class="flex items-center gap-4">
                    <span class="font-bold">প্রফেসর সাহেদ আহমেদ</span>
                    <div class="size-10 rounded-full bg-primary/10 border-2 border-primary"></div>
                </div>
            </header>

            <!-- Overall Stats -->
            <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-12">
                <div
                    class="bg-white dark:bg-slate-900 p-6 rounded-xl border border-slate-200 dark:border-slate-800 shadow-sm">
                    <p class="text-slate-500 text-sm">মোট কোর্স</p>
                    <h3 class="text-3xl font-bold">
                        <%= courses.length %>
                    </h3>
                </div>
                <div
                    class="bg-white dark:bg-slate-900 p-6 rounded-xl border border-slate-200 dark:border-slate-800 shadow-sm">
                    <p class="text-slate-500 text-sm">মোট শিক্ষার্থী</p>
                    <h3 class="text-3xl font-bold">
                        <%= studentCount %>
                    </h3>
                </div>
            </div>

            <!-- Add New Course Form -->
            <section id="manage-courses"
                class="mb-12 bg-white dark:bg-slate-900 p-8 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm">
                <h3 class="text-xl font-bold mb-6 flex items-center gap-2">
                    <span class="material-symbols-outlined text-primary">add_circle</span> নতুন কোর্স যোগ করুন
                </h3>
                <form action="/admin/add-course" method="POST" enctype="multipart/form-data"
                    class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label class="block text-sm font-bold mb-2">কোর্সের নাম</label>
                        <input type="text" name="title"
                            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none focus:ring-2 focus:ring-primary"
                            placeholder="উদা: উচ্চতর গণিত ১ম পত্র" required>
                    </div>
                    <div>
                        <label class="block text-sm font-bold mb-2">বিষয় (Subject)</label>
                        <input type="text" name="subject"
                            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none focus:ring-2 focus:ring-primary"
                            placeholder="উদা: গণিত" required>
                    </div>
                    <div>
                        <label class="block text-sm font-bold mb-2">ক্যাটাগরি</label>
                        <select name="category"
                            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none focus:ring-2 focus:ring-primary">
                            <option value="Science">Science (বিজ্ঞান)</option>
                            <option value="Arts">Arts (মানবিক)</option>
                            <option value="Commerce">Commerce (ব্যবসায় শিক্ষা)</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-sm font-bold mb-2">ক্লাস লেভেল</label>
                        <select name="classLevel"
                            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none focus:ring-2 focus:ring-primary">
                            <% ['Class 6', 'Class 7' , 'Class 8' , 'Class 9' , 'Class 10' , 'Class 11' , 'Class 12'
                                ].forEach(cls=> { %>
                                <option value="<%= cls %>">
                                    <%= cls %>
                                </option>
                                <% }) %>
                        </select>
                    </div>
                    <div class="md:col-span-2">
                        <label class="block text-sm font-bold mb-4 flex items-center justify-between">
                            <span>সাবস্ক্রিপশন প্ল্যানসমূহ (Pricing Plans)</span>
                            <button type="button" onclick="addPlanRow()"
                                class="px-3 py-1 bg-primary/10 text-primary text-xs rounded-lg font-bold hover:bg-primary/20 transition-all">+
                                নতুন প্ল্যান</button>
                        </label>
                        <div id="plans-container" class="space-y-3">
                            <div class="grid grid-cols-3 gap-3 p-4 bg-slate-50 dark:bg-slate-800/50 rounded-2xl">
                                <input type="text" name="planNames" placeholder="প্ল্যান নাম (উদা: ১ মাস)"
                                    class="px-4 py-2 rounded-xl bg-white dark:bg-slate-900 border-none text-xs">
                                <input type="number" name="planDurations" placeholder="দিন (৩০/৩৬৫/০)"
                                    class="px-4 py-2 rounded-xl bg-white dark:bg-slate-900 border-none text-xs">
                                <input type="number" name="planPrices" placeholder="মূল্য (৳)"
                                    class="px-4 py-2 rounded-xl bg-white dark:bg-slate-900 border-none text-xs">
                            </div>
                        </div>
                    </div>
    </div>
    <div>
        <label class="block text-sm font-bold mb-2 text-purple-600">ফিচারড/অ্যাড হিসেবে দেখান (যেকোনো ক্লাসের
            জন্য)</label>
        <select name="featuredForClasses" multiple
            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none focus:ring-2 focus:ring-primary text-sm h-32">
            <% ['Class 6', 'Class 7' , 'Class 8' , 'Class 9' , 'Class 10' , 'Class 11' , 'Class 12' ].forEach(cls=> { %>
                <option value="<%= cls %>">
                    <%= cls %>
                </option>
                <% }) %>
        </select>
        <p class="text-xs text-slate-400 mt-1">* একাধিক ক্লাস সিলেক্ট করতে Ctrl (Windows) অথবা Cmd (Mac) চেপে ধরুন।</p>
    </div>
    <div>
        <label class="block text-sm font-bold mb-2 text-amber-600">ফ্রি ট্রায়াল দিন (যদি থাকে)</label>
        <input type="number" name="trialPeriod" placeholder="উদা: ৭"
            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none focus:ring-2 focus:ring-primary text-sm">
    </div>
    <div class="md:col-span-2">
        <label class="block text-sm font-bold mb-2">কোর্সের বিবরণ</label>
        <textarea name="description"
            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none focus:ring-2 focus:ring-primary h-24"
            placeholder="কোর্স সম্পর্কে কিছু লিখুন..."></textarea>
    </div>
    <div>
        <label class="block text-sm font-bold mb-2">থাম্বনেইল আপলোড (Thumbnail Photo)</label>
        <input type="file" name="thumbnail" accept="image/*"
            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none focus:ring-2 focus:ring-primary">
    </div>
    <div class="flex items-end text-primary">
        <button type="submit"
            class="w-full py-3 bg-primary text-white rounded-xl font-bold shadow-lg shadow-primary/20 hover:opacity-90 transition-all">সেভ
            করুন</button>
    </div>
    </form>
    </section>

    <!-- Book/Library Management -->
    <section
        class="mb-12 bg-white dark:bg-slate-900 p-8 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm">
        <h3 class="text-xl font-bold mb-6 flex items-center gap-2">
            <span class="material-symbols-outlined text-amber-600">library_books</span> লাইব্রেরি ম্যানেজমেন্ট
            (Books/Notes)
        </h3>
        <form action="/admin/add-book" method="POST" enctype="multipart/form-data"
            class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
                <label class="block text-sm font-bold mb-2">বইয়ের নাম (Title)</label>
                <input type="text" name="title"
                    class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm"
                    placeholder="Physics 1st Paper" required>
            </div>
            <div>
                <label class="block text-sm font-bold mb-2">লেখক (Author)</label>
                <input type="text" name="author"
                    class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm"
                    placeholder="Dr. Shahjahan Tapan">
            </div>
            <div>
                <label class="block text-sm font-bold mb-2">টাইপ</label>
                <select name="type"
                    class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm">
                    <option value="Book">মেইন বই (Book)</option>
                    <option value="Note">সাপ্লিমেন্টারি নোট (Note)</option>
                </select>
            </div>
            <div>
                <label class="block text-sm font-bold mb-2">ক্লাস</label>
                <select name="classLevel"
                    class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm">
                    <% ['Class 6', 'Class 7' , 'Class 8' , 'Class 9' , 'Class 10' , 'Class 11' , 'Class 12'
                        ].forEach(cls=> { %>
                        <option value="<%= cls %>">
                            <%= cls %>
                        </option>
                        <% }) %>
                </select>
            </div>
            <div>
                <label class="block text-sm font-bold mb-2">ফাইল আপলোড (PDF only)</label>
                <input type="file" name="book" accept=".pdf" class="w-full text-xs" required>
            </div>
            <div class="flex items-end">
                <button type="submit" class="w-full py-2 bg-amber-600 text-white rounded-xl font-bold text-sm">লাইব্রেরি
                    তে যোগ
                    করুন</button>
            </div>
        </form>
    </section>
    <section
        class="mb-12 bg-white dark:bg-slate-900 p-8 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm">
        <h3 class="text-xl font-bold mb-6 flex items-center gap-2">
            <span class="material-symbols-outlined text-indigo-500">quiz</span> কোশ্চেন ব্যাংক (Question Bank)
        </h3>
        <form action="/admin/add-question" method="POST" class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
                <label class="block text-sm font-bold mb-2">বিষয় (Subject)</label>
                <input type="text" name="subject"
                    class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm"
                    placeholder="Physics" required>
            </div>
            <div>
                <label class="block text-sm font-bold mb-2">টপিক (Topic/Chapter)</label>
                <input type="text" name="topic"
                    class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm"
                    placeholder="Vector" required>
            </div>
            <div>
                <label class="block text-sm font-bold mb-2">ক্লাস</label>
                <select name="classLevel"
                    class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm">
                    <% ['Class 6', 'Class 7' , 'Class 8' , 'Class 9' , 'Class 10' , 'Class 11' , 'Class 12'
                        ].forEach(cls=> { %>
                        <option value="<%= cls %>">
                            <%= cls %>
                        </option>
                        <% }) %>
                </select>
            </div>
            <div class="md:col-span-2">
                <label class="block text-sm font-bold mb-2">প্রশ্ন (Question Text)</label>
                <input type="text" name="questionText"
                    class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm"
                    placeholder="What is Newton's second law?" required>
            </div>
            <div class="md:col-span-2">
                <label class="block text-sm font-bold mb-2">অপশনসমূহ (কমা দিয়ে আলাদা করুন)</label>
                <input type="text" name="options"
                    class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm"
                    placeholder="Option A, Option B, Option C, Option D" required>
            </div>
            <div>
                <label class="block text-sm font-bold mb-2">সঠিক উত্তর ইনডেক্স (0-3)</label>
                <input type="number" name="correctAnswerIndex" min="0" max="3"
                    class="w-full px-4 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm"
                    placeholder="0" required>
            </div>
            <div class="flex items-end">
                <button type="submit" class="w-full py-2 bg-indigo-500 text-white rounded-xl font-bold text-sm">প্রশ্ন
                    যোগ
                    করুন</button>
            </div>
        </form>
    </section>

    <!-- Course List Overview -->
    <section>
        <h3 class="text-xl font-bold mb-6">বিদ্যমান কোর্সসমূহ</h3>
        <div class="grid grid-cols-1 gap-4">
            <% courses.forEach(course=> { %>
                <div
                    class="bg-white dark:bg-slate-900 p-6 rounded-xl border border-slate-200 dark:border-slate-800 flex justify-between items-center hover:border-primary transition-all group">
                    <div class="flex items-center gap-4">
                        <div
                            class="w-16 h-10 bg-slate-100 dark:bg-slate-800 rounded-lg flex items-center justify-center text-slate-400">
                            <span class="material-symbols-outlined">menu_book</span>
                        </div>
                        <div>
                            <h4 class="font-bold text-slate-900 dark:text-white">
                                <%= course.title %>
                            </h4>
                            <p class="text-xs text-slate-500">
                                <%= course.category %> • <%= course.chapters ? course.chapters.length : 0 %> টি
                                        চ্যাপ্টার
                            </p>
                        </div>
                    </div>
                    <div class="flex gap-2">
                        <a href="/admin/course/<%= course._id %>"
                            class="p-2 text-primary hover:bg-primary/10 rounded-lg" title="ম্যানেজ করুন"><span
                                class="material-symbols-outlined">settings</span></a>
                        <button class="p-2 text-red-500 hover:bg-red-500/10 rounded-lg"><span
                                class="material-symbols-outlined">delete</span></button>
                    </div>
                </div>
                <% }); %>
                    <% if(courses.length===0) { %>
                        <p
                            class="text-slate-500 text-center py-10 bg-slate-50 dark:bg-slate-800/20 rounded-xl border-2 border-dashed border-slate-200 dark:border-slate-800">
                            কোনো কোর্স পাওয়া যায়নি। নতুন একটি যোগ করুন!</p>
                        <% } %>
        </div>
    </section>
    </main>
    </div>
    <script>
        function addPlanRow() {
            const container = document.getElementById('plans-container');
            const newRow = document.createElement('div');
            newRow.className = 'grid grid-cols-3 gap-3 p-4 bg-slate-50 dark:bg-slate-800/50 rounded-2xl';
            newRow.innerHTML = `
                <input type="text" name="planNames" placeholder="প্ল্যান নাম" class="px-4 py-2 rounded-xl bg-white dark:bg-slate-900 border-none text-xs">
                <input type="number" name="planDurations" placeholder="দিন" class="px-4 py-2 rounded-xl bg-white dark:bg-slate-900 border-none text-xs">
                <input type="number" name="planPrices" placeholder="মূল্য (৳)" class="px-4 py-2 rounded-xl bg-white dark:bg-slate-900 border-none text-xs">
            `;
            container.appendChild(newRow);
        }
    </script>
</body>

</html>