<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>কোর্স ম্যানেজমেন্ট | ODDHAY Admin</title>
    <%- include('../partials/head') %>
        <style>
            :root {
                --admin-sidebar-width: 280px;
            }

            .admin-sidebar {
                width: var(--admin-sidebar-width);
                height: 100vh;
                position: fixed;
                left: 0;
                top: 0;
                z-index: 100;
            }

            .admin-main {
                margin-left: var(--admin-sidebar-width);
                min-height: 100vh;
            }

            @media (max-width: 1024px) {
                .admin-sidebar {
                    transform: translateX(-100%);
                }

                .admin-main {
                    margin-left: 0;
                }

                .admin-sidebar.active {
                    transform: translateX(0);
                }
            }
        </style>
</head>

<body class="bg-[#f8fafc] dark:bg-[#0b1120] text-slate-900 dark:text-slate-100 font-sans">

    <!-- Mobile Header -->
    <header
        class="lg:hidden h-16 bg-white dark:bg-[#0f172a] border-b border-slate-100 dark:border-slate-800 flex items-center justify-between px-6 sticky top-0 z-[110]">
        <h1 class="font-black text-xl tracking-tighter">ODDHAY</h1>
        <button onclick="toggleAdminSidebar()"
            class="size-10 flex items-center justify-center rounded-xl bg-slate-50 dark:bg-slate-800">
            <span class="material-symbols-outlined">menu</span>
        </button>
    </header>

    <aside id="adminSidebar"
        class="admin-sidebar bg-white dark:bg-[#0f172a] border-r border-slate-100 dark:border-slate-800 overflow-y-auto transition-transform duration-300">
        <%- include('admin/sidebar_content', { active: 'courses' }) %>
    </aside>

    <main class="admin-main">
        <!-- Top Navigation -->
        <nav
            class="h-20 bg-white/80 dark:bg-[#0f172a]/80 backdrop-blur-md border-b border-slate-100 dark:border-slate-800 flex items-center justify-between px-10 sticky top-0 z-50">
            <div class="flex items-center gap-6 w-1/3">
                <div class="relative w-full max-w-xs group">
                    <span
                        class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-slate-400 group-focus-within:text-primary transition-colors text-[20px]">search</span>
                    <input type="text" placeholder="কোর্স খুঁজুন..."
                        class="w-full bg-slate-50 dark:bg-slate-800/50 border-none rounded-2xl pl-11 pr-4 py-2.5 text-sm font-bold focus:ring-2 focus:ring-primary/20 transition-all">
                </div>
            </div>

            <div class="flex items-center gap-6">
                <button onclick="document.documentElement.classList.toggle('dark')"
                    class="size-11 rounded-2xl bg-slate-50 dark:bg-slate-800 border border-slate-100 dark:border-slate-700 flex items-center justify-center text-slate-500 hover:text-primary transition-all shadow-sm">
                    <span class="material-symbols-outlined dark:hidden">dark_mode</span>
                    <span class="material-symbols-outlined hidden dark:block text-amber-400">light_mode</span>
                </button>

                <div class="flex items-center gap-4">
                    <div class="text-right hidden sm:block">
                        <p class="text-xs font-black leading-none mb-1 capitalize">
                            <%= user.name %>
                        </p>
                        <p class="text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                            <%= user.role %>
                        </p>
                    </div>
                    <div
                        class="size-11 rounded-2xl bg-primary text-white flex items-center justify-center font-black shadow-lg shadow-primary/20 uppercase">
                        <%= user.name[0] %>
                    </div>
                </div>
            </div>
        </nav>

        <section class="p-10 max-w-[1600px] mx-auto">
            <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-6 mb-12">
                <div>
                    <h1 class="text-4xl font-black tracking-tighter mb-2">কোর্স <span
                            class="text-primary">ম্যানেজমেন্ট</span></h1>
                    <p class="text-slate-500 dark:text-slate-400 font-bold text-sm">নতুন কোর্স তৈরি, লেসন আপলোড এবং
                        কন্টেন্ট নিয়ন্ত্রণ করুন।</p>
                </div>
                <% if (user.role==='admin' ) { %>
                    <button onclick="document.getElementById('addCourseModal').classList.remove('hidden')"
                        class="px-8 py-4 bg-primary text-white rounded-[2rem] font-black text-xs uppercase tracking-widest hover:bg-primary-dark transition-all shadow-xl shadow-primary/20 flex items-center gap-2">
                        <span class="material-symbols-outlined text-[20px]">add</span> নতুন কোর্স যোগ করুন
                    </button>
                    <% } %>
            </div>

            <!-- Dashboard Stats for Courses (Mini Stat Cards) -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
                <div class="p-6 rounded-[2rem] bg-indigo-500/5 border border-indigo-500/10 flex items-center gap-5">
                    <div class="size-12 rounded-2xl bg-indigo-500 text-white flex items-center justify-center">
                        <span class="material-symbols-outlined">auto_stories</span>
                    </div>
                    <div>
                        <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest leading-none mb-1">মোট
                            কোর্স</p>
                        <h3 class="text-2xl font-black">
                            <%= courses.length %>
                        </h3>
                    </div>
                </div>
                <div class="p-6 rounded-[2rem] bg-emerald-500/5 border border-emerald-500/10 flex items-center gap-5">
                    <div class="size-12 rounded-2xl bg-emerald-500 text-white flex items-center justify-center">
                        <span class="material-symbols-outlined">person_celebrate</span>
                    </div>
                    <div>
                        <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest leading-none mb-1">মোট
                            শিক্ষার্থী</p>
                        <h3 class="text-2xl font-black">
                            <%= studentCount %>
                        </h3>
                    </div>
                </div>
                <div class="p-6 rounded-[2rem] bg-rose-500/5 border border-rose-500/10 flex items-center gap-5">
                    <div class="size-12 rounded-2xl bg-rose-500 text-white flex items-center justify-center">
                        <span class="material-symbols-outlined">rate_review</span>
                    </div>
                    <div>
                        <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest leading-none mb-1">
                            পেন্ডিং এনরোলমেন্ট</p>
                        <h3 class="text-2xl font-black">০</h3>
                    </div>
                </div>
            </div>

            <!-- Course Grid -->
            <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-8">
                <% if(courses && courses.length> 0) { %>
                    <% courses.forEach(course=> { %>
                        <div
                            class="bg-white dark:bg-[#0f172a] rounded-[2.5rem] border border-slate-100 dark:border-slate-800 group hover:border-primary transition-all duration-500 shadow-sm hover:shadow-xl hover:shadow-primary/5 p-2">
                            <div class="aspect-video rounded-[2rem] overflow-hidden relative mb-6">
                                <img src="<%= course.thumbnail || '/images/course-default.jpg' %>"
                                    class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700">
                                <div class="absolute top-4 left-4 flex gap-2">
                                    <span
                                        class="px-4 py-1.5 rounded-full bg-black/40 backdrop-blur-md text-white font-black text-[9px] uppercase tracking-widest shadow-lg">
                                        <%= course.classLevel %>
                                    </span>
                                    <span
                                        class="px-4 py-1.5 rounded-full bg-emerald-500/80 backdrop-blur-md text-white font-black text-[9px] uppercase tracking-widest shadow-lg">
                                        <%= course.accessType %>
                                    </span>
                                </div>
                            </div>
                            <div class="px-6 pb-6 pt-2">
                                <h3 class="text-lg font-black mb-1 truncate">
                                    <%= course.title %>
                                </h3>
                                <p class="text-[11px] font-bold text-slate-400 uppercase tracking-widest mb-6">
                                    <%= course.subject %> • <%= course.category %>
                                </p>

                                <div
                                    class="flex items-center justify-between pt-6 border-t border-slate-100 dark:border-slate-800">
                                    <div class="flex flex-col gap-1">
                                        <div class="flex items-center gap-2">
                                            <span
                                                class="material-symbols-outlined text-primary text-[18px]">group</span>
                                            <span class="text-sm font-black text-slate-700 dark:text-slate-300">
                                                <%= course.studentCount || 0 %> ছাত্র
                                            </span>
                                        </div>
                                        <p class="text-[10px] font-bold text-slate-400 capitalize">ইন্সট্রাক্টর: <%=
                                                course.instructor ? course.instructor.name : 'অ্যাডমিন' %>
                                        </p>
                                    </div>
                                    <div class="flex gap-2">
                                        <% if (user.role==='admin' ) { %>
                                            <a href="/admin/course/<%= course._id %>"
                                                class="size-11 flex items-center justify-center bg-slate-50 dark:bg-slate-800 rounded-2xl text-slate-500 hover:text-primary hover:bg-primary/10 transition-all">
                                                <span class="material-symbols-outlined">settings</span>
                                            </a>
                                            <% } %>
                                                <a href="/course-details/<%= course._id %>"
                                                    class="size-11 flex items-center justify-center bg-primary text-white rounded-2xl shadow-lg shadow-primary/20 hover:scale-105 transition-all">
                                                    <span class="material-symbols-outlined">visibility</span>
                                                </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <% }) %>
                            <% } else { %>
                                <div
                                    class="col-span-full py-24 text-center border-4 border-dashed border-slate-100 dark:border-slate-800 rounded-[4rem]">
                                    <div
                                        class="size-24 rounded-full bg-slate-50 dark:bg-slate-800 flex items-center justify-center mx-auto mb-6">
                                        <span
                                            class="material-symbols-outlined text-[48px] text-slate-300">work_off</span>
                                    </div>
                                    <h4 class="text-2xl font-black mb-2">কোনো কোর্স পাওয়া যায়নি</h4>
                                    <p class="text-slate-400 font-bold max-w-xs mx-auto">এখন পর্যন্ত কোনো কোর্স তৈরি করা
                                        হয়নি। নতুন কোর্স যোগ করে কন্টেন্ট স্ট্রীমিং শুরু করুন।</p>
                                </div>
                                <% } %>
            </div>
        </section>
    </main>

    <!-- Add Course Modal (Simplified placeholder, can be expanded to full form) -->
    <!-- Add Course Modal -->
    <% if (user.role==='admin' ) { %>
        <div id="addCourseModal"
            class="fixed inset-0 bg-slate-900/60 backdrop-blur-sm z-[200] hidden flex items-center justify-center p-6">
            <div class="bg-white dark:bg-[#0f172a] rounded-[3rem] w-full max-w-2xl overflow-hidden shadow-2xl">
                <div class="p-10">
                    <div class="flex justify-between items-center mb-10">
                        <h3 class="text-2xl font-black">নতুন কোর্স <span class="text-primary">ফার্ম</span></h3>
                        <button onclick="document.getElementById('addCourseModal').classList.add('hidden')"
                            class="size-10 rounded-xl bg-slate-50 dark:bg-slate-800 flex items-center justify-center text-slate-400"><span
                                class="material-symbols-outlined">close</span></button>
                    </div>
                    <form action="/admin/add-course" method="POST" enctype="multipart/form-data" class="space-y-6">
                        <div class="grid grid-cols-2 gap-6">
                            <div class="space-y-2">
                                <label
                                    class="text-[10px] font-black text-slate-400 uppercase tracking-widest ml-2">কোর্স
                                    শিরোনাম</label>
                                <input type="text" name="title" required
                                    class="w-full bg-slate-50 dark:bg-slate-800 border-none rounded-2xl px-6 py-3.5 text-sm font-bold focus:ring-2 focus:ring-primary/20 transition-all">
                            </div>
                            <div class="space-y-2">
                                <label
                                    class="text-[10px] font-black text-slate-400 uppercase tracking-widest ml-2">বিষয়</label>
                                <input type="text" name="subject" required
                                    class="w-full bg-slate-50 dark:bg-slate-800 border-none rounded-2xl px-6 py-3.5 text-sm font-bold focus:ring-2 focus:ring-primary/20 transition-all">
                            </div>
                        </div>
                        <div class="grid grid-cols-3 gap-6">
                            <div class="space-y-2">
                                <label
                                    class="text-[10px] font-black text-slate-400 uppercase tracking-widest ml-2">ক্লাস</label>
                                <select name="classLevel"
                                    class="w-full bg-slate-50 dark:bg-slate-800 border-none rounded-2xl px-6 py-3.5 text-sm font-bold focus:ring-2 focus:ring-primary/20">
                                    <option>Class 9</option>
                                    <option>Class 10</option>
                                    <option>HSC</option>
                                </select>
                            </div>
                            <div class="space-y-2">
                                <label
                                    class="text-[10px] font-black text-slate-400 uppercase tracking-widest ml-2">অ্যাক্সেস</label>
                                <select name="accessType"
                                    class="w-full bg-slate-50 dark:bg-slate-800 border-none rounded-2xl px-6 py-3.5 text-sm font-bold focus:ring-2 focus:ring-primary/20">
                                    <option value="free">ফ্রি</option>
                                    <option value="paid">পেইড</option>
                                </select>
                            </div>
                            <div class="space-y-2">
                                <label
                                    class="text-[10px] font-black text-slate-400 uppercase tracking-widest ml-2">থাম্বনেইল</label>
                                <input type="file" name="thumbnail" class="w-full text-xs font-black">
                            </div>
                        </div>
                        <button type="submit"
                            class="w-full py-4 bg-primary text-white rounded-2xl font-black text-xs uppercase tracking-widest shadow-xl shadow-primary/20 hover:scale-[1.02] transition-all mt-4">কোর্স
                            সংরক্ষণ করুন</button>
                    </form>
                </div>
            </div>
        </div>
        <% } %>

            <script>
                function toggleAdminSidebar() {
                    document.getElementById('adminSidebar').classList.toggle('active');
                }
            </script>
</body>

</html>