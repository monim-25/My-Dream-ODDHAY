<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>কোর্স লাইব্রেরি | ODDHAY</title>
    <%- include('partials/head') %>
</head>

<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100 min-h-screen">
    <%- include('partials/nav') %>

        <main class="pt-32 pb-20 px-6 max-w-7xl mx-auto">
            <div class="mb-12">
                <h1 class="text-4xl md:text-5xl font-black mb-4">আমাদের <span class="text-primary">কোর্সসমূহ</span></h1>
                <p class="text-slate-500 max-w-2xl">আপনার পছন্দের বিষয়টি বেছে নিন এবং আজই শেখা শুরু করুন। আমাদের সকল
                    কোর্স অভিজ্ঞ শিক্ষকদের দ্বারা তৈরি।</p>
            </div>

            <!-- Filters -->
            <div class="flex flex-wrap gap-4 mb-12">
                <a href="/courses"
                    class="px-6 py-2.5 rounded-xl <%= !activeCategory || activeCategory === 'all' ? 'bg-primary text-white' : 'glass-nav text-slate-500' %> font-bold text-sm transition-all shadow-lg shadow-primary/20">সবগুলো</a>
                <% categories.forEach(cat=> { %>
                    <a href="/courses?category=<%= cat %>"
                        class="px-6 py-2.5 rounded-xl <%= activeCategory === cat ? 'bg-primary text-white' : 'glass-nav text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800' %> font-bold text-sm transition-all">
                        <%= cat %>
                    </a>
                    <% }) %>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
                <% courses.forEach(course=> { %>
                    <div
                        class="glass-nav rounded-[2.5rem] overflow-hidden hover:scale-[1.02] transition-all duration-500 group shadow-xl">
                        <div class="aspect-video relative overflow-hidden">
                            <img src="<%= course.thumbnail || 'https://via.placeholder.com/600x400' %>"
                                class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700">
                            <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent"></div>
                            <div class="absolute bottom-6 left-6 flex items-center gap-2">
                                <span
                                    class="px-3 py-1 rounded-lg bg-primary text-white text-[10px] font-black uppercase tracking-widest">
                                    <%= course.classLevel %>
                                </span>
                                <span
                                    class="px-3 py-1 rounded-lg bg-white/20 backdrop-blur-md text-white text-[10px] font-black uppercase tracking-widest">
                                    <%= course.category %>
                                </span>
                            </div>
                        </div>
                        <div class="p-8">
                            <h3 class="text-xl font-black mb-4 group-hover:text-primary transition-colors">
                                <%= course.title %>
                            </h3>
                            <div class="flex items-center justify-between mt-auto">
                                <div class="flex flex-col">
                                    <span class="text-[10px] font-bold text-slate-400 uppercase">শুরু করুন মাত্র</span>
                                    <span class="text-2xl font-black text-primary">৳ <%= (course.plans &&
                                            course.plans.length> 0) ? course.plans[0].price : '0' %></span>
                                </div>
                                <a href="/course-details/<%= course._id %>"
                                    class="size-12 rounded-2xl bg-primary text-white flex items-center justify-center shadow-lg shadow-primary/30 hover:rotate-90 transition-all duration-500">
                                    <span class="material-symbols-outlined">arrow_outward</span>
                                </a>
                            </div>
                        </div>
                    </div>
                    <% }) %>
                        <% if(courses.length===0) { %>
                            <div class="col-span-full py-32 text-center glass-nav rounded-[3rem]">
                                <span class="material-symbols-outlined text-8xl text-slate-200 mb-6">school</span>
                                <h2 class="text-3xl font-black mb-2 tracking-tight">বর্তমানে কোনো কোর্স নেই</h2>
                                <p class="text-slate-500">শীঘ্রই নতুন কোর্স যোগ করা হবে। আমাদের সাথেই থাকুন।</p>
                            </div>
                            <% } %>
            </div>
        </main>

        <%- include('partials/footer') %>
</body>

</html>