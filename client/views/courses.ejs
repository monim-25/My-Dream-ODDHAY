<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>কোর্স লাইব্রেরি | অধ্যায়</title>
    <%- include('partials/head') %>
</head>

<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100 min-h-screen">
    <%- include('partials/nav') %>

        <main class="relative overflow-hidden pt-36 pb-20 px-6 max-w-7xl mx-auto">
            <!-- Background Blobs -->
            <div class="bg-blob -top-20 -left-20 opacity-30"></div>
            <div class="bg-blob top-1/2 -right-20 opacity-20" style="animation-delay: -5s;"></div>

            <div class="mb-16 reveal">
                <h1 class="h1-hero">কোর্সসমূহ</h1>
            </div>

            <!-- Filters -->
            <div class="flex flex-wrap gap-3 mb-16 reveal delay-100">
                <a href="/courses"
                    class="px-8 py-3.5 rounded-2xl font-black text-xs uppercase tracking-widest transition-all shadow-xl <%= !activeCategory || activeCategory === 'all' ? 'bg-primary text-white shadow-primary/20 scale-105' : 'bg-white dark:bg-slate-900 text-slate-500 hover:text-primary border border-slate-100 dark:border-slate-800' %>">
                    সবগুলো
                </a>
                <% categories.forEach(cat=> { %>
                    <a href="/courses?category=<%= cat %>"
                        class="px-8 py-3.5 rounded-2xl font-black text-xs uppercase tracking-widest transition-all shadow-xl <%= activeCategory === cat ? 'bg-primary text-white shadow-primary/20 scale-105' : 'bg-white dark:bg-slate-900 text-slate-500 hover:text-primary border border-slate-100 dark:border-slate-800' %>">
                        <%= cat %>
                    </a>
                    <% }) %>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10 reveal delay-200">
                <% courses.forEach(course=> { %>
                    <div
                        class="group card-smart !p-0 overflow-hidden hover:translate-y-[-10px] transition-all duration-500">
                        <div class="aspect-[16/10] relative overflow-hidden">
                            <img src="<%= course.thumbnail || 'https://via.placeholder.com/600x400' %>"
                                class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-1000">
                            <div
                                class="absolute inset-0 bg-gradient-to-t from-slate-900/90 via-slate-900/10 to-transparent">
                            </div>
                            <div class="absolute top-4 left-4 flex gap-2">
                                <span
                                    class="px-3 py-1 rounded-full bg-white/20 backdrop-blur-md text-white font-black text-[10px] border border-white/20 uppercase tracking-tighter">
                                    <%= course.classLevel %>
                                </span>
                            </div>
                        </div>
                        <div class="p-8">
                            <div class="flex items-center gap-2 mb-3">
                                <span class="text-[10px] font-black text-primary uppercase tracking-widest">
                                    <%= course.category %>
                                </span>
                                <span class="text-slate-300">•</span>
                                <span
                                    class="text-[10px] font-black text-slate-400 uppercase tracking-widest leading-none">১০+
                                    লেসন</span>
                            </div>
                            <h3
                                class="text-xl font-black mb-6 group-hover:text-primary transition-colors line-clamp-2 leading-tight">
                                <%= course.title %>
                            </h3>

                            <div
                                class="flex items-center justify-between pt-6 border-t border-slate-100 dark:border-slate-800">
                                <div class="flex flex-col">
                                    <span class="text-[10px] font-black text-slate-400 uppercase mb-1">কործ ফি</span>
                                    <span class="text-2xl font-black text-primary">৳ <%= (course.plans &&
                                            course.plans.length> 0) ? course.plans[0].price : '0' %></span>
                                </div>
                                <a href="/course-details/<%= course._id %>"
                                    class="size-14 rounded-2xl bg-primary text-white flex items-center justify-center shadow-lg shadow-primary/30 hover:bg-primary-dark transition-all duration-300">
                                    <span class="material-symbols-outlined text-2xl">shopping_cart</span>
                                </a>
                            </div>
                        </div>
                    </div>
                    <% }) %>

                        <% if(courses.length===0) { %>
                            <div class="col-span-full py-32 text-center card-smart border-dashed border-2">
                                <div
                                    class="size-24 rounded-full bg-slate-100 dark:bg-slate-800 flex items-center justify-center mx-auto mb-8 text-slate-300">
                                    <span class="material-symbols-outlined text-6xl">school</span>
                                </div>
                                <h2 class="text-3xl font-black mb-4 tracking-tight">বর্তমানে কোনো কোর্স নেই</h2>
                                <p class="text-slate-500 font-bold">খুব শীঘ্রই আমরা নতুন কোর্স নিয়ে আসছি। আমাদের সাথেই
                                    থাকুন!</p>
                            </div>
                            <% } %>
            </div>
        </main>

        <%- include('partials/footer') %>

            <script>
                document.addEventListener('DOMContentLoaded', () => {
                    const observer = new IntersectionObserver((entries) => {
                        entries.forEach(entry => entry.isIntersecting && entry.target.classList.add('active'));
                    }, { threshold: 0.1 });
                    document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
                });
            </script>
</body>

</html>