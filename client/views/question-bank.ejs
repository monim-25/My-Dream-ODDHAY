<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>কোয়েশ্চেন ব্যাঙ্ক | ODDHAY</title>
    <%- include('partials/head') %>
</head>

<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100 min-h-screen">
    <%- include('partials/nav') %>

        <main class="pt-32 pb-20 px-6 max-w-7xl mx-auto">
            <div class="mb-16 reveal">
                <p class="text-primary font-bold uppercase tracking-[0.2em] text-[10px] mb-3">প্রশ্নপত্র</p>
                <h1 class="h1-hero">ব্যাঙ্ক</h1>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
                <% questions.forEach(q=> { %>
                    <div
                        class="glass-nav p-8 rounded-[2.5rem] hover:scale-[1.02] transition-all duration-500 group shadow-xl">
                        <div class="flex justify-between items-start mb-6">
                            <span
                                class="bg-indigo-500/10 text-indigo-600 text-[10px] font-black px-4 py-1.5 rounded-full uppercase tracking-widest">
                                <%= q.board %>
                            </span>
                            <span class="text-primary font-black text-xl">
                                <%= q.year %>
                            </span>
                        </div>
                        <h3 class="font-black text-xl mb-2 group-hover:text-primary transition-colors">
                            <%= q.subject %>
                        </h3>
                        <p class="text-xs font-bold text-slate-400 mb-8 uppercase tracking-widest">
                            <%= q.classLevel %>
                        </p>
                        <a href="<%= q.fileUrl %>" target="_blank"
                            class="w-full py-4 bg-primary text-white rounded-2xl font-black text-sm flex items-center justify-center gap-2 hover:opacity-90 transition-all shadow-xl shadow-primary/30">
                            ডাউনলোড করুন <span class="material-symbols-outlined text-lg">download</span>
                        </a>
                    </div>
                    <% }) %>
                        <% if(questions.length===0) { %>
                            <div class="col-span-full py-32 text-center glass-nav rounded-[3rem]">
                                <span class="material-symbols-outlined text-8xl text-slate-200 mb-6">history_edu</span>
                                <h2 class="text-3xl font-black mb-2 tracking-tight">আপাতত কোনো প্রশ্ন নেই</h2>
                                <p class="text-slate-500">শীঘ্রই সকল বোর্ড প্রশ্নগুলো এখানে যোগ করা হবে।</p>
                            </div>
                            <% } %>
            </div>
        </main>

        <%- include('partials/footer') %>
</body>

</html>