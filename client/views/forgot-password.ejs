<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>পাসওয়ার্ড রিসেট | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..900&family=Noto+Sans+Bengali:wght@300..900&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet" />
    <style>
        body { font-family: 'Lexend', 'Noto Sans Bengali', sans-serif; }
        .btn-animate { transition: all 0.2s ease; }
        .btn-animate:hover { transform: translateY(-2px); box-shadow: 0 8px 25px rgba(59,130,246,0.4); }
        .btn-animate:active { transform: translateY(0); }
    </style>
</head>

<body class="bg-gradient-to-br from-blue-50 to-indigo-100 dark:bg-[#101922] text-[#0d141b] dark:text-slate-100 min-h-screen flex items-center justify-center p-4">
    <div class="max-w-md w-full">
        <!-- Logo -->
        <div class="text-center mb-8">
            <a href="/" class="inline-flex items-center gap-2 text-2xl font-black text-blue-600">
                🎓 <span>ODDHAY</span>
            </a>
        </div>

        <div class="bg-white dark:bg-slate-900 rounded-3xl p-10 shadow-2xl border border-white/60 dark:border-slate-800">
            <div class="text-center mb-8">
                <div class="size-16 bg-blue-500/10 text-blue-500 rounded-full flex items-center justify-center mx-auto mb-4">
                    <span class="material-symbols-outlined text-3xl">lock_reset</span>
                </div>
                <h1 class="text-2xl font-black mb-2">পাসওয়ার্ড ভুলে গেছেন?</h1>
                <p class="text-slate-500 text-sm">আপনার ইমেইল দিন, আমরা একটি রিসেট লিঙ্ক পাঠাব।</p>
            </div>

            <% if (locals.error && error) { %>
                <div class="mb-6 p-4 bg-red-50 text-red-600 text-sm font-semibold rounded-xl flex items-center gap-3 border border-red-100">
                    <span class="material-symbols-outlined text-xl flex-shrink-0">error</span>
                    <span><%= error %></span>
                </div>
            <% } %>

            <% if (locals.message && message) { %>
                <div class="mb-6 p-4 bg-emerald-50 text-emerald-700 text-sm font-semibold rounded-xl flex items-center gap-3 border border-emerald-100">
                    <span class="material-symbols-outlined text-xl flex-shrink-0">mark_email_read</span>
                    <span><%= message %></span>
                </div>
                <div class="text-center mt-4">
                    <a href="/login" class="text-blue-500 font-bold hover:underline text-sm">← লগইন পেজে ফিরে যান</a>
                </div>
            <% } else { %>
                <form action="/forgot-password" method="POST" class="space-y-5" id="forgotForm">
                    <div>
                        <label class="block text-xs font-bold mb-2 ml-1 text-slate-500 uppercase tracking-wide">ইমেইল ঠিকানা</label>
                        <div class="relative">
                            <span class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 text-xl">mail</span>
                            <input type="email" name="email" id="email"
                                class="w-full pl-12 pr-4 py-3.5 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent font-medium transition-all"
                                placeholder="example@gmail.com" required autofocus>
                        </div>
                    </div>

                    <button type="submit" id="submitBtn"
                        class="btn-animate w-full py-3.5 bg-blue-600 text-white rounded-xl font-bold shadow-lg shadow-blue-500/30 flex items-center justify-center gap-2">
                        <span class="material-symbols-outlined text-xl" id="btnIcon">send</span>
                        <span id="btnText">রিসেট লিঙ্ক পাঠান</span>
                    </button>
                </form>

                <div class="mt-8 text-center">
                    <a href="/login" class="text-slate-400 font-semibold hover:text-slate-600 text-sm transition-colors">
                        ← লগইন পেজে ফিরে যান
                    </a>
                </div>
            <% } %>
        </div>

        <p class="text-center text-slate-400 text-xs mt-6">© 2025 ODDHAY শিক্ষা প্ল্যাটফর্ম</p>
    </div>

    <script>
        document.getElementById('forgotForm')?.addEventListener('submit', function() {
            const btn = document.getElementById('submitBtn');
            const icon = document.getElementById('btnIcon');
            const text = document.getElementById('btnText');
            btn.disabled = true;
            btn.classList.add('opacity-75');
            icon.textContent = 'hourglass_empty';
            text.textContent = 'পাঠানো হচ্ছে...';
        });
    </script>
</body>
</html>