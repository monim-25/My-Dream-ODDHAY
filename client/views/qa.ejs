<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>Q&A ফোরাম | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..900&family=Noto+Sans+Bengali:wght@300..900&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: { "primary": "#137fec", "background-light": "#f6f7f8", "background-dark": "#101922" },
                    fontFamily: { "display": ["Lexend", "Noto Sans Bengali", "sans-serif"] },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Lexend', 'Noto Sans Bengali', sans-serif;
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .dark .glass-card {
            background: rgba(16, 25, 34, 0.8);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }
    </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100 min-h-screen">
    <header
        class="sticky top-0 z-[100] w-full border-b border-slate-200 dark:border-slate-800 bg-white/90 dark:bg-background-dark/90 backdrop-blur-md px-6 py-4">
        <div class="max-w-7xl mx-auto flex items-center justify-between">
            <a href="/" class="flex items-center gap-2">
                <div class="text-primary"><svg class="size-8" fill="none" viewbox="0 0 48 48"
                        xmlns="http://www.w3.org/2000/svg">
                        <path d="M4 4H17.3334V17.3334H30.6666V30.6666H44V44H4V4Z" fill="currentColor"></path>
                    </svg></div>
                <h1 class="text-2xl font-black">ODDHAY</h1>
            </a>
            <nav class="hidden md:flex items-center gap-8 text-sm font-bold">
                <a href="/" class="hover:text-primary transition-colors">হোম</a>
                <a href="/#features" class="hover:text-primary transition-colors">সেবাসমূহ</a>
                <a href="/courses" class="hover:text-primary transition-colors">কোর্সসমূহ</a>
                <a href="/about" class="hover:text-primary transition-colors">আমাদের সম্পর্কে</a>
                <a href="/#contact" class="hover:text-primary transition-colors">যোগাযোগ</a>
            </nav>
            <div class="flex items-center gap-4">
                <button class="p-2 rounded-xl bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-300"
                    onclick="document.documentElement.classList.toggle('dark')">
                    <span class="material-symbols-outlined block dark:hidden">dark_mode</span>
                    <span class="material-symbols-outlined hidden dark:block">light_mode</span>
                </button>
                <div class="hidden md:flex items-center gap-3">
                    <% if (locals.user) { %>
                        <a href="/dashboard"
                            class="text-sm font-bold px-4 py-2 text-primary hover:bg-primary/10 rounded-lg">ড্যাশবোর্ড</a>
                        <a href="/logout" class="bg-red-500 text-white text-xs font-bold px-4 py-2 rounded-lg">লগআউট</a>
                        <% } else { %>
                            <a href="/login"
                                class="text-sm font-bold px-4 py-2 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-xl">লগইন</a>
                            <a href="/register"
                                class="bg-primary text-white text-sm font-bold px-5 py-2 rounded-xl">রেজিস্ট্রেশন</a>
                            <% } %>
                </div>
                <button class="md:hidden p-2" onclick="toggleMobileMenu()"><span class="material-symbols-outlined"
                        id="menu-icon">menu</span></button>
            </div>
        </div>
        <div id="mobile-menu"
            class="hidden fixed inset-0 z-[9999] bg-white dark:bg-background-dark p-6 transition-transform duration-300 translate-x-full overflow-y-auto">
            <div class="flex justify-end mb-8"><button class="p-2 rounded-full bg-slate-100 dark:bg-slate-800"
                    onclick="toggleMobileMenu()"><span class="material-symbols-outlined">close</span></button></div>
            <nav class="flex flex-col gap-6 text-center">
                <a class="text-2xl font-black" href="/" onclick="toggleMobileMenu()">হোম</a>
                <a class="text-2xl font-black" href="/#features" onclick="toggleMobileMenu()">সেবাসমূহ</a>
                <a class="text-2xl font-black" href="/courses" onclick="toggleMobileMenu()">কোর্সসমূহ</a>
                <a class="text-2xl font-black" href="/about" onclick="toggleMobileMenu()">আমাদের সম্পর্কে</a>
                <hr class="border-slate-100 dark:border-slate-800 my-4">
                <% if (locals.user) { %><a href="/dashboard"
                        class="py-3 bg-primary/10 text-primary rounded-xl font-bold">ড্যাশবোর্ড</a>
                    <% } else { %>
                        <a href="/login" class="py-4 border-2 rounded-2xl font-bold">লগইন</a>
                        <a href="/register" class="py-4 bg-primary text-white rounded-2xl font-black">রেজিস্ট্রেশন</a>
                        <% } %>
            </nav>
        </div>
    </header>

    <main class="max-w-4xl mx-auto px-6 py-12">
        <div class="mb-12 text-center">
            <h2 class="text-4xl font-black mb-4">Q&A <span class="text-primary">ফোরাম</span></h2>
            <p class="text-slate-500">আপনার যেকোনো একাডেমিক সমস্যা এখানে লিখে পাঠান, আমাদের মেন্টররা দ্রুত সমাধান দেবেন।
            </p>
        </div>

        <!-- Question Post Form -->
        <div class="glass-card p-6 rounded-[2rem] mb-12 shadow-xl">
            <form action="/qa-ask" method="POST" class="space-y-4">
                <textarea name="question" rows="3" placeholder="আপনার প্রশ্নটি বিস্তারিত লিখুন..."
                    class="w-full p-6 rounded-2xl bg-slate-50 dark:bg-slate-800 border-none focus:ring-2 focus:ring-primary transition-all resize-none"></textarea>
                <div class="flex justify-end">
                    <button type="submit"
                        class="px-8 py-3 bg-primary text-white rounded-xl font-bold flex items-center gap-2 hover:scale-105 transition-all">
                        প্রশ্ন পাঠান <span class="material-symbols-outlined text-sm">send</span>
                    </button>
                </div>
            </form>
        </div>

        <!-- Q&A List -->
        <div class="space-y-6">
            <% qas.forEach(item=> { %>
                <div
                    class="glass-card p-8 rounded-[2rem] hover:border-primary/30 transition-all border border-transparent">
                    <div class="flex items-center gap-3 mb-4">
                        <div
                            class="size-10 rounded-full bg-primary/10 flex items-center justify-center font-bold text-primary">
                            <%= item.askedBy ? item.askedBy.name[0] : 'U' %>
                        </div>
                        <div>
                            <p class="font-bold text-sm">
                                <%= item.askedBy ? item.askedBy.name : 'Unknown User' %>
                            </p>
                            <p class="text-[10px] text-slate-400">
                                <%= item.createdAt.toLocaleDateString() %>
                            </p>
                        </div>
                        <% if(item.status==='resolved' ) { %>
                            <span
                                class="ml-auto bg-green-500/10 text-green-500 text-[10px] font-bold px-3 py-1 rounded-full uppercase flex items-center gap-1">
                                <span class="material-symbols-outlined text-xs">check_circle</span> সমাধানকৃত
                            </span>
                            <% } else { %>
                                <span
                                    class="ml-auto bg-amber-500/10 text-amber-500 text-[10px] font-bold px-3 py-1 rounded-full uppercase">অপেক্ষমান</span>
                                <% } %>
                    </div>
                    <h3 class="text-xl font-bold mb-4">
                        <%= item.question %>
                    </h3>
                    <% if(item.answer) { %>
                        <div class="mt-6 p-6 bg-slate-50 dark:bg-slate-800/50 rounded-2xl border-l-4 border-primary">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="material-symbols-outlined text-primary text-sm">verified</span>
                                <span class="text-sm font-bold">এডমিন/মেন্টর দ্বারা সমাধান</span>
                            </div>
                            <p class="text-slate-600 dark:text-slate-400 leading-relaxed">
                                <%= item.answer %>
                            </p>
                        </div>
                        <% } %>
                </div>
                <% }) %>
                    <% if(qas.length===0) { %>
                        <div class="py-20 text-center glass-card rounded-[3rem]">
                            <span class="material-symbols-outlined text-6xl text-slate-300">forum</span>
                            <h3 class="text-xl font-bold mt-4">এখনো কেউ প্রশ্ন করেনি</h3>
                            <p class="text-slate-500">প্রথম প্রশ্নটি আপনিই করুন!</p>
                        </div>
                        <% } %>
        </div>
    </main>

    <script>
        function toggleMobileMenu() {
            const menu = document.getElementById('mobile-menu');
            const icon = document.getElementById('menu-icon');
            if (menu.classList.contains('hidden')) {
                menu.classList.remove('hidden'); document.body.style.overflow = 'hidden';
                setTimeout(() => menu.classList.remove('translate-x-full'), 10);
                if (icon) icon.innerText = 'close';
            } else {
                menu.classList.add('translate-x-full'); document.body.style.overflow = '';
                setTimeout(() => menu.classList.add('hidden'), 300);
                if (icon) icon.innerText = 'menu';
            }
        }
    </script>
</body>

</html>