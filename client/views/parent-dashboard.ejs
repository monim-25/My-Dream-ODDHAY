<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>অভিভাবক ড্যাশবোর্ড | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..900&family=Noto+Sans+Bengali:wght@300..900&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: { "primary": "#137fec", "background-light": "#f6f7f8", "background-dark": "#101922" },
                    fontFamily: { "display": ["Lexend", "Noto Sans Bengali", "sans-serif"] },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Lexend', 'Noto Sans Bengali', sans-serif;
        }
    </style>
</head>

<body class="bg-[#f8fafc] dark:bg-background-dark text-[#0d141b] dark:text-slate-100 flex min-h-screen">
    <!-- Sidebar -->
    <aside
        class="w-64 border-r border-slate-200 dark:border-slate-800 bg-white/70 dark:bg-background-dark/70 backdrop-blur-xl hidden lg:flex flex-col sticky top-0 h-screen">
        <div class="p-6">
            <a href="/" class="flex items-center gap-2 mb-10">
                <div class="text-primary"><svg class="size-8" fill="none" viewbox="0 0 48 48"
                        xmlns="http://www.w3.org/2000/svg">
                        <path d="M4 4H17.3334V17.3334H30.6666V30.6666H44V44H4V4Z" fill="currentColor"></path>
                    </svg></div>
                <h1 class="text-xl font-black">ODDHAY</h1>
            </a>
            <nav class="space-y-1">
                <a href="/parent/dashboard"
                    class="flex items-center gap-3 px-4 py-3 rounded-xl bg-primary/10 text-primary font-bold">
                    <span class="material-symbols-outlined">family_restroom</span> মনিটর প্যানেল
                </a>
                <a href="/logout"
                    class="flex items-center gap-3 px-4 py-3 rounded-xl text-red-500 hover:bg-red-50 transition-all font-bold">
                    <span class="material-symbols-outlined">logout</span> লগআউট
                </a>
            </nav>
        </div>
    </aside>

    <main class="flex-1 p-4 md:p-8 lg:p-12">
        <header class="flex justify-between items-center mb-10">
            <div>
                <h2 class="text-3xl font-black mb-1">অভিভাবক পোর্টাল 👋</h2>
                <p class="text-slate-500">আপনার সন্তানের পড়াশোনার অগ্রগতি পর্যবেক্ষণ করুন।</p>
            </div>
            <button class="p-3 rounded-2xl bg-white dark:bg-slate-900 border border-slate-200"
                onclick="document.documentElement.classList.toggle('dark')">
                <span class="material-symbols-outlined block dark:hidden">dark_mode</span>
                <span class="material-symbols-outlined hidden dark:block">light_mode</span>
            </button>
        </header>

        <!-- Child Selection / Add Child -->
        <section class="mb-10 flex flex-wrap gap-4 items-center justify-between">
            <div class="flex gap-4">
                <% if(confirmedChildren && confirmedChildren.length> 0) { %>
                    <% confirmedChildren.forEach((child, index)=> { %>
                        <div
                            class="px-6 py-3 rounded-2xl bg-primary text-white font-bold shadow-lg shadow-primary/20 cursor-pointer">
                            <%= child.name %>
                        </div>
                        <% }); %>
                            <% } %>
            </div>
            <button onclick="document.getElementById('add-child-modal').classList.toggle('hidden')"
                class="px-6 py-3 rounded-2xl border-2 border-dashed border-primary text-primary font-bold hover:bg-primary/5 transition-all flex items-center gap-2">
                <span class="material-symbols-outlined">add_circle</span> সন্তান যোগ করুন
            </button>
        </section>

        <!-- Progress Overview -->
        <% if(confirmedChildren && confirmedChildren.length> 0) { %>
            <% confirmedChildren.forEach(child=> { %>
                <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 mb-12">
                    <div class="lg:col-span-2 space-y-6">
                        <div
                            class="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 p-8 rounded-[2.5rem] shadow-sm">
                            <h3 class="text-xl font-bold mb-6 flex items-center justify-between">
                                <span>পড়াশোনার অগ্রগতি (<%= child.name %>)</span>
                                <span class="text-sm font-medium text-primary">
                                    <%= child.completedLessons.length %> টি লেসন শেষ হয়েছে
                                </span>
                            </h3>
                            <div class="w-full bg-slate-100 dark:bg-slate-800 h-4 rounded-full overflow-hidden">
                                <div class="bg-primary h-full transition-all duration-1000" style="width: 65%"></div>
                            </div>
                            <p class="mt-4 text-sm text-slate-500">সিলেবাসের ৬৫% সম্পন্ন হয়েছে।</p>
                        </div>

                        <div
                            class="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 p-8 rounded-[2.5rem] shadow-sm">
                            <h3 class="text-xl font-bold mb-6">সাম্প্রতিক পরীক্ষার ফলাফল</h3>
                            <div class="space-y-4">
                                <% if(child.quizResults && child.quizResults.length> 0) { %>
                                    <% child.quizResults.reverse().slice(0, 3).forEach(result=> { %>
                                        <div
                                            class="flex items-center justify-between p-4 bg-slate-50 dark:bg-slate-800 rounded-2xl">
                                            <div class="flex items-center gap-3">
                                                <div
                                                    class="size-10 rounded-xl bg-amber-500/10 text-amber-500 flex items-center justify-center font-bold">
                                                    Q</div>
                                                <div>
                                                    <p class="font-bold text-sm">
                                                        <%= result.quiz ? result.quiz.title : 'Quiz' %>
                                                    </p>
                                                    <p class="text-[10px] text-slate-400">
                                                        <%= new Date(result.date).toLocaleDateString('bn-BD') %>
                                                    </p>
                                                </div>
                                            </div>
                                            <div class="text-right">
                                                <p class="font-black text-primary">
                                                    <%= result.score %>/<%= result.total %>
                                                </p>
                                                <p class="text-[10px] text-slate-400 uppercase">স্কোর</p>
                                            </div>
                                        </div>
                                        <% }); %>
                                            <% } else { %>
                                                <p class="text-slate-400 text-center italic">এখনো কোনো পরীক্ষা দেয়নি।
                                                </p>
                                                <% } %>
                            </div>
                        </div>
                    </div>

                    <div class="lg:col-span-1">
                        <div
                            class="bg-primary p-8 rounded-[2.5rem] text-white shadow-xl shadow-primary/20 sticky top-28">
                            <h3 class="text-xl font-bold mb-4">শিক্ষকের মন্তব্য</h3>
                            <div class="space-y-4">
                                <div class="p-4 bg-white/10 rounded-2xl border border-white/20">
                                    <p class="text-sm italic">"সাহেদ গত সপ্তাহে খুব ভালো করেছে, বিশেষ করে গণিত পরীক্ষায়
                                        তার উন্নতি চোখে পড়ার মতো।"</p>
                                    <p class="text-[10px] font-bold mt-2 opacity-60">— প্রফেসর সাহেদ আহমেদ</p>
                                </div>
                            </div>
                            <button
                                class="w-full mt-6 py-3 bg-white text-primary rounded-xl font-bold text-sm hover:scale-105 transition-all">শিক্ষককে
                                বার্তা পাঠান</button>
                        </div>
                    </div>
                </div>
                <% }); %>
                    <% } else { %>
                        <div
                            class="py-20 text-center bg-white dark:bg-slate-900 rounded-[3rem] border-2 border-dashed border-slate-200 dark:border-slate-800">
                            <span class="material-symbols-outlined text-6xl text-slate-300 mb-4">child_care</span>
                            <h3 class="text-xl font-bold">কোনো সন্তান কানেক্ট করা নেই</h3>
                            <p class="text-slate-500 mt-2">আপনার সন্তানের ইমেইল দিয়ে তাকে আপনার প্রোফাইলের সাথে যোগ
                                করুন।</p>
                        </div>
                        <% } %>
    </main>

    <!-- Simple Add Child Modal -->
    <div id="add-child-modal"
        class="hidden fixed inset-0 bg-black/50 backdrop-blur-sm z-[100] flex items-center justify-center p-6">
        <div class="max-w-md w-full bg-white dark:bg-slate-900 rounded-[2.5rem] p-10 shadow-2xl relative">
            <button onclick="document.getElementById('add-child-modal').classList.add('hidden')"
                class="absolute top-6 right-6 text-slate-400 hover:text-red-500 transition-colors">
                <span class="material-symbols-outlined">close</span>
            </button>
            <h3 class="text-2xl font-black mb-2 text-slate-900 dark:text-white text-center">সন্তান যোগ করুন</h3>
            <p class="text-center text-slate-500 mb-8 text-sm">আপনার সন্তানের ওধ্যায় (ODDHAY) ইমেইল ঠিকানাটি দিন।</p>
            <form action="/parent/add-child" method="POST" class="space-y-4">
                <input type="email" name="childEmail" placeholder="সন্তানের ইমেইল (উদা: child@example.com)"
                    class="w-full px-5 py-4 rounded-2xl bg-slate-50 dark:bg-slate-800 border-none text-sm" required>
                <button type="submit"
                    class="w-full py-4 bg-primary text-white rounded-2xl font-black shadow-lg shadow-primary/30">লিংক
                    করুন</button>
            </form>
        </div>
    </div>
</body>

</html>