<!DOCTYPE html>
<html lang="bn">

<head>
    <meta charset="UTF-8">
    <title>Admin - Q&A</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-slate-50 p-10">
    <h1 class="text-3xl font-bold mb-8">Q&A প্রশ্নপত্র উত্তর দিন (Admin)</h1>
    <div class="space-y-6">
        <% qas.forEach(item=> { %>
            <div
                class="bg-white p-6 rounded-xl shadow-md border-l-4 <%= item.status === 'resolved' ? 'border-green-500' : 'border-amber-500' %>">
                <div class="flex justify-between mb-2">
                    <span class="font-bold text-gray-700">প্রশ্নকর্তা: <%= item.askedBy ? item.askedBy.name : 'Unknown'
                            %></span>
                    <span class="text-xs text-gray-400">
                        <%= item.createdAt.toLocaleString() %>
                    </span>
                </div>
                <p class="text-lg font-bold mb-4">"<%= item.question %>"</p>

                <% if(item.status==='open' ) { %>
                    <form action="/admin/qa-answer/<%= item._id %>" method="POST" class="space-y-3">
                        <textarea name="answer" placeholder="এখানে সমাধানটি লিখুন..."
                            class="w-full border p-3 rounded-lg bg-slate-50 focus:ring-2 focus:ring-blue-500 outline-none"
                            rows="3" required></textarea>
                        <button type="submit" class="bg-indigo-600 text-white px-6 py-2 rounded-lg font-bold">উত্তর সেভ
                            করুন</button>
                    </form>
                    <% } else { %>
                        <div class="bg-green-50 p-4 rounded-lg">
                            <p class="text-xs font-bold text-green-700 mb-1">প্রদত্ত উত্তর:</p>
                            <p>
                                <%= item.answer %>
                            </p>
                        </div>
                        <% } %>
            </div>
            <% }) %>
    </div>
</body>

</html>