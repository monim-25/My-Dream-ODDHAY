<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>কোর্স ম্যানেজমেন্ট | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..700&family=Noto+Sans+Bengali:wght@300..700&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#137fec",
                        "background-light": "#f6f7f8",
                        "background-dark": "#101922",
                    },
                    fontFamily: {
                        "display": ["Lexend", "Noto Sans Bengali", "sans-serif"],
                    },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Lexend', 'Noto Sans Bengali', sans-serif;
        }
    </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-[#0d141b] dark:text-slate-200">
    <div class="flex h-screen overflow-hidden">
        <aside
            class="w-64 flex-shrink-0 border-r border-slate-200 dark:border-slate-800 bg-white dark:bg-background-dark p-4 flex flex-col gap-6">
            <a href="/admin/teacher" class="flex items-center gap-3 px-2">
                <div class="bg-primary rounded-lg p-1.5 text-white"><span
                        class="material-symbols-outlined">arrow_back</span></div>
                <h1 class="text-sm font-bold">ড্যাশবোর্ডে ফিরুন</h1>
            </a>
        </aside>

        <main class="flex-1 overflow-y-auto p-8">
            <h2 class="text-3xl font-black mb-2">
                <%= course.title %>
            </h2>
            <p class="text-slate-500 mb-10">
                <%= course.category %> • কন্টেন্ট ম্যানেজমেন্ট
            </p>

            <div class="space-y-12">
                <!-- Add Chapter Section -->
                <section
                    class="bg-white dark:bg-slate-900 p-8 rounded-3xl border border-slate-200 dark:border-slate-800 shadow-sm">
                    <h3 class="text-xl font-bold mb-6 flex items-center gap-2">
                        <span class="material-symbols-outlined text-primary">folder_open</span> চ্যাপ্টার/অধ্যায় যোগ
                        করুন
                    </h3>
                    <form action="/admin/course/<%= course._id %>/add-chapter" method="POST" class="flex gap-4">
                        <input type="text" name="title" placeholder="চ্যাপ্টারের নাম (উদা: অধ্যায় ১: ভেক্টর)"
                            class="flex-1 px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none outline-none focus:ring-2 focus:ring-primary"
                            required>
                        <button type="submit" class="px-8 bg-primary text-white rounded-xl font-bold">যোগ করুন</button>
                    </form>
                </section>

                <!-- Chapters List -->
                <% (course.chapters || []).forEach(chapter=> { %>
                    <div
                        class="bg-white dark:bg-slate-900 rounded-[2.5rem] border border-slate-200 dark:border-slate-800 overflow-hidden shadow-sm">
                        <div
                            class="p-8 bg-slate-50 dark:bg-slate-800/50 border-b border-slate-100 dark:border-slate-700 flex justify-between items-center">
                            <h3 class="text-xl font-black">
                                <%= chapter.title %>
                            </h3>
                            <button class="text-red-500 hover:bg-red-50 p-2 rounded-lg transition-colors">
                                <span class="material-symbols-outlined">delete</span>
                            </button>
                        </div>

                        <div class="p-8 grid grid-cols-1 md:grid-cols-3 gap-8">
                            <!-- Recorded Classes -->
                            <div class="space-y-6">
                                <h4 class="font-bold flex items-center gap-2 text-primary">
                                    <span class="material-symbols-outlined">video_library</span> রেকর্ড ক্লাস
                                </h4>
                                <form action="/admin/course/<%= course._id %>/chapter/<%= chapter._id %>/add-recorded"
                                    method="POST" enctype="multipart/form-data" class="space-y-3">
                                    <input type="text" name="title" placeholder="ক্লাসের নাম"
                                        class="w-full px-4 py-2 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 text-sm"
                                        required>
                                    <input type="file" name="video" accept="video/*" class="w-full text-xs" required>
                                    <button type="submit"
                                        class="w-full py-2 bg-primary text-white rounded-lg text-xs font-bold">ভিডিও
                                        আপলোড</button>
                                </form>
                                <div class="space-y-2">
                                    <% chapter.recordedClasses.forEach(v=> { %>
                                        <div
                                            class="flex items-center justify-between p-3 bg-slate-50 dark:bg-slate-800/50 rounded-xl text-xs">
                                            <span class="truncate pr-2">
                                                <%= v.title %>
                                            </span>
                                            <span class="text-slate-400 shrink-0">Video</span>
                                        </div>
                                        <% }) %>
                                </div>
                            </div>

                            <!-- Live Classes -->
                            <div class="space-y-6">
                                <h4 class="font-bold flex items-center gap-2 text-rose-500">
                                    <span class="material-symbols-outlined">live_tv</span> লাইভ ক্লাস
                                </h4>
                                <form action="/admin/course/<%= course._id %>/chapter/<%= chapter._id %>/add-live"
                                    method="POST" class="space-y-3">
                                    <input type="text" name="title" placeholder="টপিক"
                                        class="w-full px-4 py-2 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 text-sm"
                                        required>
                                    <input type="url" name="meetingUrl" placeholder="মিটিং লিঙ্ক (Zoom/Meet)"
                                        class="w-full px-4 py-2 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 text-sm"
                                        required>
                                    <input type="datetime-local" name="date"
                                        class="w-full px-4 py-2 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 text-sm">
                                    <button type="submit"
                                        class="w-full py-2 bg-rose-500 text-white rounded-lg text-xs font-bold">লিঙ্ক
                                        সেভ করুন</button>
                                </form>
                                <div class="space-y-2">
                                    <% chapter.liveClasses.forEach(l=> { %>
                                        <div
                                            class="flex items-center justify-between p-3 bg-slate-50 dark:bg-slate-800/50 rounded-xl text-xs">
                                            <span class="truncate pr-2">
                                                <%= l.title %>
                                            </span>
                                            <span class="text-rose-400 shrink-0">Live</span>
                                        </div>
                                        <% }) %>
                                </div>
                            </div>

                            <!-- Notes -->
                            <div class="space-y-6">
                                <h4 class="font-bold flex items-center gap-2 text-amber-500">
                                    <span class="material-symbols-outlined">description</span> লেকচার নোটস
                                </h4>
                                <form action="/admin/course/<%= course._id %>/chapter/<%= chapter._id %>/add-note"
                                    method="POST" enctype="multipart/form-data" class="space-y-3">
                                    <input type="text" name="title" placeholder="নোটের নাম"
                                        class="w-full px-4 py-2 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 text-sm"
                                        required>
                                    <input type="file" name="note" accept=".pdf,.doc,.docx" class="w-full text-xs"
                                        required>
                                    <button type="submit"
                                        class="w-full py-2 bg-amber-500 text-white rounded-lg text-xs font-bold">ফাইল
                                        আপলোড</button>
                                </form>
                                <div class="space-y-2">
                                    <% chapter.notes.forEach(n=> { %>
                                        <div
                                            class="flex items-center justify-between p-3 bg-slate-50 dark:bg-slate-800/50 rounded-xl text-xs">
                                            <span class="truncate pr-2 text-amber-600 font-medium">
                                                <%= n.title %>
                                            </span>
                                            <span class="material-symbols-outlined text-sm">pdf_off</span>
                                        </div>
                                        <% }) %>
                                </div>
                            </div>
                        </div>
                    </div>
                    <% }) %>
            </div>
        </main>
    </div>
</body>

</html>