<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <title>লগইন | ODDHAY</title>
    <%- include('partials/head') %>
        <style>
            .auth-card {
                background: rgba(255, 255, 255, 0.8);
                backdrop-filter: blur(20px);
                border: 1px solid rgba(255, 255, 255, 0.4);
            }

            .dark .auth-card {
                background: rgba(15, 23, 42, 0.8);
                border: 1px solid rgba(255, 255, 255, 0.05);
            }
        </style>
</head>

<body
    class="bg-[#f8fafc] dark:bg-slate-950 text-slate-900 dark:text-slate-100 min-h-screen flex items-center justify-center p-6 relative overflow-x-hidden">
    <!-- Blurs -->
    <div class="absolute top-[-10%] left-[-10%] size-[500px] bg-primary/10 rounded-full blur-[120px] -z-10"></div>
    <div class="absolute bottom-[-10%] right-[-10%] size-[500px] bg-blue-500/10 rounded-full blur-[120px] -z-10"></div>

    <div class="max-w-md w-full auth-card p-12 rounded-[2.5rem] shadow-2xl reveal active text-center">
        <a href="/" class="inline-flex items-center gap-2 mb-10 group">
            <div class="text-primary group-hover:rotate-12 transition-transform duration-300">
                <svg class="size-12" fill="none" viewbox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
                    <path d="M4 4H17.3334V17.3334H30.6666V30.6666H44V44H4V4Z" fill="currentColor"></path>
                </svg>
            </div>
            <span class="text-3xl font-black">ODDHAY</span>
        </a>

        <h1 class="text-2xl font-black mb-2">আবার স্বাগতম!</h1>
        <p class="text-[10px] text-slate-500 font-black uppercase tracking-[0.2em] mb-10">আপনার অ্যাকাউন্টে লগইন করুন
        </p>

        <form action="/login" method="POST" class="space-y-5 text-left">
            <div class="space-y-4">
                <div class="relative group">
                    <span
                        class="material-symbols-outlined absolute left-5 top-1/2 -translate-y-1/2 text-slate-400 text-lg group-focus-within:text-primary transition-colors">mail</span>
                    <input type="email" name="email" placeholder="ইমেইল অ্যাড্রেস"
                        class="w-full pl-13 pr-6 py-4 rounded-2xl bg-slate-50 dark:bg-slate-900/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-xs"
                        required>
                </div>
                <div class="relative group">
                    <span
                        class="material-symbols-outlined absolute left-5 top-1/2 -translate-y-1/2 text-slate-400 text-lg group-focus-within:text-primary transition-colors">lock</span>
                    <input type="password" name="password" placeholder="পাসওয়ার্ড"
                        class="w-full pl-13 pr-6 py-4 rounded-2xl bg-slate-50 dark:bg-slate-900/50 border-none focus:ring-2 focus:ring-primary transition-all font-bold text-xs"
                        required>
                </div>
            </div>

            <div class="flex justify-end px-2">
                <a href="#"
                    class="text-[9px] font-black text-slate-400 hover:text-primary transition-colors uppercase tracking-widest">পাসওয়ার্ড
                    ভুলে গেছেন?</a>
            </div>

            <button type="submit"
                class="w-full py-4.5 bg-primary text-white rounded-2xl font-bold text-sm shadow-xl shadow-primary/20 hover:scale-[1.02] active:scale-95 transition-all flex items-center justify-center gap-2">
                লগইন করুন <span class="material-symbols-outlined text-[20px]">login</span>
            </button>
        </form>

        <p class="text-center mt-12 text-[10px] text-slate-500 font-bold uppercase tracking-widest">
            অ্যাকাউন্ট নেই? <a href="/register" class="text-primary hover:underline font-black">নতুন তৈরি করুন</a>
        </p>
    </div>
</body>

</html>