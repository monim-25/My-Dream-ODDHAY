<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>প্রোফাইল এডিট | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..900&family=Noto+Sans+Bengali:wght@300..900&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <script>
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: { "primary": "#137fec" },
                    fontFamily: { "display": ["Lexend", "Noto Sans Bengali", "sans-serif"] },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Lexend', 'Noto Sans Bengali', sans-serif;
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom, 20px);
        }
    </style>
</head>

<body class="bg-slate-50 dark:bg-[#0f172a] text-slate-900 dark:text-slate-100 min-h-screen pb-20">
    <!-- Header -->
    <header
        class="bg-white dark:bg-slate-900 border-b border-slate-200 dark:border-slate-800 px-4 py-4 sticky top-0 z-50">
        <div class="max-w-lg mx-auto flex items-center gap-3">
            <a href="/profile" class="p-2 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-xl">
                <span class="material-symbols-outlined">arrow_back</span>
            </a>
            <h1 class="text-lg font-black">প্রোফাইল এডিট</h1>
        </div>
    </header>

    <main class="max-w-lg mx-auto p-4 py-6">

        <% if(success) { %>
            <div class="mb-4 p-4 bg-emerald-100 text-emerald-700 rounded-xl text-sm font-bold">
                সফলভাবে আপডেট হয়েছে!
            </div>
            <% } %>

                <% if(error) { %>
                    <div class="mb-4 p-4 bg-red-100 text-red-700 rounded-xl text-sm font-bold">
                        <%= error %>
                    </div>
                    <% } %>

                        <!-- Profile Picture -->
                        <div
                            class="bg-white dark:bg-slate-900 p-6 rounded-2xl border border-slate-200 dark:border-slate-800 mb-4 text-center">
                            <div
                                class="size-24 rounded-2xl bg-primary/10 border-2 border-primary/20 mx-auto mb-4 flex items-center justify-center overflow-hidden">
                                <% if(user.profilePicture) { %>
                                    <img src="<%= user.profilePicture %>" class="w-full h-full object-cover">
                                    <% } else { %>
                                        <span class="material-symbols-outlined text-primary text-5xl">person</span>
                                        <% } %>
                            </div>
                            <p class="text-sm font-bold text-slate-500">প্রোফাইল ছবি</p>
                        </div>

                        <!-- Edit Form -->
                        <form action="/profile/update" method="POST"
                            class="bg-white dark:bg-slate-900 p-6 rounded-2xl border border-slate-200 dark:border-slate-800 space-y-4">

                            <div>
                                <label class="block text-xs font-bold text-slate-500 mb-1">নাম</label>
                                <input type="text" name="name" value="<%= user.name %>"
                                    class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm font-bold">
                            </div>

                            <div>
                                <label class="block text-xs font-bold text-slate-500 mb-1">ইমেইল</label>
                                <input type="email" name="email" value="<%= user.email %>"
                                    class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm font-bold"
                                    readonly>
                            </div>

                            <div>
                                <label class="block text-xs font-bold text-slate-500 mb-1">ফোন</label>
                                <input type="text" name="phone" value="<%= user.phone || '' %>"
                                    class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm font-bold">
                            </div>

                            <div>
                                <label class="block text-xs font-bold text-slate-500 mb-1">ক্লাস লেভেল</label>
                                <select name="classLevel"
                                    class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm font-bold">
                                    <option value="Class 6" <%=user.classLevel==='Class 6' ? 'selected' : '' %>>Class 6
                                    </option>
                                    <option value="Class 7" <%=user.classLevel==='Class 7' ? 'selected' : '' %>>Class 7
                                    </option>
                                    <option value="Class 8" <%=user.classLevel==='Class 8' ? 'selected' : '' %>>Class 8
                                    </option>
                                    <option value="Class 9" <%=user.classLevel==='Class 9' ? 'selected' : '' %>>Class 9
                                    </option>
                                    <option value="Class 10" <%=user.classLevel==='Class 10' ? 'selected' : '' %>>Class
                                        10</option>
                                    <option value="HSC 1st Year" <%=user.classLevel==='HSC 1st Year' ? 'selected' : ''
                                        %>>HSC 1st Year</option>
                                    <option value="HSC 2nd Year" <%=user.classLevel==='HSC 2nd Year' ? 'selected' : ''
                                        %>>HSC 2nd Year</option>
                                </select>
                            </div>

                            <div class="pt-2">
                                <button type="submit"
                                    class="w-full py-3.5 bg-primary text-white rounded-xl font-black text-sm">সংরক্ষণ
                                    করুন</button>
                            </div>
                        </form>

                        <!-- Change Password -->
                        <form action="/profile/change-password" method="POST"
                            class="bg-white dark:bg-slate-900 p-6 rounded-2xl border border-slate-200 dark:border-slate-800 space-y-4 mt-4">
                            <h3 class="font-black text-sm mb-2">পাসওয়ার্ড পরিবর্তন</h3>

                            <div>
                                <label class="block text-xs font-bold text-slate-500 mb-1">বর্তমান পাসওয়ার্ড</label>
                                <input type="password" name="currentPassword"
                                    class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm font-bold">
                            </div>

                            <div>
                                <label class="block text-xs font-bold text-slate-500 mb-1">নতুন পাসওয়ার্ড</label>
                                <input type="password" name="newPassword"
                                    class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm font-bold">
                            </div>

                            <div>
                                <label class="block text-xs font-bold text-slate-500 mb-1">নতুন পাসওয়ার্ড নিশ্চিত
                                    করুন</label>
                                <input type="password" name="confirmPassword"
                                    class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm font-bold">
                            </div>

                            <div class="pt-2">
                                <button type="submit"
                                    class="w-full py-3.5 bg-slate-800 dark:bg-slate-700 text-white rounded-xl font-black text-sm">পাসওয়ার্ড
                                    পরিবর্তন</button>
                            </div>
                        </form>
    </main>

    <!-- Mobile Bottom Nav -->
    <nav
        class="fixed bottom-0 left-0 right-0 z-[100] bg-white dark:bg-slate-900 border-t border-slate-100 dark:border-slate-800 lg:hidden pb-safe">
        <div class="flex items-center justify-around h-16">
            <a href="/dashboard" class="flex flex-col items-center gap-0.5 text-slate-400">
                <span class="material-symbols-outlined text-xl">home</span>
                <span class="text-[9px] font-black">হোম</span>
            </a>
            <a href="/messages" class="flex flex-col items-center gap-0.5 text-slate-400">
                <span class="material-symbols-outlined text-xl">mail</span>
                <span class="text-[9px] font-black">মেসেজ</span>
            </a>
            <a href="/downloads" class="flex flex-col items-center gap-0.5 text-slate-400">
                <span class="material-symbols-outlined text-xl">download</span>
                <span class="text-[9px] font-black">ডাউনলোড</span>
            </a>
            <a href="/profile" class="flex flex-col items-center gap-0.5 text-primary">
                <span class="material-symbols-outlined text-xl">person</span>
                <span class="text-[9px] font-black">প্রোফাইল</span>
            </a>
        </div>
    </nav>
</body>

</html>