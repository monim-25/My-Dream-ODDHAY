<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | অধ্যায় - ODDHAY</title>
    <!-- Fonts -->
    <link
        href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Hind+Siliguri:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <!-- Icons -->
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Outfit', 'Hind Siliguri', 'sans-serif'],
                    },
                    colors: {
                        primary: '#2563EB',
                        secondary: '#4F46E5',
                        accent: '#06B6D4',
                        dark: '#0F172A',
                        light: '#F8FAFC'
                    }
                }
            }
        }
    </script>
    <style>
        body {
            font-family: 'Outfit', 'Hind Siliguri', sans-serif;
        }

        .glass-effect {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
        }

        .dark .glass-effect {
            background: rgba(15, 23, 42, 0.95);
        }

        .pb-safe {
            padding-bottom: env(safe-area-inset-bottom);
        }
    </style>
</head>

<body
    class="bg-gray-50 text-slate-800 dark:bg-[#0B1120] dark:text-slate-100 min-h-screen transition-colors duration-300">

    <!-- Top Navigation (Mobile & Desktop) -->
    <header class="fixed top-0 left-0 right-0 z-50 glass-effect border-b border-slate-200 dark:border-slate-800 h-16">
        <div class="max-w-7xl mx-auto px-4 h-full flex items-center justify-between">
            <!-- Logo & Hamburger -->
            <div class="flex items-center gap-3">
                <button onclick="toggleSidebar()"
                    class="lg:hidden p-2 -ml-2 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-lg">
                    <span class="material-symbols-outlined">menu</span>
                </button>
                <a href="/" class="flex items-center gap-2">
                    <div
                        class="size-8 rounded-lg bg-primary text-white flex items-center justify-center font-bold text-xl">
                        O
                    </div>
                    <span class="font-bold text-lg hidden sm:block">ODDHAY</span>
                </a>
            </div>

            <!-- Right Actions -->
            <div class="flex items-center gap-3">
                <!-- Notifications -->
                <button class="relative p-2 rounded-lg hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors">
                    <span class="material-symbols-outlined text-slate-600 dark:text-slate-300">notifications</span>
                    <% if(notifications && notifications.filter(n=> !n.isRead).length > 0) { %>
                        <span
                            class="absolute top-1.5 right-1.5 size-2.5 bg-red-500 rounded-full border-2 border-white dark:border-[#0F172A]"></span>
                        <% } %>
                </button>

                <!-- Profile Dropdown -->
                <div class="relative group">
                    <button
                        class="flex items-center gap-2 pl-2 rounded-lg hover:bg-slate-100 dark:hover:bg-slate-800 transition-all">
                        <img src="<%= user.profileImage || `https://ui-avatars.com/api/?name=${user.name}&background=random` %>"
                            alt="Profile"
                            class="size-8 rounded-full border border-slate-200 dark:border-slate-700 object-cover">
                        <span class="text-sm font-medium hidden md:block max-w-[100px] truncate">
                            <%= user.name.split(' ')[0] %></span>
                        <span class="material-symbols-outlined text-slate-400 text-sm hidden md:block">expand_more</span>
                    </button>
                    <!-- Dropdown Menu -->
                    <div class="absolute right-0 top-full mt-2 w-48 bg-white dark:bg-slate-800 rounded-xl shadow-lg border border-slate-100 dark:border-slate-700 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50 transform origin-top-right">
                        <div class="p-2 space-y-1">
                            <a href="/profile" class="flex items-center gap-2 px-3 py-2 text-sm rounded-lg hover:bg-slate-50 dark:hover:bg-slate-700/50">
                                <span class="material-symbols-outlined text-lg">person</span> প্রোফাইল
                            </a>
                            <a href="/settings" class="flex items-center gap-2 px-3 py-2 text-sm rounded-lg hover:bg-slate-50 dark:hover:bg-slate-700/50">
                                <span class="material-symbols-outlined text-lg">settings</span> সেটিংস
                            </a>
                            <div class="h-px bg-slate-100 dark:bg-slate-700 my-1"></div>
                            <a href="/logout" class="flex items-center gap-2 px-3 py-2 text-sm text-red-500 rounded-lg hover:bg-red-50 dark:hover:bg-red-500/10">
                                <span class="material-symbols-outlined text-lg">logout</span> লগআউট
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- Sidebar (Desktop) -->
    <aside id="sidebar" class="fixed top-16 left-0 bottom-0 w-64 bg-white dark:bg-[#0B1120] border-r border-slate-200 dark:border-slate-800 z-40 transform -translate-x-full lg:translate-x-0 transition-transform duration-300 overflow-y-auto hidden lg:block">
        <div class="p-4 space-y-1">
            <div class="px-3 py-2 text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2">মেনু</div>
            
            <a href="/dashboard" class="flex items-center gap-3 px-3 py-2.5 bg-primary/10 text-primary rounded-xl font-medium">
                <span class="material-symbols-outlined">dashboard</span> ড্যাশবোর্ড
            </a>
            <a href="/courses" class="flex items-center gap-3 px-3 py-2.5 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-800/50 rounded-xl font-medium transition-colors">
                <span class="material-symbols-outlined">school</span> আমার কোর্স
            </a>
            <a href="/routine" class="flex items-center gap-3 px-3 py-2.5 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-800/50 rounded-xl font-medium transition-colors">
                <span class="material-symbols-outlined">event_note</span> রুটিন
            </a>
            <a href="/qa" class="flex items-center gap-3 px-3 py-2.5 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-800/50 rounded-xl font-medium transition-colors">
                <span class="material-symbols-outlined">quiz</span> প্রশ্ন ব্যাংক
            </a>
            <a href="/analytics" class="flex items-center gap-3 px-3 py-2.5 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-800/50 rounded-xl font-medium transition-colors">
                <span class="material-symbols-outlined">analytics</span> এনালিটিক্স
            </a>

            <div class="h-px bg-slate-100 dark:bg-slate-800 my-4"></div>

            <div class="px-3 py-2 text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2">অন্যান্য</div>
            <a href="/leaderboard" class="flex items-center gap-3 px-3 py-2.5 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-800/50 rounded-xl font-medium transition-colors">
                <span class="material-symbols-outlined">leaderboard</span> লিডারবোর্ড
            </a>
            <a href="/achievements" class="flex items-center gap-3 px-3 py-2.5 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-800/50 rounded-xl font-medium transition-colors">
                <span class="material-symbols-outlined">emoji_events</span> অর্জনসমূহ
            </a>
        </div>
    </aside>

    <!-- Mobile Overlay -->
    <div id="mobile-overlay" onclick="toggleSidebar()" class="fixed inset-0 bg-black/50 z-30 hidden lg:hidden backdrop-blur-sm transition-opacity"></div>

    <!-- Main Content -->
    <main class="lg:ml-64 pt-20 pb-24 lg:pb-8 min-h-screen px-4">
        <div class="max-w-5xl mx-auto space-y-6">
            
            <!-- Welcome Banner -->
            <div class="bg-gradient-to-r from-primary to-indigo-600 rounded-3xl p-6 md:p-10 text-white shadow-xl relative overflow-hidden">
                <!-- Background Decoration -->
                <div class="absolute top-0 right-0 w-64 h-64 bg-white/10 rounded-full -translate-y-1/2 translate-x-1/3 blur-3xl"></div>
                <div class="absolute bottom-0 left-0 w-48 h-48 bg-black/10 rounded-full translate-y-1/2 -translate-x-1/3 blur-2xl"></div>
                
                <div class="relative z-10">
                    <p class="text-white/80 text-sm font-medium mb-1">শুভ্র সকাল 👋</p>
                    <h1 class="text-2xl md:text-4xl font-extrabold mb-2">স্বাগতম, <%= user.name.split(' ')[0] %>!</h1>
                    <p class="text-white/90 text-sm md:text-base max-w-lg leading-relaxed">
                        তোমার পড়াশোনা চালিয়ে যাও এবং নতুন কিছু শিখো। আজকের দিনটি হোক সফলতার! 🚀
                    </p>
                    
                    <div class="flex flex-wrap gap-3 mt-6">
                        <a href="/courses" class="inline-flex items-center gap-2 px-5 py-2.5 bg-white text-primary rounded-xl font-bold text-sm hover:bg-slate-50 transition-colors shadow-sm">
                            <span class="material-symbols-outlined text-lg">play_lesson</span> শেখা শুরু করুন
                        </a>
                        <a href="/analytics" class="inline-flex items-center gap-2 px-5 py-2.5 bg-white/20 text-white rounded-xl font-bold text-sm hover:bg-white/30 transition-colors backdrop-blur-sm">
                            <span class="material-symbols-outlined text-lg">monitoring</span> অগ্রগতি দেখুন
                        </a>
                    </div>
                </div>
            </div>

            <!-- Stats Grid -->
            <div class="grid grid-cols-2 md:grid-cols-4 gap-3 md:gap-4">
                <!-- Enrolled -->
                <div class="bg-white dark:bg-slate-800 p-4 rounded-2xl border border-slate-100 dark:border-slate-700 shadow-sm">
                    <div class="flex items-center gap-3 mb-2">
                        <div class="p-2 bg-blue-50 dark:bg-blue-900/30 text-blue-500 rounded-lg">
                            <span class="material-symbols-outlined text-xl">school</span>
                        </div>
                        <span class="text-xs font-bold text-slate-400 uppercase">এনরোল্ড</span>
                    </div>
                    <p class="text-2xl font-black text-slate-800 dark:text-white">
                        <%= user.enrolledCourses ? user.enrolledCourses.length : 0 %>
                    </p>
                </div>

                <!-- Completed -->
                <div class="bg-white dark:bg-slate-800 p-4 rounded-2xl border border-slate-100 dark:border-slate-700 shadow-sm">
                    <div class="flex items-center gap-3 mb-2">
                        <div class="p-2 bg-emerald-50 dark:bg-emerald-900/30 text-emerald-500 rounded-lg">
                            <span class="material-symbols-outlined text-xl">check_circle</span>
                        </div>
                        <span class="text-xs font-bold text-slate-400 uppercase">সম্পন্ন</span>
                    </div>
                    <p class="text-2xl font-black text-slate-800 dark:text-white">
                        <%= user.completedLessons ? user.completedLessons.length : 0 %>
                    </p>
                </div>

                <!-- Quiz -->
                <div class="bg-white dark:bg-slate-800 p-4 rounded-2xl border border-slate-100 dark:border-slate-700 shadow-sm">
                    <div class="flex items-center gap-3 mb-2">
                        <div class="p-2 bg-amber-50 dark:bg-amber-900/30 text-amber-500 rounded-lg">
                            <span class="material-symbols-outlined text-xl">quiz</span>
                        </div>
                        <span class="text-xs font-bold text-slate-400 uppercase">কুইজ</span>
                    </div>
                    <p class="text-2xl font-black text-slate-800 dark:text-white">
                        <%= user.quizResults ? user.quizResults.length : 0 %>
                    </p>
                </div>

                <!-- Points -->
                <div class="bg-white dark:bg-slate-800 p-4 rounded-2xl border border-slate-100 dark:border-slate-700 shadow-sm">
                    <div class="flex items-center gap-3 mb-2">
                        <div class="p-2 bg-purple-50 dark:bg-purple-900/30 text-purple-500 rounded-lg">
                            <span class="material-symbols-outlined text-xl">stars</span>
                        </div>
                        <span class="text-xs font-bold text-slate-400 uppercase">পয়েন্ট</span>
                    </div>
                    <p class="text-2xl font-black text-slate-800 dark:text-white">
                        <%= (user.quizResults || []).reduce((acc, curr) => acc + (curr.score || 0), 0) %>
                    </p>
                </div>
            </div>

            <!-- Content Grid (2 Columns on Desktop) -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                
                <!-- Continue Learning -->
                <div class="bg-white dark:bg-slate-800 p-5 rounded-2xl border border-slate-100 dark:border-slate-700 shadow-sm">
                    <h3 class="text-sm font-bold text-slate-400 uppercase tracking-widest mb-4 flex items-center gap-2">
                        <span class="material-symbols-outlined text-primary text-lg">play_circle</span> শেখা চালিয়ে যান
                    </h3>
                    
                    <% if(user.lastWatchedLesson && user.lastWatchedLesson.course) { %>
                        <div class="flex items-start gap-4">
                            <div class="size-20 rounded-xl bg-slate-100 dark:bg-slate-700 overflow-hidden shrink-0">
                                <img src="<%= user.lastWatchedLesson.course.thumbnail || ' /images/course-default.jpg'
                                %>" class="w-full h-full object-cover">
                </div>
                <div class="flex-1 min-w-0">
                    <h4 class="font-bold text-slate-800 dark:text-white truncate mb-1">
                        <%= user.lastWatchedLesson.lessonTitle %>
                    </h4>
                    <p class="text-xs text-slate-500 dark:text-slate-400 mb-3 truncate">
                        <%= user.lastWatchedLesson.course.title %>
                    </p>
                    <a href="/course-details/<%= user.lastWatchedLesson.course._id %>"
                        class="inline-flex items-center gap-1.5 px-3 py-1.5 bg-primary text-white text-xs font-bold rounded-lg hover:bg-blue-700 transition-colors">
                        চালিয়ে যান <span class="material-symbols-outlined text-sm">arrow_forward</span>
                    </a>
                </div>
            </div>
            <% } else { %>
                <div class="py-6 text-center">
                    <div class="inline-flex p-3 rounded-full bg-slate-100 dark:bg-slate-800 text-slate-400 mb-2">
                        <span class="material-symbols-outlined text-2xl">video_library</span>
                    </div>
                    <p class="text-sm text-slate-500">এখনো কোনো ভিডিও দেখা হয়নি</p>
                    <a href="/courses" class="inline-block mt-2 text-xs font-bold text-primary">কোর্স দেখুন</a>
                </div>
                <% } %>
        </div>

        <!-- Routine / Tasks -->
        <div class="bg-white dark:bg-slate-800 p-5 rounded-2xl border border-slate-100 dark:border-slate-700 shadow-sm">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-sm font-bold text-slate-400 uppercase tracking-widest flex items-center gap-2">
                    <span class="material-symbols-outlined text-teal-500 text-lg">task_alt</span> আজকের টাস্ক
                </h3>
                <a href="/routine" class="p-1 rounded hover:bg-slate-100 dark:hover:bg-slate-700 text-slate-400">
                    <span class="material-symbols-outlined text-lg">add</span>
                </a>
            </div>

            <div class="space-y-3">
                <% if(routineTasks && routineTasks.length> 0) { %>
                    <% routineTasks.slice(0, 3).forEach(task=> { %>
                        <div
                            class="flex items-center gap-3 p-3 rounded-xl bg-slate-50 dark:bg-slate-800/50 border border-slate-100 dark:border-slate-700/50">
                            <div
                                class="size-8 rounded-full bg-teal-100 dark:bg-teal-900/30 text-teal-600 dark:text-teal-400 flex items-center justify-center shrink-0">
                                <span class="material-symbols-outlined text-sm">check</span>
                            </div>
                            <div class="flex-1 min-w-0">
                                <p class="text-sm font-bold text-slate-800 dark:text-slate-200 truncate">
                                    <%= task.title %>
                                </p>
                                <p class="text-[10px] text-slate-500 font-medium">
                                    <%= task.time || 'সময় অনির্ধারিত' %>
                                </p>
                            </div>
                        </div>
                        <% }) %>
                            <% } else { %>
                                <div class="py-6 text-center">
                                    <p class="text-sm text-slate-400">কোনো টাস্ক বাকি নেই 🎉</p>
                                    <a href="/routine" class="text-xs font-bold text-primary mt-1 inline-block">নতুন
                                        টাস্ক যোগ করুন</a>
                                </div>
                                <% } %>
            </div>
        </div>

        </div>

        <!-- Recommended Courses -->
        <div class="bg-white dark:bg-slate-800 p-5 rounded-2xl border border-slate-100 dark:border-slate-700 shadow-sm">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-sm font-bold text-slate-400 uppercase tracking-widest">আপনার জন্য জদাই করা</h3>
                <a href="/courses" class="text-xs font-bold text-primary hover:underline">সব দেখুন</a>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <% if(recommendations && recommendations.length> 0) { %>
                    <% recommendations.slice(0, 3).forEach(course=> { %>
                        <a href="/course-details/<%= course._id %>"
                            class="group block p-3 rounded-xl border border-slate-100 dark:border-slate-700 hover:border-primary/30 hover:shadow-md transition-all bg-white dark:bg-slate-800">
                            <div
                                class="aspect-video rounded-lg bg-slate-100 dark:bg-slate-700 overflow-hidden mb-3 relative">
                                <img src="<%= course.thumbnail || '/images/course-default.jpg' %>"
                                    class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500">
                                <div
                                    class="absolute top-2 right-2 px-2 py-0.5 bg-black/60 backdrop-blur-sm rounded text-[10px] font-bold text-white">
                                    <%= course.subject %>
                                </div>
                            </div>
                            <h4
                                class="font-bold text-slate-800 dark:text-white truncate group-hover:text-primary transition-colors">
                                <%= course.title %>
                            </h4>
                            <div class="flex items-center justify-between mt-2">
                                <span class="text-xs text-slate-500">
                                    <%= course.instructor || 'ODDHAY' %>
                                </span>
                                <span
                                    class="text-xs font-bold text-primary px-2 py-1 bg-primary/5 rounded-md">বিস্তারিত</span>
                            </div>
                        </a>
                        <% }) %>
                            <% } else { %>
                                <div class="col-span-full py-8 text-center text-slate-400">
                                    <span class="material-symbols-outlined text-3xl mb-2">search_off</span>
                                    <p class="text-sm">এই মুহূর্তে কোনো নতুন সাজেশন নেই</p>
                                </div>
                                <% } %>
            </div>
        </div>

        </div>
        </main>

        <!-- Mobile Bottom Nav -->
        <nav
            class="fixed bottom-0 left-0 right-0 h-16 bg-white dark:bg-[#0B1120] border-t border-slate-200 dark:border-slate-800 flex items-center justify-around lg:hidden z-50 pb-safe">
            <a href="/dashboard" class="flex flex-col items-center justify-center w-full h-full text-primary">
                <span class="material-symbols-outlined text-2xl">dashboard</span>
                <span class="text-[10px] font-medium mt-0.5">হোম</span>
            </a>
            <a href="/courses"
                class="flex flex-col items-center justify-center w-full h-full text-slate-400 hover:text-primary transition-colors">
                <span class="material-symbols-outlined text-2xl">school</span>
                <span class="text-[10px] font-medium mt-0.5">কোর্স</span>
            </a>
            <a href="/routine"
                class="flex flex-col items-center justify-center w-full h-full text-slate-400 hover:text-primary transition-colors">
                <span class="material-symbols-outlined text-2xl">event_note</span>
                <span class="text-[10px] font-medium mt-0.5">রুটিন</span>
            </a>
            <a href="/menu"
                class="flex flex-col items-center justify-center w-full h-full text-slate-400 hover:text-primary transition-colors">
                <span class="material-symbols-outlined text-2xl">menu</span>
                <span class="text-[10px] font-medium mt-0.5">মেনু</span>
            </a>
        </nav>

        <script>
            // Sidebar Toggle
            function toggleSidebar() {
                const sidebar = document.getElementById('sidebar');
                const overlay = document.getElementById('mobile-overlay');

                if (sidebar.classList.contains('-translate-x-full')) {
                    // Open
                    sidebar.classList.remove('-translate-x-full', 'hidden');
                    overlay.classList.remove('hidden');
                    document.body.style.overflow = 'hidden';
                } else {
                    // Close
                    sidebar.classList.add('-translate-x-full');
                    setTimeout(() => sidebar.classList.add('hidden'), 300);
                    overlay.classList.add('hidden');
                    document.body.style.overflow = '';
                }
            }

            // Dark Mode Check
            if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
                document.documentElement.classList.add('dark');
            }
        </script>
</body>

</html>