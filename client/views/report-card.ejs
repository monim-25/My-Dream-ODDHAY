<!DOCTYPE html>
<html lang="bn">

<head>
    <%- include('partials/head') %>
        <title>একাডেমিক রিপোর্ট কার্ড | অধ্যায়</title>
        <style>
            .report-card {
                max-width: 900px;
                margin: 40px auto;
                background: white;
                padding: 60px;
                border-radius: 4rem;
                box-shadow: 0 40px 100px -20px rgba(0, 0, 0, 0.15);
                position: relative;
                overflow: hidden;
                border: 1px solid #f1f5f9;
            }

            .watermark {
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%) rotate(-30deg);
                font-size: 15rem;
                color: rgba(99, 102, 241, 0.03);
                font-weight: 900;
                pointer-events: none;
                white-space: nowrap;
                display: flex;
                align-items: center;
                justify-content: center;
            }

            .progress-circle {
                width: 120px;
                height: 120px;
                border-radius: 50%;
                background: conic-gradient(#3b82f6 <%=stats.avgScore %>%, #f1f5f9 0);
                display: flex;
                align-items: center;
                justify-content: center;
                position: relative;
            }

            .progress-circle::after {
                content: '';
                position: absolute;
                width: 100px;
                height: 100px;
                background: white;
                border-radius: 50%;
            }
        </style>
</head>

<body class="bg-slate-50 text-slate-900 font-sans p-4">

    <div class="max-w-5xl mx-auto py-8">
        <a href="/profile"
            class="inline-flex items-center gap-2 text-slate-500 font-bold hover:text-primary transition-all bg-white px-4 py-2 rounded-xl shadow-sm border border-slate-100">
            <span class="material-symbols-outlined">arrow_back</span> প্রোফাইলে ফিরে যান
        </a>
    </div>

    <div class="report-card">
        <div class="watermark uppercase tracking-tighter">Oddhay</div>

        <!-- Header -->
        <div
            class="flex flex-col md:flex-row justify-between items-start md:items-center mb-8 md:mb-16 gap-4 md:gap-8 relative z-10">
            <div>
                <div class="flex items-center gap-2 md:gap-3 mb-3 md:mb-6">
                    <div
                        class="size-8 md:size-10 bg-primary rounded-xl flex items-center justify-center text-white shrink-0">
                        <span class="material-symbols-outlined text-lg md:text-2xl">menu_book</span>
                    </div>
                    <span class="text-2xl md:text-3xl font-black tracking-tighter uppercase text-primary">Oddhay</span>
                </div>
                <h1 class="text-2xl md:text-4xl lg:text-5xl font-black mb-1 md:mb-2 tracking-tight">একাডেমিক রিপোর্ট
                    কার্ড</h1>
                <p class="text-slate-400 font-bold text-[9px] md:text-xs tracking-wide md:tracking-widest uppercase">
                    Academic Performance Record</p>
            </div>

            <div
                class="flex flex-row md:flex-col items-center gap-3 md:gap-2 bg-blue-50/60 md:bg-transparent p-2.5 md:p-0 rounded-2xl w-full md:w-auto border border-blue-100/50 md:border-0">
                <div class="progress-circle shadow-lg shadow-blue-200/40 shrink-0 scale-[0.7] md:scale-100">
                    <span class="relative z-10 text-lg md:text-2xl font-black text-blue-600">
                        <%= stats.avgScore %>%
                    </span>
                </div>
                <p
                    class="text-[9px] md:text-[10px] font-black text-slate-700 md:text-slate-400 uppercase tracking-wide md:tracking-widest text-left md:text-center leading-tight">
                    ওভারঅল<br class="md:hidden" /> প্রগ্রেস</p>
            </div>
        </div>

        <!-- Student Info -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-8 mb-8 md:mb-16 relative z-10">
            <div
                class="p-5 md:p-8 bg-slate-900 text-white rounded-2xl md:rounded-[3rem] shadow-xl relative overflow-hidden">
                <div class="absolute right-0 top-0 size-40 bg-white/5 rounded-full -mr-20 -mt-20 blur-3xl"></div>
                <div class="flex items-center gap-3 md:gap-6">
                    <div
                        class="size-14 md:size-20 rounded-xl md:rounded-2xl bg-white/10 flex items-center justify-center overflow-hidden border border-white/20 shrink-0">
                        <img src="<%= user.profileImage || `https://ui-avatars.com/api/?name=${user.name}&background=random` %>"
                            class="w-full h-full object-cover">
                    </div>
                    <div class="flex-1 min-w-0">
                        <p
                            class="text-[7px] md:text-[9px] font-black text-white/50 uppercase tracking-widest mb-0.5 md:mb-1">
                            শিক্ষার্থীর নাম
                        </p>
                        <h2 class="text-base md:text-2xl font-black leading-tight truncate">
                            <%= user.name %>
                        </h2>
                        <p class="text-[9px] md:text-xs font-bold text-white/60 mt-0.5 md:mt-1">Class <%=
                                user.classLevel || 'N/A' %>
                        </p>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-2 gap-3 md:gap-4">
                <div
                    class="p-3 md:p-6 bg-slate-50 rounded-xl md:rounded-3xl border border-slate-100 flex flex-col items-center justify-center text-center">
                    <span
                        class="material-symbols-outlined text-accent mb-1 text-2xl md:text-3xl">local_fire_department</span>
                    <p class="text-xl md:text-2xl font-black">
                        <%= stats.daysLearning || 1 %>
                    </p>
                    <p
                        class="text-[7px] md:text-[10px] font-black text-slate-400 uppercase tracking-wide md:tracking-widest leading-tight">
                        শেখার দিন<br class="md:hidden" /><span class="hidden md:inline"> </span>(STREAK)</p>
                </div>
                <div
                    class="p-3 md:p-6 bg-slate-50 rounded-xl md:rounded-3xl border border-slate-100 flex flex-col items-center justify-center text-center">
                    <span class="material-symbols-outlined text-emerald-500 mb-1 text-2xl md:text-3xl">stars</span>
                    <p class="text-xl md:text-2xl font-black text-emerald-600">
                        <%= (user.quizResults || []).reduce((acc, curr)=> acc + (curr.score || 0), 0) %>
                    </p>
                    <p
                        class="text-[7px] md:text-[10px] font-black text-slate-400 uppercase tracking-wide md:tracking-widest leading-tight">
                        অর্জিত<br class="md:hidden" /><span class="hidden md:inline"> </span>পয়েন্ট</p>
                </div>
            </div>
        </div>

        <!-- Course Summary -->
        <div class="relative z-10">
            <h4 class="text-lg md:text-xl font-black mb-4 md:mb-8 flex items-center gap-2 md:gap-3">
                <span class="size-2 md:size-3 bg-accent rounded-full"></span> কোর্স প্রগ্রেস সামারি
            </h4>
            <div class="bg-slate-50 rounded-2xl md:rounded-[3rem] p-3 md:p-4 lg:p-8 space-y-3 md:space-y-4">
                <% if(user.enrolledCourses && user.enrolledCourses.length> 0) { %>
                    <% user.enrolledCourses.forEach(enroll=> { %>
                        <div
                            class="flex items-center justify-between p-6 bg-white rounded-3xl border border-slate-100 shadow-sm">
                            <div class="flex items-center gap-4">
                                <div
                                    class="size-10 bg-slate-100 rounded-xl flex items-center justify-center text-slate-400">
                                    <span class="material-symbols-outlined">auto_stories</span>
                                </div>
                                <div>
                                    <p class="font-black text-slate-800">
                                        <%= enroll.course ? enroll.course.title : 'নির্ধারিত নয়' %>
                                    </p>
                                    <p class="text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                                        <%= enroll.course ? enroll.course.subject : 'সাধারণ' %>
                                    </p>
                                </div>
                            </div>
                            <div class="text-right hidden sm:block">
                                <span
                                    class="px-3 py-1 bg-emerald-50 text-emerald-600 rounded-full text-[10px] font-black uppercase">সক্রিয়</span>
                            </div>
                        </div>
                        <% }) %>
                            <% } else { %>
                                <p class="text-slate-400 font-bold italic text-sm text-center py-8">কোনো কোর্সে এনরোল
                                    করা নেই।</p>
                                <% } %>
            </div>
        </div>

        <!-- Footer Info -->
        <div class="mt-20 text-center border-t border-slate-100 pt-12 relative z-10">
            <p class="text-xl font-black text-slate-300 transform rotate-12 mb-8 hidden lg:block opacity-50">VERIFIED
                Oddhay Academic Record</p>
            <p class="text-[10px] text-slate-400 font-bold tracking-widest uppercase">এটি একটি ডিজিটাল জেনারেটেড রিপোর্ট
                কার্ড - অধ্যায় এডুকেশন ফ্যামিলি</p>
        </div>
    </div>

</body>

</html>