<!DOCTYPE html>
<html lang="bn">

<head>
    <%- include('partials/head') %>
        <title>একাডেমিক রিপোর্ট কার্ড | ওড্ধায়</title>
        <style>
            @media print {
                .no-print {
                    display: none;
                }

                body {
                    background: white;
                }

                .report-card {
                    border: none;
                    box-shadow: none;
                }
            }

            .report-card {
                max-width: 800px;
                margin: 40px auto;
                background: white;
                padding: 50px;
                border-radius: 40px;
                box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.1);
                position: relative;
                overflow: hidden;
                border: 1px solid #f1f5f9;
            }

            .watermark {
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%) rotate(-30deg);
                font-size: 15rem;
                color: rgba(99, 102, 241, 0.03);
                font-weight: 900;
                pointer-events: none;
                white-space: nowrap;
            }

            .stat-badge {
                background: #f8fafc;
                padding: 20px;
                border-radius: 20px;
                text-align: center;
            }
        </style>
</head>

<body class="bg-slate-50 text-slate-900 font-sans">

    <div class="max-w-4xl mx-auto px-6 py-10 no-print flex justify-between items-center">
        <a href="/profile"
            class="flex items-center gap-2 text-slate-500 font-bold hover:text-primary transition-colors">
            <span class="material-symbols-outlined">arrow_back</span> প্রোফাইলে ফিরে যান
        </a>
        <button onclick="window.print()"
            class="px-6 py-3 bg-primary text-white rounded-2xl font-black flex items-center gap-2 shadow-lg shadow-primary/20 hover:scale-105 active:scale-95 transition-all">
            <span class="material-symbols-outlined">print</span> প্রিন্ট করুন
        </button>
    </div>

    <div class="report-card">
        <div class="watermark uppercase">Oddhay</div>

        <!-- Header -->
        <div class="flex justify-between items-start mb-16 relative z-10">
            <div>
                <div class="flex items-center gap-3 mb-6">
                    <div class="text-primary text-4xl">
                        <svg class="size-10" fill="none" viewBox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
                            <path d="M4 4H17.3334V17.3334H30.6666V30.6666H44V44H4V4Z" fill="currentColor"></path>
                        </svg>
                    </div>
                    <span class="text-3xl font-black tracking-tighter uppercase">Oddhay</span>
                </div>
                <h1 class="text-4xl font-black mb-2">একাডেমিক রিপোর্ট কার্ড</h1>
                <p class="text-slate-400 font-bold text-sm tracking-widest uppercase">Academic Session 2024-25</p>
            </div>
            <div class="text-right">
                <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest leading-none mb-1">প্রদানের
                    তারিখ</p>
                <p class="text-sm font-black">
                    <%= new Date().toLocaleDateString('bn-BD') %>
                </p>
            </div>
        </div>

        <!-- Student Profile -->
        <div
            class="flex items-center gap-8 p-10 bg-slate-900 text-white rounded-[2.5rem] mb-12 relative z-10 overflow-hidden">
            <div class="absolute right-0 top-0 size-40 bg-white/5 rounded-full -mr-20 -mt-20 blur-3xl"></div>
            <div
                class="size-24 rounded-3xl bg-white/10 flex items-center justify-center text-4xl font-black border border-white/20 shrink-0">
                <%= user.name[0] %>
            </div>
            <div>
                <p class="text-[10px] font-black text-white/50 uppercase tracking-[0.2em] mb-1">শিক্ষার্থীর তথ্য</p>
                <h2 class="text-2xl font-black mb-1">
                    <%= user.name %>
                </h2>
                <div class="flex items-center gap-4 text-sm font-bold text-white/70">
                    <span class="flex items-center gap-1.5"><span
                            class="material-symbols-outlined text-[16px]">school</span>
                        <%= user.classLevel || 'N/A' %>
                    </span>
                    <span class="flex items-center gap-1.5"><span
                            class="material-symbols-outlined text-[16px]">id_card</span> ID: <%=
                            user._id.toString().slice(-6).toUpperCase() %></span>
                </div>
            </div>
        </div>

        <!-- Stats Grid -->
        <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mb-12 relative z-10">
            <div class="stat-badge">
                <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-2 leading-none">গড় নম্বর
                </p>
                <h4 class="text-2xl font-black text-primary">
                    <%= stats.avgScore %>%
                </h4>
            </div>
            <div class="stat-badge">
                <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-2 leading-none">কুইজ সম্পন্ন
                </p>
                <h4 class="text-2xl font-black">
                    <%= stats.totalQuizzes %>
                </h4>
            </div>
            <div class="stat-badge">
                <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-2 leading-none">স্টাডি
                    আওয়ার</p>
                <h4 class="text-2xl font-black">
                    <%= stats.learningHours %>h
                </h4>
            </div>
            <div class="stat-badge">
                <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-2 leading-none">কোর্স
                    প্রগ্রেস</p>
                <h4 class="text-2xl font-black text-emerald-500">
                    <%= stats.completedCourses %>
                </h4>
            </div>
        </div>

        <!-- Course Summary -->
        <div class="mb-12 relative z-10">
            <h4 class="text-lg font-black mb-6 flex items-center gap-2">কোর্স প্রগ্রেস সামারি <div
                    class="h-[1px] flex-1 bg-slate-100"></div>
            </h4>
            <div class="space-y-4">
                <% if(user.enrolledCourses && user.enrolledCourses.length> 0) { %>
                    <% user.enrolledCourses.forEach(enroll=> { %>
                        <div class="flex items-center justify-between p-4 border-b border-slate-50">
                            <div>
                                <p class="font-black text-sm">
                                    <%= enroll.course ? enroll.course.title : 'Deleted Course' %>
                                </p>
                                <p class="text-[10px] font-bold text-slate-400">
                                    <%= enroll.course ? enroll.course.subject : '' %>
                                </p>
                            </div>
                            <div class="text-right">
                                <p class="text-xs font-black">সক্রিয়</p>
                                <p class="text-[9px] font-bold text-slate-400">একাডেমিক</p>
                            </div>
                        </div>
                        <% }) %>
                            <% } else { %>
                                <p class="text-slate-400 font-bold italic text-sm">কোনো কোর্সে এনরোল করা নেই।</p>
                                <% } %>
            </div>
        </div>

        <!-- Footer -->
        <div class="mt-20 flex justify-between items-end relative z-10">
            <div class="w-48 text-center">
                <div class="h-[1px] bg-slate-200 mb-4"></div>
                <p class="text-[10px] font-black text-slate-400 uppercase">কর্তৃপক্ষের স্বাক্ষর</p>
            </div>
            <div class="text-center">
                <div
                    class="size-16 bg-slate-50 rounded-full flex items-center justify-center mb-2 mx-auto border border-slate-100 italic font-black text-slate-300 transform rotate-12">
                    SEAL
                </div>
                <p class="text-[8px] font-black text-slate-300 uppercase">Verified Academic Record</p>
            </div>
            <div class="w-48 text-center">
                <div class="h-[1px] bg-slate-200 mb-4"></div>
                <p class="text-[10px] font-black text-slate-400 uppercase">শিক্ষকের স্বাক্ষর</p>
            </div>
        </div>

        <div class="mt-16 text-center">
            <p class="text-[10px] text-slate-300 font-bold">ওড্ধায় অনলাইন এডুকেশন প্ল্যাটফর্ম - www.oddhay.com</p>
        </div>
    </div>

</body>

</html>