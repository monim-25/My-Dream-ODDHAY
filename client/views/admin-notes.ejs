<!DOCTYPE html>
<html lang="bn">

<head>
    <meta charset="UTF-8">
    <title>Admin - Notes</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-slate-50 p-10">
    <h1 class="text-3xl font-bold mb-8">নোটস ম্যানেজমেন্ট (Admin)</h1>
    <form action="/admin/add-note" method="POST" enctype="multipart/form-data"
        class="bg-white p-6 rounded-xl shadow-md mb-10">
        <div class="grid grid-cols-2 gap-4">
            <input type="text" name="title" placeholder="নোটের শিরোনাম" class="border p-2 rounded" required>
            <input type="text" name="subject" placeholder="বিষয় (যেমন: পদার্থবিজ্ঞান)" class="border p-2 rounded"
                required>
            <input type="text" name="classLevel" placeholder="ক্লাস (যেমন: Class 10)" class="border p-2 rounded"
                required>
            <input type="file" name="note" class="border p-2 rounded">
        </div>
        <textarea name="description" placeholder="সংক্ষিপ্ত বর্ণনা" class="w-full border p-2 rounded mt-4"></textarea>
        <button type="submit" class="mt-4 bg-blue-600 text-white px-6 py-2 rounded font-bold">নোট যোগ করুন</button>
    </form>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% notes.forEach(note=> { %>
            <div class="bg-white p-4 rounded-lg shadow">
                <h3 class="font-bold">
                    <%= note.title %>
                </h3>
                <p class="text-sm text-gray-500">
                    <%= note.subject %> | <%= note.classLevel %>
                </p>
                <a href="<%= note.fileUrl %>" target="_blank" class="text-blue-500 text-xs mt-2 block">নোট ফাইল
                    ডাউনলোড</a>
            </div>
            <% }) %>
    </div>
</body>

</html>