<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>কুইজ ম্যানেজমেন্ট | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..700&family=Noto+Sans+Bengali:wght@300..700&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: { "primary": "#137fec", "background-light": "#f6f7f8", "background-dark": "#101922" },
                    fontFamily: { "display": ["Lexend", "Noto Sans Bengali", "sans-serif"] },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Lexend', 'Noto Sans Bengali', sans-serif;
        }
    </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-[#0d141b] dark:text-slate-200">
    <div class="flex h-screen overflow-hidden">
        <aside
            class="w-64 border-r border-slate-200 dark:border-slate-800 bg-white dark:bg-background-dark p-4 flex flex-col gap-6">
            <a href="/admin/teacher" class="flex items-center gap-3 px-2">
                <div class="bg-primary rounded-lg p-1.5 text-white"><span
                        class="material-symbols-outlined">arrow_back</span></div>
                <h1 class="text-sm font-bold">টিচার প্যানেল</h1>
            </a>
        </aside>

        <main class="flex-1 overflow-y-auto p-8">
            <h2 class="text-3xl font-black mb-10 text-slate-900 dark:text-white">মক এক্সাম ম্যানেজমেন্ট</h2>

            <!-- Add Quiz Form -->
            <div
                class="bg-white dark:bg-slate-900 p-8 rounded-3xl border border-slate-200 dark:border-slate-800 mb-12 shadow-sm">
                <h3 class="text-xl font-bold mb-6 flex items-center gap-2">
                    <span class="material-symbols-outlined text-primary">quiz</span> নতুন কুইজ যোগ করুন
                </h3>
                <form action="/admin/add-quiz" method="POST" class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    <div class="md:col-span-1">
                        <label class="block text-sm font-bold mb-2">কুইজ শিরোনাম</label>
                        <input type="text" name="title"
                            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm"
                            placeholder="উদা: ফিজিক্স ২য় অধ্যায় মক" required>
                    </div>
                    <div>
                        <label class="block text-sm font-bold mb-2">কোর্স সিলেক্ট করুন</label>
                        <select name="courseId"
                            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm">
                            <% courses.forEach(course=> { %>
                                <option value="<%= course._id %>">
                                    <%= course.title %>
                                </option>
                                <% }); %>
                        </select>
                    </div>
                    <div>
                        <label class="block text-sm font-bold mb-2">সময় (মিনিট)</label>
                        <input type="number" name="duration"
                            class="w-full px-4 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border-none text-sm"
                            value="10" required>
                    </div>
                    <div class="md:col-span-3 flex justify-end">
                        <button type="submit"
                            class="bg-primary text-white px-8 py-3 rounded-xl font-bold hover:opacity-90 shadow-md shadow-primary/20">কুইজ
                            তৈরি করুন</button>
                    </div>
                </form>
            </div>

            <!-- Quiz List -->
            <h3 class="text-xl font-bold mb-6">বর্তমান কুইজসমূহ</h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <% quizzes.forEach(quiz=> { %>
                    <div
                        class="bg-white dark:bg-slate-900 p-6 rounded-2xl border border-slate-200 dark:border-slate-800 flex justify-between items-center hover:border-primary transition-all group">
                        <div>
                            <h4 class="font-bold mb-1">
                                <%= quiz.title %>
                            </h4>
                            <p class="text-xs text-slate-500">কোর্স: <%= quiz.course ? quiz.course.title : 'N/A' %> •
                                    <%= quiz.questions.length %> টি প্রশ্ন</p>
                        </div>
                        <div class="flex gap-2">
                            <a href="/quiz/<%= quiz._id %>"
                                class="p-2 text-primary bg-primary/10 rounded-lg hover:bg-primary/20 transition-all font-bold text-xs"
                                target="_blank">View Live</a>
                            <button class="p-2 text-red-500 hover:bg-red-500/10 rounded-lg"><span
                                    class="material-symbols-outlined">delete</span></button>
                        </div>
                    </div>
                    <% }); %>
            </div>
        </main>
    </div>
</body>

</html>