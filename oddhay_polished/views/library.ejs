<!DOCTYPE html>
<html class="light" lang="bn">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>ভার্চুয়াল লাইব্রেরি | ODDHAY</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..900&family=Noto+Sans+Bengali:wght@300..900&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: { "primary": "#137fec", "background-light": "#f6f7f8", "background-dark": "#101922" },
                    fontFamily: { "display": ["Lexend", "Noto Sans Bengali", "sans-serif"] },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Lexend', 'Noto Sans Bengali', sans-serif;
        }
    </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-[#0d141b] dark:text-slate-100 min-h-screen">
    <!-- Navbar -->
    <header
        class="border-b border-slate-200 dark:border-slate-800 bg-white/80 dark:bg-background-dark/80 backdrop-blur-md px-6 py-4 sticky top-0 z-[100]">
        <div class="max-w-7xl mx-auto flex items-center justify-between">
            <a href="/dashboard" class="flex items-center gap-3">
                <div class="text-primary"><span class="material-symbols-outlined text-3xl">local_library</span></div>
                <h1 class="text-xl font-bold">ভার্চুয়াল লাইব্রেরি</h1>
            </a>
            <div class="hidden md:flex items-center gap-8 text-sm font-bold">
                <a href="/" class="hover:text-primary transition-colors">হোম</a>
                <a href="/dashboard" class="hover:text-primary transition-colors">ড্যাশবোর্ড</a>
                <a href="/courses" class="hover:text-primary transition-colors">কোর্সসমূহ</a>
            </div>
            <div class="flex items-center gap-4">
                <button class="p-2 rounded-xl bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-300"
                    onclick="document.documentElement.classList.toggle('dark')">
                    <span class="material-symbols-outlined block dark:hidden">dark_mode</span>
                    <span class="material-symbols-outlined hidden dark:block">light_mode</span>
                </button>
                <a href="/profile"
                    class="hidden md:flex size-10 rounded-full bg-primary/10 items-center justify-center font-bold text-primary hover:bg-primary hover:text-white transition-all">
                    <%= locals.user ? user.name[0] : 'U' %>
                </a>
                <button class="md:hidden p-2 text-slate-600 dark:text-slate-300" onclick="toggleMobileMenu()">
                    <span class="material-symbols-outlined" id="menu-icon">menu</span>
                </button>
            </div>
        </div>

        <!-- Mobile Menu Overlay -->
        <div id="mobile-menu"
            class="hidden fixed inset-0 z-[90] bg-white dark:bg-background-dark p-6 pt-24 transition-all duration-300 translate-x-full">
            <nav class="flex flex-col gap-6 text-center">
                <a class="text-2xl font-black" href="/">হোম</a>
                <a class="text-2xl font-black" href="/dashboard">ড্যাশবোর্ড</a>
                <a class="text-2xl font-black" href="/courses">কোর্সসমূহ</a>
                <a class="text-2xl font-black text-primary" href="/library">লাইব্রেরি</a>
                <a class="text-2xl font-black" href="/profile">প্রোফাইল</a>
                <hr class="border-slate-100 dark:border-slate-800">
                <a href="/logout" class="text-xl font-bold text-red-500">লগআউট</a>
            </nav>
        </div>
    </header>

    <script>
        function toggleMobileMenu() {
            const menu = document.getElementById('mobile-menu');
            const icon = document.getElementById('menu-icon');
            if (menu.classList.contains('hidden')) {
                menu.classList.remove('hidden');
                setTimeout(() => menu.classList.remove('translate-x-full'), 10);
                icon.innerText = 'close';
            } else {
                menu.classList.add('translate-x-full');
                setTimeout(() => menu.classList.add('hidden'), 300);
                icon.innerText = 'menu';
            }
        }
    </script>

    <main class="max-w-7xl mx-auto px-6 py-12">
        <div class="flex flex-col md:flex-row justify-between items-end gap-6 mb-12">
            <div>
                <h2 class="text-4xl font-black mb-4 tracking-tight">পড়ুন এবং নোটস সংগ্রহ করুন</h2>
                <p class="text-slate-500 max-w-xl">আপনার ক্লাস এবং পছন্দসই বিষয় অনুযায়ী বই এবং স্পেশাল নোটস খুঁজে পেতে
                    নিচের ফিল্টার ব্যবহার করুন।</p>
            </div>

            <div class="flex gap-3">
                <select id="class-filter"
                    class="px-4 py-2.5 rounded-xl border-none bg-white dark:bg-slate-900 shadow-sm text-sm font-bold focus:ring-2 focus:ring-primary">
                    <option value="all">সব ক্লাস</option>
                    <option value="Class 9">ক্লাস ৯</option>
                    <option value="Class 10">ক্লাস ১০</option>
                    <option value="HSC">HSC</option>
                </select>
                <select id="type-filter"
                    class="px-4 py-2.5 rounded-xl border-none bg-white dark:bg-slate-900 shadow-sm text-sm font-bold focus:ring-2 focus:ring-primary">
                    <option value="all">সবই</option>
                    <option value="Book">বই (Reading)</option>
                    <option value="Note">স্পেশাল নোটস</option>
                </select>
            </div>
        </div>

        <!-- Library Grid -->
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-8">
            <% books.forEach(item=> { %>
                <div class="library-item group" data-class="<%= item.classLevel %>" data-type="<%= item.type %>">
                    <div
                        class="aspect-[3/4] rounded-2xl bg-slate-200 dark:bg-slate-800 overflow-hidden relative shadow-md hover:shadow-2xl transition-all duration-500 hover:-translate-y-2">
                        <% if(item.thumbnail) { %>
                            <img src="<%= item.thumbnail %>" alt="<%= item.title %>" class="w-full h-full object-cover">
                            <% } else { %>
                                <div class="w-full h-full flex flex-col items-center justify-center text-slate-400">
                                    <span class="material-symbols-outlined text-4xl mb-2">
                                        <%= item.type==='Book' ? 'menu_book' : 'description' %>
                                    </span>
                                    <span class="text-[10px] font-bold uppercase">
                                        <%= item.type %>
                                    </span>
                                </div>
                                <% } %>
                                    <div
                                        class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity flex items-end p-4">
                                        <a href="<%= item.fileUrl %>" target="_blank"
                                            class="w-full py-2 bg-primary text-white text-xs font-bold rounded-lg text-center backdrop-blur-md">পড়ুন</a>
                                    </div>
                    </div>
                    <div class="mt-4">
                        <span
                            class="text-[10px] font-bold text-primary bg-primary/10 px-2 py-0.5 rounded-full uppercase mb-2 inline-block">
                            <%= item.category %>
                        </span>
                        <h4 class="font-bold text-sm truncate">
                            <%= item.title %>
                        </h4>
                        <p class="text-[10px] text-slate-500 mt-1">
                            <%= item.classLevel %>
                        </p>
                    </div>
                </div>
                <% }); %>

                    <% if(books.length===0) { %>
                        <div class="col-span-full py-20 text-center">
                            <span class="material-symbols-outlined text-6xl text-slate-300">auto_stories</span>
                            <h3 class="text-xl font-bold mt-4">লাইব্রেরিতে আপাতত কিছু নেই।</h3>
                            <p class="text-slate-500">শীঘ্রই নতুন বই এবং নোটস যোগ করা হবে।</p>
                        </div>
                        <% } %>
        </div>
    </main>

    <script>
        const classFilter = document.getElementById('class-filter');
        const typeFilter = document.getElementById('type-filter');
        const items = document.querySelectorAll('.library-item');

        function filter() {
            const classVal = classFilter.value;
            const typeVal = typeFilter.value;

            items.forEach(item => {
                const itemClass = item.getAttribute('data-class');
                const itemType = item.getAttribute('data-type');

                const showClass = (classVal === 'all' || itemClass === classVal);
                const showType = (typeVal === 'all' || itemType === typeVal);

                if (showClass && showType) {
                    item.style.display = 'block';
                } else {
                    item.style.display = 'none';
                }
            });
        }

        classFilter.addEventListener('change', filter);
        typeFilter.addEventListener('change', filter);
    </script>
</body>

</html>